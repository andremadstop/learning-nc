---
phase: 01-cli-state-machine
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/src/components/PbqCli.vue
autonomous: false
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05

must_haves:
  truths:
    - "conf t typed in a Router> terminal changes the prompt to Router(config)#"
    - "interface FastEthernet0/0 typed in config mode changes the prompt to Router(config-if)#"
    - "exit in config-if mode goes to Router(config)#, exit in config mode goes to Router>"
    - "end in config or config-if mode always goes to Router>"
    - "typing gibberish shows % Invalid input detected at '^' marker. as an output line"
    - "a command defined in command_outputs shows the configured text as output lines"
    - "prompt stays at the new mode for subsequent commands (mode persists)"
    - "existing questions with initial_prompt and no domain field still work unchanged"
    - "multi-line command_outputs values render as separate lines (not a single line with \\n)"
  artifacts:
    - path: "app/src/components/PbqCli.vue"
      provides: "State-machine-aware CLI terminal component"
      contains:
        - "import { evaluateCommand, getPrompt, DOMAIN_SCHEMAS } from '../utils/cliStateMachine'"
        - "termModes in data()"
        - "termContexts in data()"
        - "getPrompt(term) method"
        - "submitCommand rewritten to use evaluateCommand()"
  key_links:
    - from: "PbqCli.vue submitCommand"
      to: "evaluateCommand() in cliStateMachine.js"
      via: "ES module import, called on every Enter keypress"
      pattern: "evaluateCommand\\("
    - from: "PbqCli.vue template"
      to: "getPrompt() method"
      via: "{{ currentPrompt(term) }} in input row"
      pattern: "currentPrompt"
    - from: "PbqCli.vue emit"
      to: "TrainingService::scorePbqAnswer"
      via: "$emit('update', term.name, [...history]) — format must stay 'PROMPT CMD\\n...'"
      pattern: "\\$emit\\('update'"
---

<objective>
Integrate the cliStateMachine.js utility into PbqCli.vue. Add per-terminal mode/context state, replace the static prompt with a dynamic computed prompt, rewrite submitCommand() to use evaluateCommand(), and render multi-line command output without v-html.

Purpose: This makes the terminal interactive — users see mode changes in real time and get feedback for every command, which is required for Cisco IOS exam simulation.
Output: app/src/components/PbqCli.vue with full state machine integration. All five CLI requirements satisfied.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-state-machine/01-RESEARCH.md
@.planning/phases/01-cli-state-machine/01-01-SUMMARY.md
@app/src/components/PbqCli.vue
@app/src/utils/cliStateMachine.js
</context>

<interfaces>
<!-- Imports this plan consumes from 01-01 -->

From app/src/utils/cliStateMachine.js:
  import { evaluateCommand, getPrompt, DOMAIN_SCHEMAS } from '../utils/cliStateMachine'

  evaluateCommand(cmd, domain, currentMode, context, commandOutputs)
    -> { type, nextMode, nextContext, lines: string[] }

  getPrompt(domain, mode, host, context)
    -> string

Current PbqCli.vue data() structure (to be extended):
  data() {
    const inputBuffers = {}   // { [termName]: '' }
    const localHistory = {}   // { [termName]: string[] }
    -- ADD: termModes = {}    // { [termName]: string }  e.g. { Router: 'exec' }
    -- ADD: termContexts = {} // { [termName]: Object }  e.g. { Router: {} }
    return { inputBuffers, localHistory, termModes, termContexts }
  }

Current submitCommand (to be replaced):
  line 53-67 in PbqCli.vue — pushes "prompt + ' ' + cmd" and emits update

Backend scoring contract (DO NOT BREAK):
  TrainingService.php line 1369: implode("\n", $history) then preg_match pattern
  History format must remain: each entry is the full line as typed including prompt,
  e.g. ["Router> show version", "Router(config)# interface Fa0/0"]
  Output lines from command_outputs and error messages are also in history (fine --
  they won't accidentally match scoring patterns for IP/interface commands).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add state to data() and import cliStateMachine</name>
  <files>app/src/components/PbqCli.vue</files>
  <action>
Add the ES module import at the top of the script block (after the opening script tag):
  import { evaluateCommand, getPrompt, DOMAIN_SCHEMAS } from '../utils/cliStateMachine'

Extend data() to initialize termModes and termContexts alongside the existing inputBuffers and localHistory. The initialization loop already iterates over this.config.terminals -- extend it:

  data() {
    const inputBuffers = {}
    const localHistory = {}
    const termModes = {}
    const termContexts = {}
    for (const term of (this.config.terminals || [])) {
      inputBuffers[term.name] = ''
      localHistory[term.name] = Array.isArray(this.value[term.name]) ? [...this.value[term.name]] : []
      const domain = this.config.domain || 'generic'
      const schema = DOMAIN_SCHEMAS[domain] || DOMAIN_SCHEMAS.generic
      termModes[term.name] = schema.defaultMode
      termContexts[term.name] = {}
    }
    return { inputBuffers, localHistory, termModes, termContexts }
  }

Add a currentPrompt(term) method that delegates to getPrompt():
  currentPrompt(term) {
    const domain = this.config.domain || 'generic'
    const mode = this.termModes[term.name]
    const context = this.termContexts[term.name] || {}
    -- Backward compat: if no domain field and generic would produce "termname> ",
    -- but term.initial_prompt is set, use initial_prompt instead.
    if (!this.config.domain && term.initial_prompt) {
      return term.initial_prompt
    }
    return getPrompt(domain, mode, term.name, context)
  }

In the template, replace the static prompt binding:
  BEFORE: <span class="pbq-terminal-prompt">{{ term.initial_prompt }} </span>
  AFTER:  <span class="pbq-terminal-prompt">{{ currentPrompt(term) }} </span>

(Keep the trailing space inside the span for visual separation from input.)
  </action>
  <verify>Manual: build and load in browser. A question with domain: "cisco_ios" and terminal name "Router" must show "Router>" prompt. A question with no domain field and initial_prompt: "Switch>" must still show "Switch>".</verify>
  <done>Import present in script block. termModes and termContexts initialized in data() for each terminal. currentPrompt() method exists. Template uses currentPrompt(term) instead of term.initial_prompt. Backward compat: no-domain questions show term.initial_prompt unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite submitCommand() and add multi-line output rendering</name>
  <files>app/src/components/PbqCli.vue</files>
  <action>
Replace the submitCommand(term) method body. Keep the method signature and the $nextTick scroll logic at the end unchanged.

New submitCommand(term) body:
  const cmd = this.inputBuffers[term.name].trim()
  if (!cmd) return

  const domain = this.config.domain || 'generic'
  const currentMode = this.termModes[term.name]
  const context = this.termContexts[term.name] || {}
  const commandOutputs = this.config.command_outputs || {}

  -- Build the prompt line for this command (the line that appears as typed)
  const promptStr = this.currentPrompt(term)
  const cmdLine = promptStr + ' ' + cmd

  -- Evaluate the command against the state machine
  const result = evaluateCommand(cmd, domain, currentMode, context, commandOutputs)

  -- Build the history array update
  const history = this.localHistory[term.name] || []
  history.push(cmdLine)
  for (const line of result.lines) {
    history.push(line)
  }

  -- Update reactive state using $set (Vue 2 reactivity safe)
  this.$set(this.localHistory, term.name, history)
  this.$set(this.termModes, term.name, result.nextMode)
  this.$set(this.termContexts, term.name, result.nextContext)

  -- Clear input
  this.inputBuffers[term.name] = ''

  -- Emit (keep existing emit contract: full history array per terminal)
  this.$emit('update', term.name, [...history])

  -- Scroll (keep existing $nextTick scroll logic)
  this.$nextTick(() => {
    const refs = this.$refs['body_' + term.name]
    const el = Array.isArray(refs) ? refs[0] : refs
    if (el) el.scrollTop = el.scrollHeight
  })

NC CSP compliance for output lines: the terminal body already uses:
  <div v-for="(line, i) in getHistory(term.name)" :key="i" class="pbq-terminal-line">{{ line }}</div>
with CSS .pbq-terminal-line { white-space: pre-wrap; }

This is correct -- each output line from result.lines is pushed as a separate string into history, and rendered as plain text via {{ line }} (NOT v-html). Do NOT use v-html anywhere. Multi-line command_outputs values have already been split on '\n' by evaluateCommand(), so each line is a separate history entry.

The getHistory() method is unchanged. No template changes are needed for output rendering.
  </action>
  <verify>Manual (see checkpoint task below for full verification steps)</verify>
  <done>submitCommand() uses evaluateCommand() for all command processing. termModes and termContexts updated via $set after each command. history accumulates both the typed cmd line and output lines. $emit('update') called with full history. No v-html used anywhere in the component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CLI state machine in browser</name>
  <action>Deploy built JS bundle to learning-dev Docker container and verify all 5 CLI scenarios in the browser (see how-to-verify).</action>
  <verify>All 5 scenarios in how-to-verify pass without errors.</verify>
  <done>User confirms all 5 scenarios pass and types approved.</done>
  <what-built>
    Full CLI state machine integration in PbqCli.vue. Mode transitions, error messages, and command_outputs feedback are all wired up. Build and deploy to learning-dev, then test the following scenarios in a CLI PBQ question configured with domain: "cisco_ios".
  </what-built>
  <how-to-verify>
    Deploy:
      ssh learning-dev 'cd ~/learning-nc/app && npm run build'
      ssh learning-dev 'cd ~/learning-nc/app && tar cf /tmp/js-bundle.tar js/'
      ssh learning-dev 'docker cp /tmp/js-bundle.tar learning-app:/tmp/'
      ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && tar xf /tmp/js-bundle.tar"'

    Open http://learning-dev:8080 and open a CLI PBQ question with this config:
      {
        "domain": "cisco_ios",
        "hint": "Configure the loopback interface",
        "terminals": [{ "name": "Router", "initial_prompt": "Router>" }],
        "command_outputs": { "show version": "Cisco IOS Version 15.2" },
        "evaluation": [{ "terminal": "Router", "required_pattern": "ip address", "points": 1 }]
      }

    Test scenario 1 (CLI-02 - mode transitions):
      1. Prompt shows: Router>
      2. Type: conf t  -> prompt must change to: Router(config)#
      3. Type: interface Fa0/0  -> prompt must change to: Router(config-if)#
      4. Type: exit  -> prompt must return to: Router(config)#
      5. Type: end  -> prompt must return to: Router>

    Test scenario 2 (CLI-03 - error messages):
      6. Type: xyzzy  -> terminal shows output line: % Invalid input detected at '^' marker.
      7. Prompt stays at Router> after the error

    Test scenario 3 (CLI-04 - command_outputs):
      8. Type: show version  -> terminal shows output line: Cisco IOS Version 15.2

    Test scenario 4 (CLI-05 - persistence):
      9. Type: conf t, then type: ip address 192.168.1.1 255.255.255.0
      10. The prompt for ip address command is Router(config)# (mode persisted from conf t)

    Test scenario 5 (CLI-01 - backward compat):
      11. Load a question with NO domain field and initial_prompt: "Switch>"
      12. Prompt must show Switch> (not "Switch> " with extra space or "undefined>")
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 scenarios pass, or describe which scenario failed and what was observed.</resume-signal>
</task>

</tasks>

<verification>
Build must complete without webpack errors. All five CLI requirements satisfied:
- CLI-01: domain field selects correct prompt schema (cisco_ios -> Router>, Router(config)#)
- CLI-02: conf t / interface X / exit / end transitions work correctly
- CLI-03: unknown commands show domain error message
- CLI-04: command_outputs keys produce configured output lines
- CLI-05: mode persists across multiple commands within the same question session
- Backward compat: no-domain questions work unchanged
- No v-html in component (NC CSP compliant)
</verification>

<success_criteria>
PbqCli.vue imports and uses cliStateMachine.js. All five mode transition test cases pass in the browser. Error messages appear for unknown commands. command_outputs feedback renders as plain text lines. Mode persists between commands. Existing questions without domain field continue to work. npm run build produces no errors.
</success_criteria>

<output>
After completion, create .planning/phases/01-cli-state-machine/01-02-SUMMARY.md
</output>
