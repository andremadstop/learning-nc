---
phase: 161-audit-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - 161-03
files_modified:
  - docs/audit-fork-runbook.md
autonomous: true
requirements:
  - AUDIT-09

must_haves:
  truths:
    - "docs/audit-fork-runbook.md exists with the full fork-resolution procedure (detect → preserve → classify → resolve → verify)"
    - "The runbook path matches exactly what occ learning:audit:verify prints on failure and via --fork-runbook: docs/audit-fork-runbook.md"
    - "The runbook references the real occ command name (learning:audit:verify) and the checkpoint/anchor mechanics from Phase 161"
  artifacts:
    - path: "docs/audit-fork-runbook.md"
      provides: "Complete fork-resolution admin procedure (5 numbered sections)"
      contains: "learning:audit:verify"
  key_links:
    - from: "AuditVerifyCommand::execute() on failure (built in 161-03)"
      to: "docs/audit-fork-runbook.md"
      via: "output line 'Fork resolution runbook: docs/audit-fork-runbook.md' + --fork-runbook flag"
      pattern: "docs/audit-fork-runbook.md"
---

<objective>
AUDIT-09 — Fork-Resolution-Runbook: create the documented admin process for what to do when
`occ learning:audit:verify` reports a chain fork / break / signature mismatch.

The occ SUPPORT already exists — Plan 161-03 built `AuditVerifyCommand`, which prints
`Fork resolution runbook: docs/audit-fork-runbook.md` on any integrity failure and supports a
`--fork-runbook` flag that prints that same path. This plan's DELIVERABLE is the runbook DOCUMENT
itself: the file that path points to. The repo docs convention is a flat `docs/` directory
(existing: `docs/import-pool-workflow.md`), so `docs/audit-fork-runbook.md` is the correct home.

Purpose: When the verifier fails, an operator needs a tested, step-by-step procedure — detect scope,
preserve evidence (DB snapshot + Forgejo anchor cross-check), classify root cause, resolve by
severity, and re-verify — rather than improvising against tamper-evidence infrastructure.

Output: `docs/audit-fork-runbook.md`, path-consistent with the command from 161-03.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/161-audit-hardening/161-RESEARCH.md
@.planning/phases/161-audit-hardening/161-03-SUMMARY.md

<interfaces>
<!-- From Plan 161-03 (AuditVerifyCommand — the occ support this runbook documents): -->
<!--   - Command name: learning:audit:verify -->
<!--   - On ANY failure the output ends with: "Fork resolution runbook: docs/audit-fork-runbook.md" -->
<!--   - Flag: --fork-runbook  → prints "docs/audit-fork-runbook.md" and returns SUCCESS -->
<!--   - Flag: --json          → machine-readable verify output -->
<!-- From Phase 160/161 mechanics the runbook must reference correctly: -->
<!--   - Tables: nc_learning_audit_events, nc_learning_audit_chain_state, nc_learning_audit_checkpoints -->
<!--   - Checkpoints: Ed25519-signed (sodium), signed_payload stored verbatim, optional Forgejo anchor_url -->
<!--   - DSGVO-01: user_id may be NULL on erased rows while user_ref/chain_hash stay immutable (NOT tamper) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/audit-fork-runbook.md (AUDIT-09)</name>
  <files>docs/audit-fork-runbook.md</files>
  <action>
Create `docs/audit-fork-runbook.md`. The path MUST match exactly what `AuditVerifyCommand` (Plan
161-03) prints — `docs/audit-fork-runbook.md`, repo-relative. Do NOT invent a different location.

Write the following content (5 numbered sections). Keep the occ command name literal
(`learning:audit:verify`) and the table names literal (`nc_` prefix, matching this instance's
Phase-160 tables), so the operator can copy-paste.

```markdown
# Audit Fork Resolution Runbook

**When to use:** `occ learning:audit:verify` exits 1 and reports a chain break, signature mismatch,
or anchor mismatch.
**Who:** System Administrator with Nextcloud shell access.
**Goal:** Identify what changed, preserve evidence, restore chain integrity.

---

## 1. Detect — Understand the Scope

Run the verifier and capture output:
```
occ learning:audit:verify --json > /tmp/audit-verify-$(date +%Y%m%d-%H%M).json
cat /tmp/audit-verify-*.json
```

Note:
- Which `seq_num` values are reported broken
- Whether checkpoint signatures are invalid (key rotation? payload drift?)
- Whether `anchor_url` content differs from `signed_payload` (external tamper evidence)

## 2. Preserve — Do Not Touch the Chain

**Before any action, take a database snapshot** of the three audit tables:
```
# PostgreSQL (devcloud / Relay):
docker exec devcloud-db pg_dump -U nc_user nc_db \
    -t "nc_learning_audit_events" \
    -t "nc_learning_audit_chain_state" \
    -t "nc_learning_audit_checkpoints" \
    > /tmp/audit-snapshot-$(date +%Y%m%d-%H%M).sql

# MariaDB:
mysqldump -u nc_user -p nc_db \
    nc_learning_audit_events nc_learning_audit_chain_state nc_learning_audit_checkpoints \
    > /tmp/audit-snapshot-$(date +%Y%m%d-%H%M).sql
```

**Cross-reference the Forgejo anchor** (if enabled):
- Open the `anchor_url` from `nc_learning_audit_checkpoints` in a browser
- Compare the anchored file content against the `signed_payload` column of the same checkpoint row
- A mismatch means the DB was modified AFTER anchoring — strong, admin-independent tamper evidence
  (the Forgejo commit time is outside the NC admin's control).

## 3. Classify — Determine Root Cause

| Symptom | Likely Cause | Action |
|---------|-------------|--------|
| Single event `chain_hash` wrong, surrounding events OK | Direct DB edit of that row | Preserve snapshot, escalate to DPO |
| All events after seq N wrong | seq N row was edited, breakage propagated | Preserve; reconstruct expected state from the Forgejo anchor |
| Checkpoint signature invalid, chain itself intact | Key rotation without a fresh checkpoint | Re-run `occ learning:audit:verify` after re-checkpointing (Level 1 below) |
| `user_id` NULL but row still verifies | DSGVO-01 Art.17 erasure — **NOT tamper** | No action; `user_ref`/`chain_hash` are immutable by design |
| Anchor content differs from `signed_payload` | DB edit after anchoring | Strongest tamper evidence — escalate immediately |
| verify passes after `occ upgrade` | Migration not applied | Run `occ upgrade`, re-verify |

## 4. Resolve — Options by Severity

### Level 1: Configuration issue (not tamper)
- Missing/rotated signing key → rotate + re-checkpoint:
  `php occ learning:cert:init-issuer --rotate` then let the weekly `AuditCheckpointJob` re-sign
  (or trigger a checkpoint per the operations notes).
- Migration missing → `php occ upgrade`, then `occ learning:audit:verify`.

### Level 2: Single corrupted row (data-integrity issue)
- Restore the affected row from the database snapshot (Section 2) — original bytes only.
- Re-run `occ learning:audit:verify`; if clean, document the incident in the Datenschutz-Log.
- **Do NOT recompute or hand-edit `chain_hash` / `user_ref`** — restore the original stored bytes.

### Level 3: Deliberate tamper (escalation required)
1. Do not modify anything further.
2. Export the snapshot + the Forgejo anchor URLs as evidence.
3. Contact the Datenschutzbeauftragter (DPO) immediately.
4. Preserve server state for forensic analysis (do NOT run `occ upgrade` or maintenance).

## 5. Verify Fix

After any remediation:
```
occ learning:audit:verify
# Must exit 0 before marking the incident resolved.
```

## Reference

- Chain design & 6-field canonical: Phase 160 migration notes + Phase 161 RESEARCH.md §Pattern 2
- DSGVO-01 erasure compatibility (`user_id` NULL is not tamper): Phase 160 RESEARCH.md §DSGVO-01
- Checkpoint signing (Ed25519 / `signed_payload` verbatim): Phase 161 RESEARCH.md §Pattern 1
- Forgejo anchor mechanics: Phase 161 RESEARCH.md §AUDIT-05 / Pitfall 7
- Current audit config: `occ config:list learning | grep -E "audit|forgejo|checkpoint"`
```

After creating the file, confirm the command from Plan 161-03 prints this exact path:
```bash
grep -rn "docs/audit-fork-runbook" /home/andre/Workspace/Code/learning-nc/app/lib/Command/AuditVerifyCommand.php
```
If 161-03 already emits `docs/audit-fork-runbook.md` (it does per its plan), no code change is
needed — this is a doc-only deliverable. If the grep shows a different path, treat it as a defect in
161-03 and reconcile to `docs/audit-fork-runbook.md` (the path both plans agreed on).
  </action>
  <verify>
    <automated>test -f /home/andre/Workspace/Code/learning-nc/docs/audit-fork-runbook.md && grep -q "learning:audit:verify" /home/andre/Workspace/Code/learning-nc/docs/audit-fork-runbook.md && grep -c "docs/audit-fork-runbook" /home/andre/Workspace/Code/learning-nc/app/lib/Command/AuditVerifyCommand.php</automated>
  </verify>
  <done>docs/audit-fork-runbook.md exists with all 5 numbered sections (Detect, Preserve, Classify, Resolve, Verify); references occ learning:audit:verify + checkpoint/anchor mechanics; grep confirms AuditVerifyCommand.php emits the exact same path</done>
</task>

</tasks>

<verification>
1. `ls docs/audit-fork-runbook.md` — file exists
2. `grep -c "^## " docs/audit-fork-runbook.md` — 5+ sections present (Detect/Preserve/Classify/Resolve/Verify + Reference)
3. `ssh relais 'docker exec -u www-data devcloud-app php occ learning:audit:verify --fork-runbook'` — prints `docs/audit-fork-runbook.md`, exits 0
4. `grep -rn "docs/audit-fork-runbook" app/lib/Command/AuditVerifyCommand.php` — command path matches the doc path
</verification>

<success_criteria>
- docs/audit-fork-runbook.md committed with the complete 5-step procedure
- Path is byte-identical to what occ learning:audit:verify emits (docs/audit-fork-runbook.md)
- Runbook cites the real command name + checkpoint/anchor/DSGVO-erasure mechanics (no invented APIs)
- Doc-only: no PHP/JS change required unless 161-03's emitted path drifts (then reconcile)
</success_criteria>

<output>
After completion, create `.planning/phases/161-audit-hardening/161-06-SUMMARY.md`
</output>
