---
phase: 05-pbq-author-tool
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/src/components/PbqAuthorTool.vue
  - app/src/components/QuestionForm.vue
autonomous: false
requirements:
  - AUTHOR-03
  - AUTHOR-02

must_haves:
  truths:
    - "Live preview renders the correct PBQ simulation for every subtype as the instructor fills in the form"
    - "Instructor can open PbqAuthorTool from a button in QuestionForm"
    - "Clicking 'JSON kopieren' puts the generated JSON on the clipboard"
    - "QuestionForm has a pbq_subtype select and pbq_config textarea that pre-populate from existing question data"
    - "QuestionForm save() sends pbq_subtype and pbq_config in its payload"
  artifacts:
    - path: "app/src/components/PbqAuthorTool.vue"
      provides: "Extended with PbqRenderer live preview section"
      contains: "PbqRenderer"
    - path: "app/src/components/QuestionForm.vue"
      provides: "PBQ config section with subtype select, config textarea, open-author-tool button"
      contains: "pbqSubtype"
  key_links:
    - from: "app/src/components/PbqAuthorTool.vue"
      to: "app/src/components/PbqRenderer.vue"
      via: ":question='previewQuestion' @submit='()=>{}' @skip='()=>{}'"
      pattern: "PbqRenderer"
    - from: "app/src/components/QuestionForm.vue"
      to: "app/src/components/PbqAuthorTool.vue"
      via: "NcDialog v-if='showAuthorTool' containing PbqAuthorTool"
      pattern: "showAuthorTool"
---

<objective>
Wire the live-preview into PbqAuthorTool and integrate the tool into QuestionForm.

Purpose: AUTHOR-03 (live preview) and the integration path (AUTHOR-02 "JSON kann in QuestionForm eingefügt werden") require connecting PbqRenderer to the author tool and exposing PBQ config fields in QuestionForm.

Output:
- PbqAuthorTool.vue — gains PbqRenderer live preview section + previewQuestion computed
- QuestionForm.vue — gains pbq_subtype select, pbq_config textarea, "PBQ Config Builder" button opening PbqAuthorTool in NcDialog, save() extended with PBQ fields
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/andre/Workspace/Code/learning-nc/.planning/PROJECT.md
@/home/andre/Workspace/Code/learning-nc/.planning/ROADMAP.md
@/home/andre/Workspace/Code/learning-nc/.planning/phases/05-pbq-author-tool/05-RESEARCH.md
@/home/andre/Workspace/Code/learning-nc/.planning/phases/05-pbq-author-tool/05-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

From app/src/components/PbqRenderer.vue (props for live preview):
```javascript
props: {
  question:     { type: Object, required: true },  // { pbq_subtype: string, pbq_config: object }
  initialValue: { type: Object, default: null },
  disabled:     { type: Boolean, default: false },
}
// Emits: 'submit' (localAnswer), 'skip'
// Wire: @submit="() => {}" @skip="() => {}" to suppress Vue warnings
```

From app/src/components/QuestionForm.vue (save() emit, mounted() init pattern):
```javascript
// save() emits:
this.$emit('save', {
  text, explanation, difficulty, questionType, answers, imageFile, removeImage
  // Plan 02 adds: pbqSubtype, pbqConfig (string | null)
})

// mounted() init pattern (add to existing block):
this.form.pbqSubtype = this.question.pbq_subtype || ''
this.form.pbqConfig  = this.question.pbq_config
  ? JSON.stringify(this.question.pbq_config, null, 2)
  : ''
```

PbqAuthorTool.vue (from Plan 01 — will exist by the time Plan 02 executes):
```javascript
// computed already present:
generatedJson()  // → JSON string
selectedSubtype  // data property

// Plan 02 adds:
computed: {
  previewQuestion() {
    return { pbq_subtype: this.selectedSubtype, pbq_config: this.generatedConfig }
  }
}
// and imports + renders PbqRenderer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live preview to PbqAuthorTool.vue</name>
  <files>app/src/components/PbqAuthorTool.vue</files>
  <action>
    Extend the existing PbqAuthorTool.vue (built in Plan 01). Three changes:

    1. ADD import at the top of the script:
       ```javascript
       import PbqRenderer from './PbqRenderer.vue'
       ```
       Add PbqRenderer to components: { NcButton, PbqRenderer }

    2. ADD computed property previewQuestion() (after generatedJson):
       ```javascript
       previewQuestion() {
         return {
           pbq_subtype: this.selectedSubtype,
           pbq_config: this.generatedConfig,
         }
       },
       ```

    3. ADD preview section to the template, BETWEEN the form sections and the JSON output section:
       ```html
       <!-- Live Preview -->
       <div v-if="selectedSubtype" class="author-preview-section">
         <h4 class="author-preview-title">Vorschau</h4>
         <div class="author-preview-wrapper">
           <PbqRenderer
             :question="previewQuestion"
             :disabled="false"
             @submit="() => {}"
             @skip="() => {}"
           />
         </div>
       </div>
       ```

    4. ADD scoped CSS:
       ```css
       .author-preview-section { margin-top: 24px; border-top: 1px solid var(--color-border); padding-top: 16px; }
       .author-preview-title { font-size: 14px; font-weight: 600; margin-bottom: 12px; color: var(--color-main-text); }
       .author-preview-wrapper { border: 1px solid var(--color-border); border-radius: var(--border-radius-large); padding: 16px; overflow-y: auto; max-height: 450px; }
       ```

    CSP constraint: no v-html. PbqRenderer handles all rendering internally via its own Vue template — no raw HTML is injected here.

    Vue 2 pitfall avoided: PbqRenderer's @submit and @skip are wired to empty arrow functions to prevent "Unhandled event" console warnings when the instructor clicks "PBQ abschicken" in the preview.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>PbqAuthorTool.vue imports PbqRenderer and contains the previewQuestion computed. npm test still passes. No v-html in file.</done>
</task>

<task type="auto">
  <name>Task 2: Extend QuestionForm with PBQ config section + author tool integration</name>
  <files>app/src/components/QuestionForm.vue</files>
  <action>
    Extend the existing QuestionForm.vue with PBQ config support. Minimal changes only — preserve all existing form logic untouched.

    SCRIPT changes:

    1. Add import at top:
       ```javascript
       import PbqAuthorTool from './PbqAuthorTool.vue'
       ```
       Add to components: { ..., PbqAuthorTool }

    2. Add to data() → form object:
       ```javascript
       pbqSubtype: '',
       pbqConfig: '',
       ```

    3. Add to data() root (outside form):
       ```javascript
       showAuthorTool: false,
       ```

    4. In mounted(), inside the `if (this.question)` block, after existing field assignments:
       ```javascript
       this.form.pbqSubtype = this.question.pbq_subtype || ''
       this.form.pbqConfig  = this.question.pbq_config
         ? JSON.stringify(this.question.pbq_config, null, 2)
         : ''
       ```

    5. In save(), add pbqSubtype and pbqConfig to the emitted payload:
       ```javascript
       pbqSubtype: this.form.pbqSubtype || null,
       pbqConfig:  this.form.pbqConfig ? this.form.pbqConfig.trim() : null,
       ```

    TEMPLATE changes (add as last section inside the <form>, before .dialog-actions):

    ```html
    <!-- PBQ Config Section -->
    <div class="form-group">
      <label for="pbq-subtype">{{ t('learning', 'PBQ Type (optional)') }}</label>
      <select id="pbq-subtype" v-model="form.pbqSubtype" class="nc-input">
        <option value="">{{ t('learning', 'None (standard question)') }}</option>
        <option value="cli">CLI Terminal</option>
        <option value="placement">Device Placement</option>
        <option value="dropdown">Inline Dropdown</option>
        <option value="cable">Cable Mapping</option>
        <option value="multi_panel">Multi-Panel</option>
      </select>
    </div>

    <div v-if="form.pbqSubtype" class="form-group">
      <label for="pbq-config">{{ t('learning', 'PBQ Config (JSON)') }}</label>
      <div class="pbq-config-actions">
        <NcButton type="secondary" @click="showAuthorTool = true">
          {{ t('learning', 'PBQ Config Builder') }}
        </NcButton>
      </div>
      <textarea
        id="pbq-config"
        v-model="form.pbqConfig"
        rows="6"
        :placeholder="t('learning', 'Paste JSON from PBQ Config Builder or enter manually...')"
        class="nc-input pbq-config-textarea"
      ></textarea>
    </div>

    <NcDialog
      v-if="showAuthorTool"
      :name="t('learning', 'PBQ Config Builder')"
      size="large"
      @closing="showAuthorTool = false"
    >
      <PbqAuthorTool />
    </NcDialog>
    ```

    CSS additions (scoped):
    ```css
    .pbq-config-actions { margin-bottom: 8px; }
    .pbq-config-textarea { font-family: monospace; font-size: 12px; }
    ```

    Note: The instructor workflow is: select PBQ type → click "PBQ Config Builder" → build config visually → click "JSON kopieren" → close dialog → paste into the pbq-config textarea. The clipboard content matches generatedJson exactly. No direct component binding is needed — clipboard is the integration bridge.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>QuestionForm.vue imports PbqAuthorTool. form.pbqSubtype and form.pbqConfig present in data(). save() emits pbqSubtype and pbqConfig. npm test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser verify — live preview + QuestionForm PBQ integration</name>
  <what-built>
    - PbqAuthorTool.vue: full visual editor with 5 subtype form sections, generatedConfig computed, generatedJson output display, live PbqRenderer preview, Copy JSON button
    - QuestionForm.vue: PBQ type selector, config textarea, "PBQ Config Builder" button opening PbqAuthorTool in NcDialog
  </what-built>
  <how-to-verify>
    Deploy to learning-dev first:
    ```
    rsync -avz --exclude node_modules --exclude .git /home/andre/Workspace/Code/learning-nc/app/ learning-dev:~/learning-nc/app/
    ssh learning-dev 'cd ~/learning-nc/app && npm run build'
    ssh learning-dev 'cd ~/learning-nc/app && tar cf /tmp/js-bundle.tar js/'
    ssh learning-dev 'docker cp /tmp/js-bundle.tar learning-app:/tmp/'
    ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && tar xf /tmp/js-bundle.tar"'
    ```

    Then open http://learning-dev:8080 in browser and verify:

    1. Open any pool → click "+" to create a new question → QuestionForm opens
    2. Scroll to bottom → "PBQ Type" select visible → select "CLI Terminal"
    3. "PBQ Config JSON" textarea appears + "PBQ Config Builder" button appears
    4. Click "PBQ Config Builder" → NcDialog opens showing PbqAuthorTool
    5. In PbqAuthorTool: subtype selector shows "CLI Terminal" by default
    6. Fill in "Terminals" section: add a terminal named "Router"
    7. Add a command output: cmd="show ip route", output="Routing table..."
    8. Live preview section below the form shows an interactive CLI terminal
    9. Type "show ip route" in the preview terminal — output text appears
    10. Click "JSON kopieren" → "Kopiert!" feedback shows briefly
    11. Close dialog → paste into pbq-config textarea → JSON appears correctly
    12. Switch subtype to "Device Placement" → placement form renders, topology node editor visible
    13. Switch to "Multi-Panel" → CLI + topology editors both visible
    14. Switch to "Cable Mapping" → raw JSON textarea visible (v1 passthrough)
    15. No console errors, no v-html warnings
  </how-to-verify>
  <action>Deploy to learning-dev (commands in how-to-verify), then open http://learning-dev:8080 and run all 15 verification steps.</action>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `cd /home/andre/Workspace/Code/learning-nc/app && npm test` — full suite green
- `grep -n "v-html" /home/andre/Workspace/Code/learning-nc/app/src/components/PbqAuthorTool.vue` — no matches
- `grep -n "v-html" /home/andre/Workspace/Code/learning-nc/app/src/components/QuestionForm.vue` — no matches
- `grep -n "PbqRenderer" /home/andre/Workspace/Code/learning-nc/app/src/components/PbqAuthorTool.vue` — import + component + template usage all present
- `grep -n "pbqSubtype\|pbqConfig" /home/andre/Workspace/Code/learning-nc/app/src/components/QuestionForm.vue` — data fields + save() emission present
</verification>

<success_criteria>
- Live preview renders PbqRenderer for each subtype as form fields change (AUTHOR-03)
- QuestionForm can open PbqAuthorTool via NcDialog (AUTHOR-01, AUTHOR-02)
- Clipboard copy tested and confirmed via browser checkpoint
- save() in QuestionForm sends pbqSubtype + pbqConfig (AUTHOR-02 integration path)
- Full test suite green
- Browser checkpoint approved by user
</success_criteria>

<output>
After completion, create /home/andre/Workspace/Code/learning-nc/.planning/phases/05-pbq-author-tool/05-02-SUMMARY.md
</output>
