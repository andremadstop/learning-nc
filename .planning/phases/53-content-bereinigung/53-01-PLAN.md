---
phase: 53-content-bereinigung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/kurs-materialien/Wireshark-Anleitung.md
  - app/data/kurs-materialien/Nmap-Anleitung.md
  - app/data/kurs-materialien/Network-Plus-Wissensbasis.md
  - app/data/kurs-materialien/00-Lehrplan.md
  - app/data/kurs-materialien/Netzwerkaufbau-bei-Grossevents.md
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "Wireshark-Anleitung contains no 192.168.178.x IPs, no SSH aliases (cockpit, proxmox, learning-dev, adguard, homeassistant, n8n), no personal hostnames"
    - "Nmap-Anleitung contains no 192.168.178.x IPs, no SSH aliases, no personal host references"
    - "Network+ Wissensbasis contains no Homelab section with personal IPs, no personal device mapping"
    - "00-Lehrplan contains no personal tool paths, no SSH aliases, no personal IPs in exercises"
    - "Grossevents guide is copied with wikilinks converted to plain text"
    - "All five cleaned copies exist in app/data/kurs-materialien/"
    - "Original files in Personal Vault are unchanged"
  artifacts:
    - path: "app/data/kurs-materialien/Wireshark-Anleitung.md"
      provides: "Cleaned Wireshark guide with generic examples"
    - path: "app/data/kurs-materialien/Nmap-Anleitung.md"
      provides: "Cleaned Nmap guide with generic examples"
    - path: "app/data/kurs-materialien/Network-Plus-Wissensbasis.md"
      provides: "Cleaned Network+ knowledge base"
    - path: "app/data/kurs-materialien/00-Lehrplan.md"
      provides: "Cleaned curriculum with generic lab examples"
    - path: "app/data/kurs-materialien/Netzwerkaufbau-bei-Grossevents.md"
      provides: "Cleaned event networking guide"
  key_links:
    - from: "app/data/kurs-materialien/*.md"
      to: "Personal Vault originals"
      via: "content comparison"
      pattern: "educational content preserved, personal data removed"
---

<objective>
Clean all five Network+ guides from personal Homelab references and produce shareable copies in the learning-nc repo.

Purpose: The guides contain private IPs (192.168.178.x), SSH aliases (cockpit, proxmox, learning-dev, adguard, homeassistant, n8n), personal hostnames, and Obsidian wikilinks that must be replaced with generic examples before distributing to students.

Output: Five cleaned markdown files in `app/data/kurs-materialien/`, ready for Phase 54 distribution via NC Shared Folder.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source files to clean (READ ONLY — do NOT modify originals):
- /home/andre/ObsidianVaults/Personal/Wissen/Comptia Netzwerk+/Wireshark-Anleitung.md
- /home/andre/ObsidianVaults/Personal/Wissen/Comptia Netzwerk+/Nmap-Anleitung.md
- /home/andre/ObsidianVaults/Personal/Wissen/Comptia Netzwerk+/Network-Plus-Wissensbasis.md
- /home/andre/ObsidianVaults/Personal/Wissen/Comptia Netzwerk+/00-Lehrplan.md
- /home/andre/ObsidianVaults/Personal/Wissen/Comptia Netzwerk+/Netzerkaufbau bei Großevents.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean Wireshark, Nmap, and Lehrplan guides</name>
  <files>
    app/data/kurs-materialien/Wireshark-Anleitung.md
    app/data/kurs-materialien/Nmap-Anleitung.md
    app/data/kurs-materialien/00-Lehrplan.md
  </files>
  <action>
Create directory `app/data/kurs-materialien/`. For each of the three files, read the original from the Personal Vault, apply ALL replacements below, and write the cleaned copy to the staging directory.

**Replacement rules (apply consistently across all files):**

1. **Private IPs — replace the ENTIRE 192.168.178.x subnet:**
   - `192.168.178.0/24` -> `10.0.0.0/24`
   - `192.168.178.1` (Fritz!Box/Router) -> `10.0.0.1`
   - `192.168.178.9` (CachyOS/Workstation) -> `10.0.0.10`
   - `192.168.178.10` (Proxmox) -> `10.0.0.20`
   - `192.168.178.30` (AdGuard) -> `10.0.0.30`
   - `192.168.178.36` (Home Assistant) -> `10.0.0.40`
   - `192.168.178.42` -> `10.0.0.42`
   - `192.168.178.50` -> `10.0.0.50`
   - `192.168.178.59` (Cockpit) -> `10.0.0.50` (use different if conflict, e.g. `10.0.0.60`)
   - `192.168.178.65` (learning-dev) -> `10.0.0.100`
   - `192.168.178.68` (n8n) -> `10.0.0.70`
   - Any remaining `192.168.178.x` -> `10.0.0.x` (preserve last octet)

2. **SSH aliases and personal hostnames:**
   - `cockpit` (as hostname) -> `mgmt-server`
   - `proxmox` (as hostname) -> `hypervisor-01`
   - `learning-dev` (as hostname) -> `app-server`
   - `adguard` / `AdGuard` (as hostname) -> `dns-server`
   - `homeassistant` / `Home Assistant` (as service on specific IP) -> `iot-hub`
   - `n8n` (as hostname) -> `automation-server`
   - `vaultwarden` -> `password-manager`

3. **SSH commands:**
   - `ssh root@proxmox` -> `ssh root@hypervisor-01`
   - `ssh root@adguard` -> `ssh root@dns-server`
   - `ssh learning-dev` -> `ssh app-server`

4. **Personal device references:**
   - `CachyOS Workstation` / `CachyOS` (as hostname context) -> `Linux Workstation` / `workstation`
   - `Fritz!Box` -> `home router` or `lab-router`
   - `NixOS` (as Cockpit OS) -> remove or generalize
   - `Cockpit (.59)` / `Cockpit` (as server name) -> `mgmt-server`
   - `enp6s0` -> `eth0` (generic interface name)
   - References to "dein Homelab" -> "dein Lab" or "das Lab-Netzwerk"
   - `~/ObsidianVaults/Personal/...` -> remove or replace with generic path

5. **Obsidian wikilinks:**
   - `[[00-Lehrplan]]` -> `siehe 00-Lehrplan.md`
   - `[[Wireshark-Anleitung]]` -> `siehe Wireshark-Anleitung.md`
   - `[[Nmap-Anleitung]]` -> `siehe Nmap-Anleitung.md`
   - `[[Wissen-Index]]` -> remove (not part of shared materials)
   - `[[Netzerkaufbau bei Großevents]]` -> `siehe Netzwerkaufbau-bei-Grossevents.md`
   - Any other `[[...]]` wikilinks -> convert to plain text or remove if linking to personal vault content

6. **Context-specific intro lines:**
   - Remove or rewrite lines like "Diese Anleitung bezieht sich auf das konkrete Homelab-Setup (CachyOS + Proxmox)" -> "Diese Anleitung zeigt typische Anwendungsfaelle in einem Lab-Netzwerk."
   - Remove or rewrite "Homelab-Setup (CachyOS, Subnetz 192.168.178.0/24)" -> "Lab-Setup (Linux, Subnetz 10.0.0.0/24)"

7. **Section headings referencing Homelab:**
   - "Nützliche Homelab-Filter" -> "Nützliche Lab-Filter"
   - "Lernübungen mit deinem Homelab" -> "Lernübungen im Lab"
   - "Homelab-Rezepte" -> "Lab-Rezepte"
   - "Dein Homelab als Lernumgebung" -> "Lab als Lernumgebung"
   - "Homelab-Werkzeuge" -> "Lab-Werkzeuge"
   - "Homelab-Vernetzung" -> remove or generalize

8. **Lehrplan-specific (00-Lehrplan.md):**
   - The "Homelab-Werkzeuge" table references specific paths (`CachyOS Workstation`, `learning-dev:8080`, `~/ObsidianVaults/...`). Replace with generic equivalents.
   - "Remote Capture" SSH command: generalize the hostname
   - All exercise blocks throughout all 12 blocks: apply IP/hostname replacements
   - "Wireshark Cheat Sheet (dein Homelab)" -> "Wireshark Cheat Sheet (Lab)"
   - Verlinkung section at bottom: convert wikilinks, remove personal vault links

**CRITICAL: Preserve ALL educational content.** The structure, explanations, ASCII diagrams, tables, code block formatting, and learning objectives must remain identical. Only personal identifiers change.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc && grep -rn "192\.168\.178" app/data/kurs-materialien/Wireshark-Anleitung.md app/data/kurs-materialien/Nmap-Anleitung.md app/data/kurs-materialien/00-Lehrplan.md && echo "FAIL: personal IPs found" || echo "PASS: no personal IPs" ; grep -rn -i "cockpit\|proxmox\|learning-dev\|adguard\|homeassistant\|vaultwarden\|CachyOS\|Fritz!Box" app/data/kurs-materialien/Wireshark-Anleitung.md app/data/kurs-materialien/Nmap-Anleitung.md app/data/kurs-materialien/00-Lehrplan.md && echo "FAIL: personal hostnames found" || echo "PASS: no personal hostnames" ; grep -rn "\[\[" app/data/kurs-materialien/Wireshark-Anleitung.md app/data/kurs-materialien/Nmap-Anleitung.md app/data/kurs-materialien/00-Lehrplan.md && echo "FAIL: wikilinks found" || echo "PASS: no wikilinks"</automated>
  </verify>
  <done>Three cleaned files exist in app/data/kurs-materialien/ with zero matches for personal IPs, SSH aliases, or wikilinks. Educational content structure is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Clean Network+ Wissensbasis and Grossevents, verify all five files</name>
  <files>
    app/data/kurs-materialien/Network-Plus-Wissensbasis.md
    app/data/kurs-materialien/Netzwerkaufbau-bei-Grossevents.md
  </files>
  <action>
**Network-Plus-Wissensbasis.md:** Read original, apply the SAME replacement rules from Task 1. This file has specific personal sections to handle:

1. The "Dein Homelab:" block (lines ~158-167) with the full device map — replace with a generic "Lab-Netzwerk:" block using 10.0.0.x IPs and generic hostnames (same mapping as Task 1).
2. "Dein Homelab als Lernumgebung" ASCII diagram section (lines ~796-820) — replace all personal references with generic lab equivalents. Remove references to specific services like "Ops-Zentrale, SSHFS" or "Workflow-Automation".
3. Scattered IP references in examples throughout the file (troubleshooting commands table, etc.).
4. Wikilinks at the bottom (Wireshark-Anleitung, Nmap-Anleitung, personal vault links to Proxmox/AdGuard/learning-dev ops notes) — convert shared guide links to plain text, REMOVE personal vault links entirely (the `../../Ops/services/...` and `../../Ops/glossar/...` links).
5. Section "Homelab-Vernetzung" — remove entirely or replace with generic "Lab-Vernetzung" without linking to personal ops notes.

**Netzwerkaufbau-bei-Grossevents.md:** This file has NO personal references (confirmed by grep). Read original, write copy with:
1. Filename normalization: save as `Netzwerkaufbau-bei-Grossevents.md` (fix typo "Netzerkaufbau" -> "Netzwerkaufbau")
2. If any wikilinks exist, convert to plain text. (None expected based on analysis.)
3. Content passes through unchanged.

**Final verification across all 5 files:**
Run comprehensive grep for ALL personal patterns across all 5 cleaned files to catch anything missed.

**Verify originals unchanged:**
Run a quick check that the original files in the Personal Vault still contain personal references (proving they were not modified).
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc && echo "=== Checking all 5 files ===" && grep -rn "192\.168\.178\|cockpit\|proxmox\|learning-dev\|adguard\|homeassistant\|vaultwarden\|n8n\|CachyOS\|Fritz!Box\|Homelab\|\[\[" app/data/kurs-materialien/ && echo "FAIL: personal references or wikilinks found" || echo "PASS: all 5 files clean" ; echo "=== Verify originals unchanged ===" && grep -c "192\.168\.178" "/home/andre/ObsidianVaults/Personal/Wissen/Comptia Netzwerk+/Wireshark-Anleitung.md" | xargs -I{} echo "Original Wireshark has {} personal IP matches (expected >0)" ; ls -la app/data/kurs-materialien/*.md | wc -l | xargs -I{} echo "Total cleaned files: {} (expected 5)"</automated>
  </verify>
  <done>All five cleaned files exist in app/data/kurs-materialien/. Zero personal references across all files. Originals in Personal Vault are unmodified. File count is exactly 5.</done>
</task>

</tasks>

<verification>
1. `grep -rn "192\.168\.178" app/data/kurs-materialien/` returns no matches
2. `grep -rn -i "cockpit\|proxmox\|learning-dev\|adguard\|homeassistant\|vaultwarden\|CachyOS\|Fritz!Box" app/data/kurs-materialien/` returns no matches
3. `grep -rn "\[\[" app/data/kurs-materialien/` returns no matches
4. `ls app/data/kurs-materialien/*.md | wc -l` returns 5
5. Original files in Personal Vault still contain personal references (unchanged)
6. Educational content (headings, tables, code blocks, ASCII diagrams) is structurally intact
</verification>

<success_criteria>
- Five cleaned markdown files exist in app/data/kurs-materialien/
- Zero matches for personal IPs (192.168.178.x), SSH aliases, personal hostnames, or Obsidian wikilinks
- All educational content preserved (structure, explanations, examples work with generic IPs)
- Originals in ~/ObsidianVaults/Personal/Wissen/Comptia Netzwerk+/ are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/53-content-bereinigung/53-01-SUMMARY.md`
</output>
