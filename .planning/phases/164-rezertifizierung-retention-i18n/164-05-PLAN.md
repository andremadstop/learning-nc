---
phase: 164-rezertifizierung-retention-i18n
plan: 05
type: tdd
wave: 4
depends_on: ["03", "04"]
files_modified:
  - app/lib/BackgroundJob/RecertPeriodCloseJob.php
  - app/lib/Service/AssignmentService.php
autonomous: true
requirements: [RECERT-04]
must_haves:
  truths:
    - "RecertPeriodCloseJob (daily) closes periods whose cert is expired past the grace window and opens a fresh assignment row"
    - "Running the job twice creates EXACTLY ONE new assignment row per period (testDoubleRunSingleRow GREEN)"
    - "Period-close does NOT set revoked/revoked_at → old verify URL reads 'expired', not 'withdrawn' (SC2)"
  artifacts:
    - path: "app/lib/BackgroundJob/RecertPeriodCloseJob.php"
      provides: "daily period-close orchestration calling AssignmentService::closePeriod"
      contains: "closeExpiredPeriods"
  key_links:
    - from: "RecertPeriodCloseJob::run"
      to: "AssignmentService::closePeriod (built in 164-04)"
      via: "iterate expired periods past grace"
      pattern: "closePeriod"
---

<objective>
Implement the daily RecertPeriodCloseJob body: find periods whose cert has expired beyond the grace window, close them via AssignmentService::closePeriod (built in 164-04), and rely on the UNIQUE active_period_key constraint for idempotency. Flips testDoubleRunSingleRow GREEN.

Purpose: Reconciliation note (locked decision #2) — RECERT-04's literal "revoked_at setzen" is deliberately NOT implemented; SC2 is authoritative (old URL must read "expired"). Verify keys on getRevoked() bool, so leaving revoked=false + relying on expires_at<now yields "expired". The close is audited via logComplianceEvent, not the cert revoked_at column.
Output: RecertPeriodCloseJob orchestration + AssignmentService query for expired periods.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-RESEARCH.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-VALIDATION.md

<interfaces>
Built in prior plans:
- AssignmentService::closePeriod(string $subjectType, string $subjectId, int $courseId): void  (164-04) — NULLs keys, inserts fresh row, no revoked.
- RecertPeriodCloseJob skeleton + boot() registration (164-03).
- Config: ConfigDefaults::RECERT_GRACE_DAYS_DEFAULT ('14'), read via IConfig.
Idempotency lever: UNIQUE active_period_key. A 2nd job run either sees period not-yet-expired (skip) or hits UNIQUE on re-INSERT → exactly one new row (SC3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: closeExpiredPeriods query + job orchestration (RECERT-04)</name>
  <files>app/lib/Service/AssignmentService.php, app/lib/BackgroundJob/RecertPeriodCloseJob.php</files>
  <action>
    1. AssignmentService: add `closeExpiredPeriods(int $now): int` — select active assignment periods (active_period_key IS NOT NULL) whose linked cert expires_at < now - graceDays*86400 (grace from IConfig, DST-irrelevant here — plain second window against expires_at per research), and for each call closePeriod(...). Return count closed. Read grace via IConfig->getAppValue('learning','recert_grace_days', ConfigDefaults::RECERT_GRACE_DAYS_DEFAULT).
    2. RecertPeriodCloseJob::run — replace the no-op stub with `$this->assignmentService->closeExpiredPeriods($this->time->getTime());` inside the existing try/catch+log. (Job already registered in boot() by 164-03 — do NOT touch Application.php.)
    Flips GREEN: testDoubleRunSingleRow. Do NOT run PHP/deploy.
  </action>
  <verify>
    <automated>grep -q "closeExpiredPeriods" app/lib/Service/AssignmentService.php && grep -q "closeExpiredPeriods" app/lib/BackgroundJob/RecertPeriodCloseJob.php && echo OK  # orchestrator: phpunit --filter testDoubleRunSingleRow PASS</automated>
  </verify>
  <done>Job closes expired-past-grace periods idempotently; exactly one new assignment row per period across re-runs; revoked untouched (SC2). testDoubleRunSingleRow green at wave merge.</done>
</task>

</tasks>

<verification>
- Job body wired; no Application.php edit.
- Orchestrator wave merge: PHPStan L5 clean + phpunit testDoubleRunSingleRow GREEN.
- Reconciliation note present (RECERT-04 revoked_at vs SC2).
</verification>

<success_criteria>
Expired periods close daily, idempotently, and the old verify URL reads "expired" (never "withdrawn").
</success_criteria>

<output>
After completion, create `.planning/phases/164-rezertifizierung-retention-i18n/164-05-SUMMARY.md`.
</output>
