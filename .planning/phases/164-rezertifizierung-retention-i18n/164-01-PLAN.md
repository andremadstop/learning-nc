---
phase: 164-rezertifizierung-retention-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/Migration/Version009600Date20260702000000.php
  - app/appinfo/info.xml
  - app/lib/AppInfo/ConfigDefaults.php
  - app/tests/Support/PhpUnitStubs.php
autonomous: true
requirements: [RECERT-02, RECERT-03, RECERT-06, DSGVO-03]
must_haves:
  truths:
    - "Migration Version009600 adds cert_validity_months to learning_courses, anonymized_at to learning_certificates, and creates learning_recert_reminders with UNIQUE(cert_id, threshold_days)"
    - "Retention window and grace period are config-driven (IConfig app values) with conservative defaults"
    - "PhpUnitStubs cover the new OCP surface so downstream unit tests compile"
  artifacts:
    - path: "app/lib/Migration/Version009600Date20260702000000.php"
      provides: "cert_validity_months + anonymized_at columns + learning_recert_reminders table"
      contains: "learning_recert_reminders"
    - path: "app/lib/AppInfo/ConfigDefaults.php"
      provides: "retention_years + recert_grace_days default constants"
  key_links:
    - from: "learning_recert_reminders"
      to: "reminder idempotency (RECERT-06)"
      via: "UNIQUE(cert_id, threshold_days)"
      pattern: "learn_rcrt_rem_uq"
---

<objective>
Lay the schema + config foundation for the cert-lifecycle loop. Add the per-course validity column, the retention tombstone column, the reminder-idempotency backing table, and the two configurable policy knobs (retention window, grace period). Extend the PHPUnit stub surface so the Wave 2 RED tests can compile.

Purpose: Every downstream wave (guard redesign, period-close, reminders, retention, i18n) depends on this schema + config existing. This is the only plan that touches the migration and info.xml version.
Output: Version009600 migration, info.xml bump to 5.2.0.3, ConfigDefaults constants, extended PhpUnitStubs.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-RESEARCH.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-VALIDATION.md

<interfaces>
Migration house rules (from research §Migration + prior 009x00 migrations):
- `from('learning_courses')` etc. are UNPREFIXED — NC prepends `oc_`.
- All columns via `OCP\DB\Types::*` (BIGINT, INTEGER).
- Index/constraint names ≤ 27 chars (MariaDB InnoDB cap).
- Guard every add with `$schema->hasTable(...)` / `$table->hasColumn(...)`.
- Migrations extend `OCP\Migration\SimpleMigrationStep`; signature:
  `changeSchema(IOutput $output, Closure $schemaClosure, array $options): ?ISchemaWrapper`
- Reference the newest existing migration in app/lib/Migration/ (Version009500*) for the exact class shape.

Config keys (IConfig app values, key = 'learning'):
- 'retention_years'   default '3'   (RECERT retention — locked decision #3; research suggested 5; 3 chosen conservatively, FLAGGED for AWO/DSGVO confirmation)
- 'recert_grace_days' default '14'  (RECERT-03 grace before "overdue")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration Version009600 (validity column + tombstone column + reminder table)</name>
  <files>app/lib/Migration/Version009600Date20260702000000.php, app/appinfo/info.xml</files>
  <action>
    Create migration Version009600Date20260702000000 (mirror the class shape of the newest existing app/lib/Migration/Version009500* file — same namespace OCA\Learning\Migration, same SimpleMigrationStep base, same hasTable/hasColumn guards).

    Schema changes (all guarded):
    1. On table `learning_courses` (UNPREFIXED): add column `cert_validity_months` — Types::BIGINT, notnull=false (nullable; NULL = inherit/no-expiry, default 12 applied in CODE not schema).
    2. On table `learning_certificates` (UNPREFIXED): add column `anonymized_at` — Types::BIGINT, notnull=false (nullable; NULL = live, set = retention tombstone). Consumed by CertificateVerifyService (164-06 anonymized branch) + RetentionService (164-07 DSGVO-03 crypto-erasure).
    3. Create table `learning_recert_reminders` if not exists:
       - `id`             BIGINT, autoincrement, notnull, primary key
       - `cert_id`        BIGINT, notnull   (FK-by-convention → learning_certificates.id)
       - `threshold_days` BIGINT, notnull   (30 | 7)
       - `sent_at`        BIGINT, notnull   (unix seconds)
       - UNIQUE index on (cert_id, threshold_days) named `learn_rcrt_rem_uq`  (24 chars ✓)
       - plain INDEX on (cert_id) named `learn_rcrt_rem_cert_idx`             (23 chars ✓)

    Bump app/appinfo/info.xml <version> from 5.2.0.2 to 5.2.0.3 (the migration travels with this bump; orchestrator runs `occ upgrade` at wave-merge — do NOT run occ yourself).

    Do NOT run PHP/PHPStan/deploy — orchestrator centralizes Gate 1 + deploy per wave.
  </action>
  <verify>
    <automated>grep -q "learning_recert_reminders" app/lib/Migration/Version009600Date20260702000000.php && grep -q "learn_rcrt_rem_uq" app/lib/Migration/Version009600Date20260702000000.php && grep -q "cert_validity_months" app/lib/Migration/Version009600Date20260702000000.php && grep -q "anonymized_at" app/lib/Migration/Version009600Date20260702000000.php && grep -q "5.2.0.3" app/appinfo/info.xml && echo OK</automated>
  </verify>
  <done>Migration file exists with both columns + table + both index names (≤27 chars); info.xml at 5.2.0.3. All schema ops hasTable/hasColumn-guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Config defaults + PhpUnitStubs extension</name>
  <files>app/lib/AppInfo/ConfigDefaults.php, app/tests/Support/PhpUnitStubs.php</files>
  <action>
    1. Create app/lib/AppInfo/ConfigDefaults.php (namespace OCA\Learning\AppInfo) as a tiny constants holder used by services that read policy:
       - const RETENTION_YEARS_DEFAULT = '3';   // locked decision #3; FLAGGED for AWO/DSGVO confirmation (research suggested 5)
       - const RECERT_GRACE_DAYS_DEFAULT = '14';
       - const CERT_VALIDITY_MONTHS_DEFAULT = 12;
       - const RECERT_REMINDER_THRESHOLDS = [30, 7];
       Add a class doc-comment: values read at runtime via IConfig->getAppValue('learning', 'retention_years', ConfigDefaults::RETENTION_YEARS_DEFAULT).
    2. Extend app/tests/Support/PhpUnitStubs.php with any NOT-yet-stubbed OCP surface the Wave 2 tests need:
       - OCP\BackgroundJob\TimedJob (and IJob/IJobList if absent)
       - OCP\AppFramework\Utility\ITimeFactory (if absent)
       - OCP\IConfig getAppValue/setAppValue (if absent)
       Only add interfaces/classes that are MISSING — grep first; do not duplicate existing stubs.
  </action>
  <verify>
    <automated>grep -q "RETENTION_YEARS_DEFAULT" app/lib/AppInfo/ConfigDefaults.php && grep -q "RECERT_GRACE_DAYS_DEFAULT" app/lib/AppInfo/ConfigDefaults.php && grep -q "RECERT_REMINDER_THRESHOLDS" app/lib/AppInfo/ConfigDefaults.php && echo OK</automated>
  </verify>
  <done>ConfigDefaults holds the 4 constants; PhpUnitStubs compiles and covers TimedJob/ITimeFactory/IConfig used downstream.</done>
</task>

</tasks>

<verification>
- Migration present with correct table/columns/index names (≤27 chars, unprefixed).
- info.xml at 5.2.0.3.
- ConfigDefaults constants present; retention default = 3 (flagged).
- PhpUnitStubs extended (orchestrator runs PHPStan L5 at wave merge).
</verification>

<success_criteria>
Downstream Wave 2 test files can reference the reminder table, config defaults, and stubbed OCP surface without missing-symbol errors. `occ upgrade` (orchestrator) applies Version009600 cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/164-rezertifizierung-retention-i18n/164-01-SUMMARY.md`.
</output>
