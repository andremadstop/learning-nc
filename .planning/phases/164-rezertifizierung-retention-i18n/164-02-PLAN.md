---
phase: 164-rezertifizierung-retention-i18n
plan: 02
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - app/lib/Service/PassCriteriaService.php
  - app/lib/Service/IssuanceService.php
  - app/lib/Service/AssignmentService.php
  - app/lib/Service/CertificateVerifyService.php
  - app/tests/Unit/Service/PassCriteriaServiceTest.php
  - app/tests/Unit/Service/IssuanceServiceTest.php
  - app/tests/Unit/Service/CertificateVerifyServiceTest.php
autonomous: true
requirements: [RECERT-01, RECERT-02, RECERT-05, RECERT-07]
must_haves:
  truths:
    - "Final signatures for the RECERT-05 guard, DST date-math seam, closePeriod(), and derived verify-states are frozen as stubs"
    - "RED tests exist and FAIL against the stubs: testReCertPeriodGuard, testRevokeNoAutoReissue, testReCertNewRowOldUrlResolves, testValidityDstCrossing"
  artifacts:
    - path: "app/lib/Service/AssignmentService.php"
      provides: "closePeriod() signature stub"
      contains: "function closePeriod"
    - path: "app/tests/Unit/Service/PassCriteriaServiceTest.php"
      provides: "RED locking tests for the union guard"
      contains: "testReCertPeriodGuard"
  key_links:
    - from: "PassCriteriaServiceTest::testRevokeNoAutoReissue"
      to: "the punitive-revoke-no-reissue safety property"
      via: "asserts no cert minted on 2nd /pass-status after revoke"
      pattern: "testRevokeNoAutoReissue"
---

<objective>
Freeze the FINAL signatures for the security-critical RECERT-05 blast radius (guard, issuance date-math, closePeriod, verify derived-states) as stubs, then write the locking RED tests that define the contract Codex will review in Wave 3. Observed-RED discipline: tests must FAIL now, and the Codex-gated impl plan (164-04) flips them GREEN.

Purpose: This plan writes the executable specification of the union-guard redesign BEFORE any implementation. The stubs are what fabric→Gemini→grumpy-Codex review. No behavior changes here.
Output: Stubbed signatures + 4 RED tests.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-RESEARCH.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-VALIDATION.md

<interfaces>
Existing signatures (verified 2026-07-02) — DO NOT change these public shapes:
```php
// PassCriteriaService.php
public function evaluate(string $userId, int $courseId): PassResult;            // single external caller CourseController:789 (/pass-status, side-effecting read)
private function emitPassEventIfFirst(string $userId, int $courseId, int $score, int $threshold): void; // ← guard lives here
// IssuanceService.php
public function issueIfPassed(string $userId, int $courseId, PassResult $result): ?Certificate; // date-math seam line ~103
// AssignmentService.php  (markPassed/markInProgress currently DEAD CODE — zero callers)
public function markInProgress(string $userId, int $courseId): void;
public function markPassed(string $userId, int $courseId): void;
public function createAssignment(...): void;   // active_period_key = "{courseId}:{subjectType}:{subjectId}"
// CertificateVerifyService.php  — verifyByVerificationId precedence: invalid → withdrawn (getRevoked() BOOL, line 113) → expired (expires_at<now || signed validUntil<now) → valid
public function verifyByVerificationId(string $vid): array;                     // returns ['status'=>...]
```
UNION GUARD contract to be frozen (locked decision #1 — recommendation, Codex locks it in 164-04):
> A pass may emit + issue iff EITHER (a) NO cert has ever been issued for (user,course) [self-learner first cert / ASSIGN-04], OR (b) an assignment row exists with active_period_key IS NOT NULL AND status != 'passed' [open obligation period]. Within an open period, transition assignment.status→'passed' (wire markPassed) so a 2nd /pass-status does NOT re-emit/re-issue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze signatures — guard helper, date-math seam, closePeriod, derived-state helper (stubs only)</name>
  <files>app/lib/Service/PassCriteriaService.php, app/lib/Service/IssuanceService.php, app/lib/Service/AssignmentService.php, app/lib/Service/CertificateVerifyService.php</files>
  <action>
    Add signature stubs ONLY (no behavior change to existing code paths — existing methods keep working exactly as today so no regression):
    1. PassCriteriaService: add `private function mayIssue(string $userId, int $courseId): bool` returning a stub value (throw new \LogicException('not implemented — 164-04') OR return false) — the union-guard seam. Do NOT yet call it from emitPassEventIfFirst.
    2. IssuanceService: add `private function computeExpiry(int $issuedAt, int $courseId, ?int $assignmentOverrideMonths): ?int` stub (throw/return null) — the DST-safe date-math seam that will replace the `$issuedAt + $validityDays * 86400` line. Leave the existing line intact for now.
    3. AssignmentService: add `public function closePeriod(string $subjectType, string $subjectId, int $courseId): void` stub (throw new \LogicException('not implemented — 164-04')). NULL of active_idem_key/active_period_key + fresh-row insert comes in impl. Add PHPDoc: MUST NOT set revoked/revoked_at (SC2 — old URL must read "expired" not "withdrawn"; verify keys on getRevoked() bool). Close is audited via AuditService::logComplianceEvent, not the cert revoked_at column.
    4. CertificateVerifyService: add `public function deriveLifecycleState(Certificate $cert, int $now): string` stub returning 'valid' — the RECERT-01/03 derived-states seam (valid/expiring/overdue/expired) layered on top of verifyByVerificationId's base status. Do NOT change verifyByVerificationId yet.
    Before editing evaluate/emitPassEventIfFirst in 164-04, that plan runs gitnexus impact — here we only ADD stubs, existing behavior unchanged.
  </action>
  <verify>
    <automated>grep -q "function mayIssue" app/lib/Service/PassCriteriaService.php && grep -q "function computeExpiry" app/lib/Service/IssuanceService.php && grep -q "function closePeriod" app/lib/Service/AssignmentService.php && grep -q "function deriveLifecycleState" app/lib/Service/CertificateVerifyService.php && echo OK</automated>
  </verify>
  <done>Four signature stubs present; existing methods untouched (no regression to current pass/issue/verify behavior).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests — union guard + revoke-no-reissue + re-cert new row (RECERT-05/07)</name>
  <files>app/tests/Unit/Service/PassCriteriaServiceTest.php</files>
  <behavior>
    - testReCertPeriodGuard: given an assignment with active_period_key set and status='assigned', a pass emits+issues ONCE; a 2nd evaluate() in the SAME period (status now 'passed') does NOT re-emit/re-issue. A NEW period (fresh row, status='assigned') allows a new pass.
    - testRevokeNoAutoReissue: given a punitively revoked newest cert (revoked=true, active_idem_key=NULL) and NO open assignment period, evaluate() on next /pass-status mints NO new cert (must NOT auto-reissue).
    - testReCertNewRowOldUrlResolves (RECERT-07): re-cert in a new period inserts a NEW Certificate row (distinct verification_id); the OLD verification_id remains resolvable (immutable history).
  </behavior>
  <action>
    Add these three test methods to the existing app/tests/Unit/Service/PassCriteriaServiceTest.php (mock IssuanceService, AssignmentService, mappers as the existing tests do). They MUST FAIL now (mayIssue stub throws/false, markPassed unwired). Use the PhpUnitStubs surface. These are the locking tests Codex reviews before 164-04 implements.
    Do NOT run PHPUnit yourself — orchestrator runs it at wave merge and confirms RED.
  </action>
  <verify>
    <automated>grep -q "testReCertPeriodGuard" app/tests/Unit/Service/PassCriteriaServiceTest.php && grep -q "testRevokeNoAutoReissue" app/tests/Unit/Service/PassCriteriaServiceTest.php && grep -q "testReCertNewRowOldUrlResolves" app/tests/Unit/Service/PassCriteriaServiceTest.php && echo OK</automated>
  </verify>
  <done>Three RED locking tests present; expected to FAIL against stubs (orchestrator confirms RED at wave merge).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED tests — DST validity crossing + closed-period-reads-expired (RECERT-01/02/04-SC2)</name>
  <files>app/tests/Unit/Service/IssuanceServiceTest.php, app/tests/Unit/Service/CertificateVerifyServiceTest.php</files>
  <behavior>
    - testValidityDstCrossing (IssuanceServiceTest): with TZ=Europe/Berlin, issuing at 2026-03-29 (spring-forward day) with 12-month validity yields expiry 2027-03-29 via DateTimeImmutable::modify('+12 months') — NOT issuedAt + N*86400. Assert the exact expected timestamp differs from the naive +seconds result (must discriminate the two; under UTC it would not).
    - testClosedPeriodReadsExpired (CertificateVerifyServiceTest): the SC2 lock. The test drives AssignmentService::closePeriod on a period, then asserts CertificateVerifyService::verifyByVerificationId on the OLD cert returns status 'expired' — NOT 'withdrawn'. It MUST be RED at Wave 2 because closePeriod is a stub that throws (164-02). It flips GREEN only when 164-04 implements closePeriod leaving revoked=false (relying on expires_at<now). This ties the lock to closePeriod's write-set, not to a display helper. Seed the cert so expires_at<now and revoked=false is the post-close state.
  </behavior>
  <action>
    1. Add testValidityDstCrossing to app/tests/Unit/Service/IssuanceServiceTest.php. Pin TZ=Europe/Berlin (putenv / date_default_timezone_set). Assert against computeExpiry() (frozen seam) — RED now. Include the RECERT-02 per-course months + per-assignment override inputs.
    2. Add testClosedPeriodReadsExpired to app/tests/Unit/Service/CertificateVerifyServiceTest.php: call AssignmentService::closePeriod(...) (stub → throws → RED), then verifyByVerificationId(oldVid) must equal 'expired' (not 'withdrawn'). Mock the signing/key surface as the existing verify tests do so the base precedence (withdrawn keys on getRevoked() bool → false → falls through to expired) is exercised.
    Do NOT run PHPUnit — orchestrator confirms RED at wave merge.
  </action>
  <verify>
    <automated>grep -q "testValidityDstCrossing" app/tests/Unit/Service/IssuanceServiceTest.php && grep -Eq "Europe/Berlin" app/tests/Unit/Service/IssuanceServiceTest.php && grep -q "testClosedPeriodReadsExpired" app/tests/Unit/Service/CertificateVerifyServiceTest.php && grep -q "closePeriod" app/tests/Unit/Service/CertificateVerifyServiceTest.php && echo OK</automated>
  </verify>
  <done>DST test (TZ=Europe/Berlin) + testClosedPeriodReadsExpired present; the latter exercises closePeriod (RED via stub) and asserts verifyByVerificationId=='expired'. Both RED at wave merge.</done>
</task>

</tasks>

<verification>
- 4 signature stubs added, existing behavior unchanged.
- 5 RED tests present (3 in PassCriteriaServiceTest, 1 in IssuanceServiceTest, 1 in CertificateVerifyServiceTest), TZ-pinned where date-math is involved.
- Orchestrator at wave merge: PHPStan L5 clean + confirms the new tests are RED.
</verification>

<success_criteria>
The union-guard + DST + closePeriod + derived-state contracts are frozen and expressed as failing tests, ready for the Codex pre-impl review that gates plan 164-04.
</success_criteria>

<output>
After completion, create `.planning/phases/164-rezertifizierung-retention-i18n/164-02-SUMMARY.md`.
</output>
