---
phase: 164-rezertifizierung-retention-i18n
plan: 07
type: tdd
wave: 4
depends_on: ["03", "04"]
files_modified:
  - app/lib/Service/RetentionService.php
  - app/lib/BackgroundJob/RetentionJob.php
  - app/l10n/de.json
  - app/l10n/en.json
  - app/l10n/fr.json
  - app/l10n/ru.json
  - app/l10n/ar.json
  - app/l10n/de.js
  - app/l10n/en.js
  - app/l10n/fr.js
  - app/l10n/ru.js
  - app/l10n/ar.js
  - README.md
autonomous: true
requirements: [RECERT-07, DSGVO-03, DSGVO-05]
must_haves:
  truths:
    - "RetentionService anonymizes aged certs (null user_id + scrub credential_json + tombstone) and aged audit rows (null user_id only); the audit hash-chain still verifies (testAnonymizeKeepsChain GREEN)"
    - "All new v5.2.0 keys exist in all 5 languages; RecertL10n.test.js + check-i18n-parity.sh pass (DSGVO-05)"
    - "README documents: retention window bounds RECERT-07 'permanent' URL; AWO Betriebsvereinbarung (BetrVG §87 Abs.1 Nr.6) required before prod rollout of reminders/monitoring"
  artifacts:
    - path: "app/lib/Service/RetentionService.php"
      provides: "crypto-erasure anonymization preserving the audit chain"
      contains: "anonymizeExpired"
  key_links:
    - from: "RetentionService::anonymizeExpired"
      to: "AuditService chain integrity"
      via: "null user_id ONLY on audit rows (chain_hash unchanged)"
      pattern: "anonymize"
    - from: "app/l10n/*.json"
      to: "RecertL10n.test.js key-set"
      via: "l10n_js_sync.py regenerates .js from .json"
      pattern: "recert"
---

<objective>
Implement DSGVO-03 retention (crypto-erasure that preserves the audit chain), fill all new v5.2.0 i18n keys across 5 languages (DSGVO-05), and record the two non-code reconciliation/documentation items (RECERT-07 permanence bound + AWO Betriebsvereinbarung). Flips testAnonymizeKeepsChain + RecertL10n.test.js GREEN.

Purpose: Closes the data-lifecycle and multilingual gates. Sequenced after the Codex-gated wave (dep 164-04) per the mandated ordering; touches only retention/i18n/doc files (no issuance code). Retention crypto-erasure scrubs cert credential_json (breaks the cert SIGNATURE — acceptable erasure) but nulls only user_id on audit rows so the hash-chain stays verifiable.
Output: RetentionService/Job bodies, 5-language key fill + regenerated .js, README notes.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-RESEARCH.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-VALIDATION.md

<interfaces>
Retention (DSGVO-03), config retention_years (ConfigDefaults::RETENTION_YEARS_DEFAULT='3', FLAGGED for AWO/DSGVO confirmation):
- Certs older than retention window: set user_id=NULL, scrub credential_json (empty/redacted — crypto-erasure removes credentialSubject.name PII), set anonymized_at=now (tombstone → verify returns defined 'anonymized' state built in 164-06). This BREAKS the cert signature — acceptable per erasure semantics; state it in code + README.
- Audit rows: null user_id ONLY (DSGVO-01 UserDeletedListener pattern) — chain_hash is computed over facts NOT user_id, so the chain stays immutable/verifiable. NEVER scrub context_json (bound into the chain).
- Assignments: null/delete subject_id.
i18n (DSGVO-05): the canonical new-key list is enumerated in app/tests/unit/RecertL10n.test.js (164-03). Edit de.json as source (DE-source convention), add real translations in en/fr/ru/ar .json, then regenerate ALL .js via `python3 scripts/l10n_js_sync.py`. check-i18n-parity.sh must pass. (Frontend reads l10n/<lang>.js not .json — stale .js causes issues #18-#21.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RetentionService anonymization + RetentionJob body (DSGVO-03)</name>
  <files>app/lib/Service/RetentionService.php, app/lib/BackgroundJob/RetentionJob.php</files>
  <action>
    1. RetentionService::anonymizeExpired — read retention_years (IConfig, default via ConfigDefaults); compute cutoff via DateTimeImmutable::modify('-N years'); for aged certs: null user_id + scrub credential_json + set anonymized_at=now (tombstone). For aged audit rows: null user_id ONLY (do NOT touch context_json/chain_hash). For aged assignments: null/delete subject_id. Return count anonymized. Log via LoggerInterface.
    2. RetentionJob::run — replace no-op with `$this->retentionService->anonymizeExpired();` in try/catch+log. (Job registered in boot() by 164-03 — do NOT touch Application.php.)
    Flips GREEN: testAnonymizeKeepsChain (cert credential_json scrubbed AND AuditService chain re-verification still passes). Do NOT run PHP.
  </action>
  <verify>
    <automated>grep -q "anonymizeExpired" app/lib/Service/RetentionService.php && grep -q "anonymizeExpired" app/lib/BackgroundJob/RetentionJob.php && grep -Eq "credential_json|anonymized_at" app/lib/Service/RetentionService.php && echo OK  # orchestrator: phpunit --filter testAnonymizeKeepsChain PASS</automated>
  </verify>
  <done>Retention crypto-erases certs + null-user-ids audit rows; chain still verifies; testAnonymizeKeepsChain green at wave merge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: i18n fill — 5 languages + regenerate .js (DSGVO-05)</name>
  <files>app/l10n/de.json, app/l10n/en.json, app/l10n/fr.json, app/l10n/ru.json, app/l10n/ar.json, app/l10n/de.js, app/l10n/en.js, app/l10n/fr.js, app/l10n/ru.js, app/l10n/ar.js</files>
  <action>
    Add every new v5.2.0 key from the canonical list in app/tests/unit/RecertL10n.test.js to all 5 .json files: recert_reminder subject + body, status labels (valid/expiring/overdue/expired), re-enrollment UI strings, validity/retention config UI strings. de.json = source (real DE); provide real translations for en/fr/ru/ar (not copies of DE). Then run `python3 scripts/l10n_js_sync.py` to regenerate ALL .js from .json (never hand-edit .js). Flips GREEN: RecertL10n.test.js + check-i18n-parity.sh.
  </action>
  <verify>
    <automated>python3 scripts/l10n_js_sync.py --check && bash scripts/check-i18n-parity.sh && echo OK</automated>
  </verify>
  <done>All new keys present in 5 languages; .js regenerated in sync; RecertL10n.test.js + parity check green (Vitest at wave merge, ESLint 0).</done>
</task>

<task type="auto">
  <name>Task 3: Documentation — RECERT-07 permanence bound + AWO Betriebsvereinbarung (non-code)</name>
  <files>README.md</files>
  <action>
    Add a short "Cert lifecycle & compliance" section to README.md documenting:
    1. RECERT-07 reconciliation: the verify URL is "permanent" only until the DSGVO-03 retention window (retention_years, default 3, FLAGGED for AWO/DSGVO confirmation) anonymizes the record; after that the URL returns a defined "no longer available" state (tombstone), never a 500.
    2. AWO Betriebsvereinbarung (BetrVG §87 Abs.1 Nr.6): the client must be informed that a works-council agreement is required BEFORE production rollout of reminders/monitoring. The Phase 162 transient-segment design is the technical mitigation. This is an organizational/legal prerequisite, not a code gate.
  </action>
  <verify>
    <automated>grep -qi "Betriebsvereinbarung\|BetrVG" README.md && grep -qi "retention" README.md && echo OK</automated>
  </verify>
  <done>README documents both reconciliations; surfaced for Andre/AWO.</done>
</task>

</tasks>

<verification>
- Retention body + job wired; chain preserved.
- 5-language parity green (l10n_js_sync --check + check-i18n-parity.sh).
- README notes present.
- Orchestrator wave merge: full PHPUnit + PHPStan L5 + Vitest + i18n parity all green.
</verification>

<success_criteria>
Data ages out safely without breaking the audit chain; all v5.2.0 strings are multilingual; the permanence bound and the works-council prerequisite are documented.
</success_criteria>

<output>
After completion, create `.planning/phases/164-rezertifizierung-retention-i18n/164-07-SUMMARY.md`.
</output>
