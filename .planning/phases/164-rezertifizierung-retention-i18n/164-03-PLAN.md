---
phase: 164-rezertifizierung-retention-i18n
plan: 03
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - app/lib/Service/RecertReminderService.php
  - app/lib/Db/RecertReminderMapper.php
  - app/lib/Db/RecertReminder.php
  - app/lib/Service/RetentionService.php
  - app/lib/BackgroundJob/RecertPeriodCloseJob.php
  - app/lib/BackgroundJob/RetentionJob.php
  - app/lib/AppInfo/Application.php
  - app/tests/Unit/BackgroundJob/RecertPeriodCloseJobTest.php
  - app/tests/Unit/Service/RecertReminderServiceTest.php
  - app/tests/Unit/Service/RetentionServiceTest.php
  - app/tests/unit/RecertL10n.test.js
autonomous: true
requirements: [RECERT-04, RECERT-06, RECERT-07, DSGVO-03, DSGVO-05]
must_haves:
  truths:
    - "Skeletons frozen for RecertReminderService(+mapper/entity), RetentionService, RecertPeriodCloseJob, RetentionJob"
    - "Both TimedJobs are registered in Application::boot() (skeleton bodies no-op until impl)"
    - "RED tests fail against stubs: testDoubleRunSingleRow, testOncePerThreshold, testAnonymizeKeepsChain; RecertL10n parity test fails on missing keys"
  artifacts:
    - path: "app/lib/BackgroundJob/RecertPeriodCloseJob.php"
      provides: "daily TimedJob skeleton"
      contains: "extends TimedJob"
    - path: "app/lib/Db/RecertReminderMapper.php"
      provides: "insert-once + findByCertAndThreshold on learning_recert_reminders"
      contains: "learning_recert_reminders"
  key_links:
    - from: "Application::boot()"
      to: "RecertPeriodCloseJob + RetentionJob"
      via: "$jobList->add(... ) guarded by has()"
      pattern: "jobList->add"
---

<objective>
Freeze the lifecycle-service skeletons (reminders, retention, both TimedJobs, reminder mapper/entity), register the jobs in boot(), and write the idempotency + retention + i18n-parity RED tests. Observed-RED: these tests FAIL against the stubs; impl plans 164-05/06/07 flip them GREEN.

Purpose: Owns ALL Application::boot() job registration (so Wave 3 impl plans never touch Application.php → no merge conflict) and all new lifecycle class shapes. Independent of the guard blast radius (different files) → runs parallel with 164-02.
Output: 6 skeleton files + boot() registration + 3 PHP RED tests + 1 JS RED parity test.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-RESEARCH.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-VALIDATION.md

<interfaces>
TimedJob template (from lib/BackgroundJob/AuditCheckpointJob.php):
```php
class RecertPeriodCloseJob extends \OCP\BackgroundJob\TimedJob {
    public function __construct(ITimeFactory $time, private AssignmentService $svc, private LoggerInterface $log) {
        parent::__construct($time); $this->setInterval(86400); // daily
    }
    protected function run($argument): void { try { /* impl in 164-05 */ } catch (\Throwable $e) { $this->log->warning('RecertPeriodCloseJob: '.$e->getMessage(), ['app'=>'learning']); } }
}
```
Register in Application::boot() alongside the existing block (lib/AppInfo/Application.php ~line 116-134):
```php
if (!$jobList->has(RecertPeriodCloseJob::class, null)) { $jobList->add(RecertPeriodCloseJob::class); }
if (!$jobList->has(RetentionJob::class, null)) { $jobList->add(RetentionJob::class); }
```
> Jobs are registered imperatively in boot(), NOT in info.xml (house rule).

Reminder mapper (QBMapper) — table `learning_recert_reminders` (unprefixed), UNIQUE(cert_id, threshold_days):
```php
public function insertOnce(int $certId, int $thresholdDays, int $sentAt): bool; // catch REASON_UNIQUE_CONSTRAINT_VIOLATION → return false ("already sent")
public function findByCertAndThreshold(int $certId, int $thresholdDays): ?RecertReminder;
```
Notifier reuse (lib/Notification/Notifier.php has cases badge_earned…compliance_reminder) — a new case `recert_reminder` is added in impl (164-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skeletons + boot() registration</name>
  <files>app/lib/Service/RecertReminderService.php, app/lib/Db/RecertReminderMapper.php, app/lib/Db/RecertReminder.php, app/lib/Service/RetentionService.php, app/lib/BackgroundJob/RecertPeriodCloseJob.php, app/lib/BackgroundJob/RetentionJob.php, app/lib/AppInfo/Application.php</files>
  <action>
    Create signature stubs (namespace OCA\Learning\*):
    1. Db/RecertReminder entity (QBMapper Entity): int cert_id, int threshold_days, int sent_at (+ getters/setters, addType).
    2. Db/RecertReminderMapper (extends QBMapper, table 'learning_recert_reminders'): stub insertOnce(): bool and findByCertAndThreshold(): ?RecertReminder (throw \LogicException('164-06')).
    3. Service/RecertReminderService: constructor injecting CertificateMapper, RecertReminderMapper, IManager (notifications), ITimeFactory, IConfig, LoggerInterface. Public stub `sendRecertReminders(): int` (throw \LogicException('164-06')). PHPDoc: T-30/T-7 computed from expires_at via DateTimeImmutable (DST-safe), idempotent per (certId, threshold) via insertOnce.
    4. Service/RetentionService: constructor injecting CertificateMapper, AuditService/AuditMapper, AssignmentService/mapper, IConfig, ITimeFactory, LoggerInterface. Public stub `anonymizeExpired(): int` (throw \LogicException('164-07')). PHPDoc: null user_id + scrub credential_json (crypto-erasure) + set anonymized_at=now (tombstone, non-null); audit rows null user_id ONLY (chain immutable).
    5. BackgroundJob/RecertPeriodCloseJob + RetentionJob: TimedJob skeletons per the template above, setInterval(86400), run() no-op with try/catch+log (impl later).
    6. Application.php: register BOTH jobs in boot() alongside the existing has()/add() block. Add the `use` imports.
    Do NOT run PHP/deploy.
  </action>
  <verify>
    <automated>grep -q "extends TimedJob\|extends \\\\OCP\\\\BackgroundJob\\\\TimedJob" app/lib/BackgroundJob/RecertPeriodCloseJob.php && grep -q "extends TimedJob\|extends \\\\OCP\\\\BackgroundJob\\\\TimedJob" app/lib/BackgroundJob/RetentionJob.php && grep -q "learning_recert_reminders" app/lib/Db/RecertReminderMapper.php && grep -q "RecertPeriodCloseJob" app/lib/AppInfo/Application.php && grep -q "RetentionJob" app/lib/AppInfo/Application.php && grep -q "function sendRecertReminders" app/lib/Service/RecertReminderService.php && grep -q "function anonymizeExpired" app/lib/Service/RetentionService.php && echo OK</automated>
  </verify>
  <done>All 6 skeletons present with frozen signatures; both jobs registered in boot() (guarded by has()); no-op bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests — period-close idempotency + reminder-once</name>
  <files>app/tests/Unit/BackgroundJob/RecertPeriodCloseJobTest.php, app/tests/Unit/Service/RecertReminderServiceTest.php</files>
  <behavior>
    - testDoubleRunSingleRow: running RecertPeriodCloseJob (via closePeriod) TWICE for the same expired period creates EXACTLY ONE new assignment row (2nd run either sees not-expired or hits UNIQUE active_period_key → skip). Assert insert called once / one row.
    - testOncePerThreshold: RecertReminderService::sendRecertReminders() fires a notification EXACTLY once per (certId, threshold) across TWO job runs — 2nd run's insertOnce returns false (UNIQUE violation caught) → no 2nd notification. Assert IManager->notify called once per pair.
  </behavior>
  <action>
    Create the two test files (mirror existing BackgroundJob/Service test style; mock AssignmentService/mappers/IManager/ITimeFactory via PhpUnitStubs). Pin ITimeFactory to a fixed "now" so thresholds are deterministic. Tests MUST FAIL against stubs. Do NOT run PHPUnit.
  </action>
  <verify>
    <automated>grep -q "testDoubleRunSingleRow" app/tests/Unit/BackgroundJob/RecertPeriodCloseJobTest.php && grep -q "testOncePerThreshold" app/tests/Unit/Service/RecertReminderServiceTest.php && echo OK</automated>
  </verify>
  <done>Both idempotency RED tests present, deterministic clock; expected RED (orchestrator confirms).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED tests — retention chain + i18n parity</name>
  <files>app/tests/Unit/Service/RetentionServiceTest.php, app/tests/unit/RecertL10n.test.js</files>
  <behavior>
    - testAnonymizeKeepsChain: anonymizeExpired() on an aged cert NULLs user_id AND scrubs credential_json (crypto-erasure) AND sets anonymized_at=now (tombstone, non-null — matches the 164-01 migration column and the 164-06 anonymized_at !== null guard); on the linked audit rows it NULLs user_id ONLY. Assert: (1) cert credential_json empty/redacted AND anonymized_at is non-null; (2) AuditService chain re-verification still passes (chain_hash unchanged — user_id nulling does not feed the hash). This is the DSGVO-03 correctness property.
    - RecertL10n.test.js (mirror app/tests/unit/BadgeL10n.test.js): the set of NEW v5.2.0 keys (recert_reminder subject + body, status labels valid/expiring/overdue/expired, re-enrollment strings, validity/retention config UI) must exist in ALL 5 langs (de/en/fr/ru/ar). FAILS now because the keys are not yet added (164-07 adds them).
  </behavior>
  <action>
    1. Create RetentionServiceTest.php (mock CertificateMapper, AuditService/mapper). Assert chain still verifies after anonymize. RED against stub.
    2. Create app/tests/unit/RecertL10n.test.js mirroring BadgeL10n.test.js. Enumerate the expected new key list explicitly in the test (single source of truth for 164-07). Assert parity across the 5 l10n json files. RED now.
    Do NOT run tests.
  </action>
  <verify>
    <automated>grep -q "testAnonymizeKeepsChain" app/tests/Unit/Service/RetentionServiceTest.php && test -f app/tests/unit/RecertL10n.test.js && grep -Eq "de|en|fr|ru|ar" app/tests/unit/RecertL10n.test.js && echo OK</automated>
  </verify>
  <done>Retention chain RED test + RecertL10n parity RED test present; the JS test enumerates the canonical new-key list.</done>
</task>

</tasks>

<verification>
- 6 skeletons + boot() registration present; no-op job bodies.
- 3 PHP RED tests + 1 JS RED parity test present.
- Orchestrator at wave merge: PHPStan L5 clean, Vitest runs (RecertL10n RED), PHPUnit confirms the 3 RED tests fail.
</verification>

<success_criteria>
Lifecycle contracts frozen; idempotency + retention + i18n specified as failing tests; jobs wired into boot(). Wave 3 impl plans touch only service/job bodies, never Application.php.
</success_criteria>

<output>
After completion, create `.planning/phases/164-rezertifizierung-retention-i18n/164-03-SUMMARY.md`.
</output>
