---
phase: 164-rezertifizierung-retention-i18n
plan: 04
type: tdd
wave: 3
depends_on: ["02"]
files_modified:
  - app/lib/Service/PassCriteriaService.php
  - app/lib/Service/IssuanceService.php
  - app/lib/Service/AssignmentService.php
autonomous: true
codex_gated: true
requirements: [RECERT-01, RECERT-02, RECERT-05, RECERT-07]
must_haves:
  truths:
    - "A punitive revoke does NOT auto-reissue on the next GET /pass-status (testRevokeNoAutoReissue GREEN)"
    - "Re-cert allows a new pass in a NEW period but NOT a double-issue within one period (testReCertPeriodGuard GREEN)"
    - "Re-cert creates a NEW cert row; the old verification_id URL stays resolvable (testReCertNewRowOldUrlResolves GREEN)"
    - "Validity uses DateTimeImmutable::modify('+N months'); DST crossing 2026-03-29→2027-03-29 correct under TZ=Europe/Berlin (testValidityDstCrossing GREEN)"
    - "closePeriod() NULLs active_idem_key + active_period_key and inserts a fresh assignment row WITHOUT setting revoked/revoked_at"
    - "After closePeriod, the old verify URL reads 'expired' not 'withdrawn' — closePeriod leaves revoked=false (testClosedPeriodReadsExpired GREEN, SC2)"
  artifacts:
    - path: "app/lib/Service/PassCriteriaService.php"
      provides: "union guard wired into emitPassEventIfFirst + markPassed transition"
      contains: "mayIssue"
    - path: "app/lib/Service/IssuanceService.php"
      provides: "DST-safe computeExpiry replacing the +N*86400 line"
      contains: "modify"
  key_links:
    - from: "PassCriteriaService::emitPassEventIfFirst"
      to: "AssignmentService::markPassed (in-period dedup)"
      via: "status transition assigned→passed after issue"
      pattern: "markPassed"
    - from: "IssuanceService::issueIfPassed"
      to: "no-open-unmet-period guard"
      via: "replaces the !revoked idempotency guard"
      pattern: "mayIssue|active_period_key"
---

<objective>
⚠ SECURITY-CRITICAL — OPEN-HEART SURGERY on the cert-issuance guard (RECERT-05). Implement the union-rule guard, the DST-safe date-math, the markPassed/markInProgress wiring, and closePeriod(), flipping the Wave-2 RED tests GREEN. **This plan is GATED by a mandatory pre-implementation 3-KI security review (fabric → Gemini → grumpy Codex). Codex is the HARD gate: NO implementation task may run until Codex returns SHIP.**

Purpose: A wrong redesign either re-issues a punitively-revoked cert (proven bug — the guard must NOT key on cert-revocation-state) or blocks legitimate re-cert. The assignment PERIOD is the single source of "may (re)issue now", reconciled with ASSIGN-04 as a UNION.
Output: Guard + date-math + closePeriod implemented; 5 locking tests GREEN.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-RESEARCH.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-VALIDATION.md

<interfaces>
UNION GUARD (locked decision #1 — Codex must confirm it closes all four properties):
> A pass may emit + issue iff EITHER
>   (a) NO cert has ever been issued for (user,course)      [self-learner first cert / ASSIGN-04], OR
>   (b) an assignment row exists with active_period_key IS NOT NULL AND status != 'passed'  [open obligation period].
> Within an open period, transition assignment.status → 'passed' (markPassed) so a 2nd /pass-status does NOT re-emit/re-issue.

Behaviour matrix that MUST hold:
| Situation | newest cert | open period? | re-issue? |
| self-learner first pass | none | n/a | YES (a) |
| same period, 2nd GET | non-revoked | status=passed | NO |
| instructor punitive revoke | revoked=true | none | NO |
| period-close → new pass | expired | new row status=assigned | YES |
| self-learner expired, no assignment | expired | none | NO (documented limitation) |

DST date-math (replaces IssuanceService line ~103 `$issuedAt + $validityDays*86400`):
```php
$months = $assignmentOverrideMonths ?? $course->getCertValidityMonths() ?? 12;
$expiresAt = $months > 0
  ? (new \DateTimeImmutable('@'.$issuedAt))->setTimezone(new \DateTimeZone($serverTz))->modify('+'.$months.' months')->getTimestamp()
  : null;
```
closePeriod: NULL active_idem_key + active_period_key, INSERT fresh assignment row, DO NOT set revoked/revoked_at (verify keys on getRevoked() bool → stays "expired" per SC2). Audit the close via AuditService::logComplianceEvent (facts only: course_id, verification_id, timestamps — NO PII in context_json).
</interfaces>
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: MANDATORY pre-impl 3-KI security review (fabric → Gemini → grumpy Codex — Codex is the hard gate)</name>
  <files>.planning/phases/164-rezertifizierung-retention-i18n/164-04-CODEX-REVIEW.md</files>
  <action>
    BEFORE any code edit in Tasks 2-3, run the pre-implementation review of the union-guard design + the Wave-2 RED tests (164-02):
    1. fabric --model gemini-2.5-pro < input  — design/completeness review of the union rule + behaviour matrix.
    2. Gemini (fabric) — confirm the matrix + closePeriod write-set (must NOT set revoked/revoked_at) satisfies SC2.
    3. grumpy Codex (HARD GATE): `codex exec --sandbox read-only "<prompt>" < /dev/null` — feed the union rule, the behaviour matrix, the RED tests, and the four safety properties: (i) punitive-revoke-no-reissue, (ii) self-learner first cert, (iii) no double-issue in-period, (iv) re-issue allowed after period close. Also review the markPassed/markInProgress wiring (new integration in the hot /pass-status path) and the issueIfPassed guard change (!revoked → no-open-unmet-period).
    Record findings + resolutions in 164-04-CODEX-REVIEW.md. Iterate the design until Codex returns SHIP. **Tasks 2-3 are BLOCKED until 164-04-CODEX-REVIEW.md records a Codex SHIP.**
    Also run gitnexus `impact({target:"evaluate", direction:"upstream"})` and record the blast radius (expected: single external caller CourseController:789) in the review doc — CLAUDE.md hard rule before editing PassCriteriaService::evaluate.
  </action>
  <verify>
    <automated>grep -qi "SHIP" .planning/phases/164-rezertifizierung-retention-i18n/164-04-CODEX-REVIEW.md && grep -qi "impact\|blast" .planning/phases/164-rezertifizierung-retention-i18n/164-04-CODEX-REVIEW.md && echo OK</automated>
  </verify>
  <done>164-04-CODEX-REVIEW.md records fabric+Gemini+Codex findings, the gitnexus impact result, and a Codex SHIP verdict on the union guard. Only then may impl proceed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement union guard + markPassed/markInProgress wiring (RECERT-05/07)</name>
  <files>app/lib/Service/PassCriteriaService.php, app/lib/Service/IssuanceService.php</files>
  <action>
    Only after Task 1 Codex SHIP. Run gitnexus impact on evaluate() first (per CLAUDE.md).
    1. PassCriteriaService::mayIssue — implement the UNION rule: (a) CertificateMapper::findByUserAndCourse returns nothing, OR (b) an assignment row exists with active_period_key IS NOT NULL AND status != 'passed'.
    2. Wire mayIssue into emitPassEventIfFirst: gate emit+issue on mayIssue; after a successful issue in an open period, call AssignmentService::markPassed (in-period dedup). Wire markInProgress on first attempt where appropriate. (markPassed/markInProgress were DEAD CODE — this is the new integration Codex reviewed.)
    3. IssuanceService::issueIfPassed — replace the `!revoked` idempotency guard with the period-aware check (do not return a stale expired cert that would block re-cert; do not auto-reissue a punitively-revoked cert). Keep the DB UNIQUE(active_idem_key) as the concurrency backstop (CAS house pattern — NC has no forUpdate()).
    Flips GREEN: testReCertPeriodGuard, testRevokeNoAutoReissue, testReCertNewRowOldUrlResolves.
    Do NOT run PHP/deploy — orchestrator gates.
  </action>
  <verify>
    <automated>grep -q "mayIssue" app/lib/Service/PassCriteriaService.php && grep -q "markPassed" app/lib/Service/PassCriteriaService.php && echo OK  # orchestrator: phpunit --filter 'testReCertPeriodGuard|testRevokeNoAutoReissue|testReCertNewRowOldUrlResolves' must PASS</automated>
  </verify>
  <done>Union guard implemented + wired; markPassed dedup in-period; issueIfPassed period-aware. The 3 guard/history RED tests pass at orchestrator wave merge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DST-safe date-math + per-course/per-assignment validity + closePeriod (RECERT-01/02/04-close)</name>
  <files>app/lib/Service/IssuanceService.php, app/lib/Service/AssignmentService.php</files>
  <action>
    1. IssuanceService::computeExpiry — implement DateTimeImmutable::modify('+N months') per the interfaces block; months = per-assignment override ?? course.cert_validity_months ?? 12; months<=0 → null (no expiry). Replace the line-~103 `+N*86400` call site with computeExpiry(). Keep buildCredential validUntil in sync (signed authority).
    2. AssignmentService::closePeriod — NULL active_idem_key + active_period_key, INSERT a fresh assignment row (reuse the same active_period_key string so a 2nd close hits UNIQUE → exactly one row), DO NOT set revoked/revoked_at. Audit via AuditService::logComplianceEvent (facts only). This is the write closePeriod stub promised; RecertPeriodCloseJob body (164-05) calls it.
    closePeriod leaving revoked=false is exactly what makes the old verify URL read 'expired' not 'withdrawn' (verify keys on getRevoked() bool).
    Flips GREEN: testValidityDstCrossing, testClosedPeriodReadsExpired.
  </action>
  <verify>
    <automated>grep -q "modify(" app/lib/Service/IssuanceService.php && grep -q "computeExpiry" app/lib/Service/IssuanceService.php && grep -Eq "active_period_key" app/lib/Service/AssignmentService.php && echo OK  # orchestrator: phpunit --filter 'testValidityDstCrossing|testClosedPeriodReadsExpired' PASS (TZ=Europe/Berlin)</automated>
  </verify>
  <done>Date-math uses modify() (no +86400); per-course + per-assignment validity honored; closePeriod frees the re-issue slot without touching revoked. DST test green at wave merge.</done>
</task>

</tasks>

<verification>
- Task 1 Codex SHIP recorded before any code edit (hard gate).
- gitnexus impact run on evaluate() before editing.
- Orchestrator wave merge: PHPStan L5 clean + full PHPUnit; the 4 locking tests GREEN.
</verification>

<success_criteria>
The RECERT-05 union guard is implemented under Codex sign-off; punitive-revoke does not auto-reissue; re-cert works per period; DST-safe validity holds; closePeriod frees re-issue without rendering "withdrawn".
</success_criteria>

<output>
After completion, create `.planning/phases/164-rezertifizierung-retention-i18n/164-04-SUMMARY.md` (include the Codex SHIP reference).
</output>
