---
phase: 164-rezertifizierung-retention-i18n
plan: 06
type: tdd
wave: 4
depends_on: ["02", "03", "04"]
files_modified:
  - app/lib/Service/CertificateVerifyService.php
  - app/tests/Unit/Service/CertificateVerifyServiceTest.php
  - app/lib/Service/RecertReminderService.php
  - app/lib/Db/RecertReminderMapper.php
  - app/lib/BackgroundJob/SendRemindersJob.php
  - app/lib/Notification/Notifier.php
autonomous: true
requirements: [RECERT-01, RECERT-03, RECERT-06]
must_haves:
  truths:
    - "Verify derives lifecycle states valid/expiring/overdue with the grace window (testDeriveLifecycleStates GREEN, RECERT-01/03)"
    - "An anonymized cert (anonymized_at set) returns a defined 'anonymized' status from verify — never a 500 (testAnonymizedReadsDefinedState GREEN)"
    - "Reminders fire at T-30 + T-7 exactly once per (certId, threshold) across job re-runs (testOncePerThreshold GREEN)"
  artifacts:
    - path: "app/lib/Service/RecertReminderService.php"
      provides: "T-30/T-7 reminder dispatch, idempotent via insertOnce"
      contains: "sendRecertReminders"
    - path: "app/lib/Notification/Notifier.php"
      provides: "recert_reminder notification case"
      contains: "recert_reminder"
  key_links:
    - from: "RecertReminderService::sendRecertReminders"
      to: "RecertReminderMapper::insertOnce (UNIQUE cert_id,threshold_days)"
      via: "catch unique-violation → skip send"
      pattern: "insertOnce"
    - from: "SendRemindersJob::run"
      to: "RecertReminderService::sendRecertReminders"
      via: "existing reminder job dispatch"
      pattern: "sendRecertReminders"
---

<objective>
Implement the cert lifecycle states (RECERT-01/03) and the T-30/T-7 recert reminders (RECERT-06). Verify derives valid/expiring/overdue/expired with the grace window and returns a defined state for anonymized tombstones. Reminders are idempotent per (cert, threshold) via the UNIQUE-slot insert. Flips testDeriveLifecycleStates + testAnonymizedReadsDefinedState + testOncePerThreshold GREEN. (SC2's closed→'expired' lock lives in 164-04 where closePeriod is built.)

Purpose: Sequenced after the Codex-gated wave (dep 164-04) per the mandated ordering, though it touches only verify/reminder files (no issuance code). Reuses the existing Notifier + SendRemindersJob dispatch — no new job, no Application.php edit.
Output: derived-state logic, anonymized branch, reminder dispatch + Notifier case.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-RESEARCH.md
@.planning/phases/164-rezertifizierung-retention-i18n/164-VALIDATION.md

<interfaces>
Verify precedence (CertificateVerifyService::verifyByVerificationId): invalid → withdrawn (getRevoked() bool, line 113) → expired (signed validUntil<now || expires_at<now) → valid. deriveLifecycleState(Certificate,$now) stub (164-02) layers derived states on top:
- expired: expires_at < now
- overdue: expired AND now > expires_at + grace  (RECERT-03; grace = recert_grace_days)
- expiring: now within reminder window before expires_at (not yet expired)
- valid: otherwise
- anonymized: anonymized_at !== null → return early, defined "no longer available"/"expired" banner, NO PII, checked BEFORE signature verify (credential_json is scrubbed for anonymized certs).
Reminder thresholds: ConfigDefaults::RECERT_REMINDER_THRESHOLDS = [30, 7]. T-30/T-7 computed from expires_at via DateTimeImmutable (DST-safe). Idempotency: RecertReminderMapper::insertOnce(certId, threshold, sentAt) catches REASON_UNIQUE_CONSTRAINT_VIOLATION → false → skip notify.
Notifier: add `case 'recert_reminder'` mirroring the existing 'compliance_reminder' case (subject + link). INotificationManager is the primary, mail-less-safe channel (RECERT-06); IMailer only additive where getEMailAddress() non-null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify lifecycle states + anonymized branch (RECERT-01/03)</name>
  <files>app/lib/Service/CertificateVerifyService.php</files>
  <action>
    Implement deriveLifecycleState(Certificate, int $now): return 'anonymized' first if anonymized_at !== null (defined banner, no signature check); else valid/expiring/overdue/expired per the interfaces block (grace from IConfig recert_grace_days). Ensure verifyByVerificationId stays backward-compatible (its base status precedence unchanged); derived state is additive (e.g. surfaced in the DTO for the UI banner). For the anonymized tombstone, verifyByVerificationId must return a defined 'anonymized' status BEFORE the signature check (credential_json is scrubbed) — never a 500.
    Add the two locking tests to app/tests/Unit/Service/CertificateVerifyServiceTest.php:
    - testDeriveLifecycleStates: assert expiring (within window, not expired) and overdue (expired + past grace) classifications against a pinned clock.
    - testAnonymizedReadsDefinedState: a cert with anonymized_at set + scrubbed credential_json → verifyByVerificationId returns status 'anonymized' and does NOT throw/500.
    Do NOT run PHP.
  </action>
  <verify>
    <automated>grep -q "deriveLifecycleState" app/lib/Service/CertificateVerifyService.php && grep -Eq "overdue|expiring|anonymized" app/lib/Service/CertificateVerifyService.php && grep -q "testDeriveLifecycleStates" app/tests/Unit/Service/CertificateVerifyServiceTest.php && grep -q "testAnonymizedReadsDefinedState" app/tests/Unit/Service/CertificateVerifyServiceTest.php && echo OK  # orchestrator: phpunit --filter 'testDeriveLifecycleStates|testAnonymizedReadsDefinedState' PASS</automated>
  </verify>
  <done>Derived states + anonymized branch implemented; testDeriveLifecycleStates + testAnonymizedReadsDefinedState green at wave merge; no 500 for anonymized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reminder dispatch (T-30/T-7) + mapper insertOnce + Notifier case (RECERT-06)</name>
  <files>app/lib/Service/RecertReminderService.php, app/lib/Db/RecertReminderMapper.php, app/lib/BackgroundJob/SendRemindersJob.php, app/lib/Notification/Notifier.php</files>
  <action>
    1. RecertReminderMapper::insertOnce — INSERT into learning_recert_reminders; catch \OCP\DB\Exception::REASON_UNIQUE_CONSTRAINT_VIOLATION → return false (already sent). findByCertAndThreshold as declared.
    2. RecertReminderService::sendRecertReminders — for each non-revoked cert with expires_at set: compute T-30/T-7 boundaries from expires_at via DateTimeImmutable (DST-safe); if now is at/past a threshold and insertOnce(certId, threshold, now) returns true → notify via IManager (objectType 'recert_reminder'). Return count sent.
    3. SendRemindersJob::run — add a call to $this->recertReminderService->sendRecertReminders() alongside the existing reminder dispatches (inject the service). No new job / no Application.php edit.
    4. Notifier::prepare — add `case 'recert_reminder'` mirroring 'compliance_reminder' (localized subject + verify/course link). Keys must land in i18n (164-07 owns the translation fill; use the key names enumerated in RecertL10n.test.js).
    Flips GREEN: testOncePerThreshold. Do NOT run PHP.
  </action>
  <verify>
    <automated>grep -q "sendRecertReminders" app/lib/Service/RecertReminderService.php && grep -q "sendRecertReminders" app/lib/BackgroundJob/SendRemindersJob.php && grep -q "recert_reminder" app/lib/Notification/Notifier.php && grep -q "insertOnce" app/lib/Db/RecertReminderMapper.php && echo OK  # orchestrator: phpunit --filter testOncePerThreshold PASS</automated>
  </verify>
  <done>Reminders fire T-30/T-7, idempotent per (cert, threshold) across re-runs; Notifier renders recert_reminder; testOncePerThreshold green at wave merge.</done>
</task>

</tasks>

<verification>
- Derived states + anonymized branch; reminder dispatch + mapper + Notifier case.
- No Application.php edit (reuses SendRemindersJob).
- Orchestrator wave merge: PHPStan L5 clean; phpunit testDeriveLifecycleStates + testAnonymizedReadsDefinedState + testOncePerThreshold GREEN.
</verification>

<success_criteria>
Cert status surfaces valid/expiring/overdue/expired with grace; anonymized certs return a defined state; reminders fire twice (T-30, T-7) with no storms.
</success_criteria>

<output>
After completion, create `.planning/phases/164-rezertifizierung-retention-i18n/164-06-SUMMARY.md`.
</output>
