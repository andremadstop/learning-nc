---
phase: 44-design-token-system-narrative-skin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/css/style.css
  - app/src/components/AbenteuerMode.vue
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03

must_haves:
  truths:
    - "Alle Farben der App sind als --lnc-* CSS-Custom-Properties in style.css definiert"
    - "AbenteuerMode rendert im Dark-Theme mit --lnc-surface-dark Hintergrund"
    - "Training/Standard-Modus rendert im Light-Theme mit --lnc-surface-light Hintergrund"
    - "Animationen fade, snap-in, pulse existieren als utility-Klassen"
    - "Bei prefers-reduced-motion werden alle Animationen durch sofortige Zustandswechsel ersetzt"
  artifacts:
    - path: "app/css/style.css"
      provides: "CSS-Token-Layer, Dark/Light mode tokens, Motion utility classes"
      contains: "--lnc-primary"
    - path: "app/src/components/AbenteuerMode.vue"
      provides: "Migrated component using global --lnc-* tokens instead of local --rpg-* vars"
      contains: "--lnc-"
  key_links:
    - from: "app/css/style.css"
      to: "app/src/components/AbenteuerMode.vue"
      via: "CSS custom properties inheritance"
      pattern: "var\\(--lnc-"
---

<objective>
CSS-Token-Layer mit Farbpaletten, Dark/Light Mode und Motion-Utilities in style.css definieren, dann AbenteuerMode.vue als erste Komponente auf die neuen Tokens migrieren.

Purpose: Fundament fuer konsistentes visuelles System — alle nachfolgenden Phasen (Charakter, UI-Komponenten, Kampagnen) bauen auf diesen Tokens auf.
Output: style.css mit Token-Layer, Dark/Light-Scopes, Motion-Utilities; AbenteuerMode.vue migriert auf --lnc-* Tokens.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/css/style.css
@app/src/components/AbenteuerMode.vue

<interfaces>
<!-- Existing CSS structure in style.css: layout, touch targets, responsive breakpoints, focus indicators, reduced-motion rule -->
<!-- AbenteuerMode.vue has local scoped vars that map to the hybrid palette:
  --rpg-bg: #0d1117      → --lnc-surface-dark
  --rpg-surface: #161b22 → --lnc-surface (new semantic alias)
  --rpg-border: #30363d  → --lnc-border-soft
  --rpg-text: #c9d1d9    → --lnc-text-light
  --rpg-text-muted: #8b949e → --lnc-text-muted (new)
  --rpg-accent: #58a6ff  → --lnc-cyan
  --rpg-success: #3fb950 → --lnc-green
  --rpg-danger: #f85149  → --lnc-danger
  --rpg-gold: #d29922    → --lnc-amber
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-Token-Layer + Dark/Light Scopes + Motion Utilities</name>
  <files>app/css/style.css</files>
  <action>
Add the following sections to style.css BEFORE the existing layout rules:

1. **Token Layer** — `:root` block with all color tokens using the hybrid palette:
   - `--lnc-primary: #0082C9` (NC blue, falls back to `var(--color-primary-element, #0082C9)`)
   - `--lnc-ink: #0F172A`
   - `--lnc-surface-dark: #0D1117`
   - `--lnc-surface-light: #F4F7FB`
   - `--lnc-surface: #161B22` (panel surfaces in dark mode)
   - `--lnc-cyan: #58A6FF`
   - `--lnc-amber: #D29922`
   - `--lnc-magenta: #FF4FD8`
   - `--lnc-danger: #F85149`
   - `--lnc-green: #00E676`
   - `--lnc-text-light: #C9D1D9`
   - `--lnc-text-dark: #1A1A2E`
   - `--lnc-text-muted: #8B949E`
   - `--lnc-border-soft: rgba(255, 255, 255, 0.12)`
   - `--lnc-border-hard: #30363D`
   - `--lnc-shadow-card: 0 12px 32px rgba(15, 23, 42, 0.18)`
   - `--lnc-shadow-glow: 0 0 0 1px rgba(88, 166, 255, 0.18), 0 0 24px rgba(88, 166, 255, 0.16)`
   - Radius tokens: `--lnc-radius-sm: 8px`, `--lnc-radius-md: 14px`, `--lnc-radius-lg: 20px`
   - Spacing tokens: `--lnc-space-xs: 4px`, `--lnc-space-sm: 8px`, `--lnc-space-md: 12px`, `--lnc-space-lg: 16px`, `--lnc-space-xl: 24px`

2. **Dark Mode Scope** — `[data-lnc-theme="dark"]` selector:
   - `--lnc-bg: var(--lnc-surface-dark)`
   - `--lnc-text: var(--lnc-text-light)`
   - `--lnc-text-secondary: var(--lnc-text-muted)`
   - `--lnc-border: var(--lnc-border-soft)`
   - `--lnc-panel: var(--lnc-surface)`
   - `background: var(--lnc-bg); color: var(--lnc-text);`

3. **Light Mode Scope** — `[data-lnc-theme="light"]` selector (also set as `:root` defaults):
   - `--lnc-bg: var(--lnc-surface-light)`
   - `--lnc-text: var(--lnc-text-dark)`
   - `--lnc-text-secondary: #6B7280`
   - `--lnc-border: #E5E7EB`
   - `--lnc-panel: #FFFFFF`
   - `background: var(--lnc-bg); color: var(--lnc-text);`

4. **Motion Utilities** — Three keyframe animations + utility classes:
   ```css
   @keyframes lnc-fade { from { opacity: 0; } to { opacity: 1; } }
   @keyframes lnc-snap-in { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } }
   @keyframes lnc-pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.6; } }

   .lnc-fade { animation: lnc-fade 300ms ease-out both; }
   .lnc-snap-in { animation: lnc-snap-in 200ms ease-out both; }
   .lnc-pulse { animation: lnc-pulse 2s ease-in-out infinite; }
   ```
   The existing `@media (prefers-reduced-motion: reduce)` rule already sets all animation-duration to 0.01ms — this covers these new animations too. Keep the existing rule as-is.

Do NOT modify the existing layout, touch target, responsive, or focus-visible rules — only prepend the new token sections.
  </action>
  <verify>
    <automated>grep -c "\-\-lnc-primary" /home/andre/Workspace/Code/learning-nc/app/css/style.css && grep -c "data-lnc-theme" /home/andre/Workspace/Code/learning-nc/app/css/style.css && grep -c "lnc-fade" /home/andre/Workspace/Code/learning-nc/app/css/style.css</automated>
  </verify>
  <done>style.css contains :root token block with all --lnc-* vars, dark/light scope selectors with data-lnc-theme attribute, and three motion utility classes (fade, snap-in, pulse). Each grep returns at least 1 match.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AbenteuerMode.vue from --rpg-* to --lnc-* tokens</name>
  <files>app/src/components/AbenteuerMode.vue</files>
  <action>
In AbenteuerMode.vue's scoped style section:

1. **Remove the local :root-like variable declarations** (the block defining --rpg-bg, --rpg-surface, etc. around line 1180).

2. **Replace all --rpg-* references** with their --lnc-* equivalents:
   - `var(--rpg-bg)` → `var(--lnc-bg)` (inherits from dark scope)
   - `var(--rpg-surface)` → `var(--lnc-panel)`
   - `var(--rpg-border)` → `var(--lnc-border)`
   - `var(--rpg-text)` → `var(--lnc-text)`
   - `var(--rpg-text-muted)` → `var(--lnc-text-secondary)`
   - `var(--rpg-accent)` → `var(--lnc-cyan)`
   - `var(--rpg-success)` → `var(--lnc-green)`
   - `var(--rpg-danger)` → `var(--lnc-danger)`
   - `var(--rpg-gold)` → `var(--lnc-amber)`

3. **Add data-lnc-theme="dark" attribute** to the root element of the AbenteuerMode template (the outermost div). This ensures the dark scope tokens apply automatically. Look for the root `<div>` in the template and add `data-lnc-theme="dark"`.

4. **Keep hardcoded hex values** in the difficulty/progress badge classes (.ab-diff-beginner etc.) — these are decorative backgrounds that don't need tokenization yet. Only replace the --rpg-* variable references.

5. Do NOT change any template structure, logic, or non-style code.
  </action>
  <verify>
    <automated>grep -c "\-\-rpg-" /home/andre/Workspace/Code/learning-nc/app/src/components/AbenteuerMode.vue | xargs test 0 -eq && echo "PASS: no --rpg- refs remain" || echo "FAIL: --rpg- refs still exist"</automated>
  </verify>
  <done>AbenteuerMode.vue has zero --rpg-* variable references, uses --lnc-* tokens exclusively, and has data-lnc-theme="dark" on its root element. Hardcoded hex values in badge classes are acceptable.</done>
</task>

</tasks>

<verification>
1. `grep -c "\-\-lnc-primary" app/css/style.css` returns >= 1
2. `grep -c "data-lnc-theme" app/css/style.css` returns >= 2 (dark + light)
3. `grep -c "lnc-fade\|lnc-snap-in\|lnc-pulse" app/css/style.css` returns >= 6 (keyframes + classes)
4. `grep -c "\-\-rpg-" app/src/components/AbenteuerMode.vue` returns 0
5. `grep -c "\-\-lnc-" app/src/components/AbenteuerMode.vue` returns >= 10
</verification>

<success_criteria>
- All hybrid palette colors defined as --lnc-* CSS custom properties in style.css
- Dark mode scope (data-lnc-theme="dark") and Light mode scope (data-lnc-theme="light") defined
- Motion utilities (fade, snap-in, pulse) defined as keyframes + utility classes
- prefers-reduced-motion covers all new animations (existing global rule)
- AbenteuerMode.vue fully migrated from --rpg-* to --lnc-* tokens
- AbenteuerMode root element has data-lnc-theme="dark"
</success_criteria>

<output>
After completion, create `.planning/phases/44-design-token-system-narrative-skin/44-01-SUMMARY.md`
</output>
