---
phase: 44-design-token-system-narrative-skin
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - app/css/style.css
  - app/src/components/AbenteuerMode.vue
autonomous: false
requirements:
  - DS-04

must_haves:
  truths:
    - "Der Abenteuer-Modus zeigt den Paper & Circuits Skin — sichtbar durch veraenderte Hintergruende, Borders und Typografie"
    - "Panels haben einen Glas-Effekt mit backdrop-filter und abgerundete Ecken"
    - "Narrative Textbloecke nutzen eine Serif-Schrift (Crimson Pro oder System-Serif), System-Feedback nutzt Sans-Serif"
    - "Der Skin ist nur im Abenteuer-Modus aktiv, nicht in Training/Leitner/Exam"
  artifacts:
    - path: "app/css/style.css"
      provides: "Paper & Circuits skin CSS classes under [data-lnc-skin='paper-circuits']"
      contains: "paper-circuits"
    - path: "app/src/components/AbenteuerMode.vue"
      provides: "data-lnc-skin='paper-circuits' attribute on root element, serif typography"
      contains: "paper-circuits"
  key_links:
    - from: "app/css/style.css"
      to: "app/src/components/AbenteuerMode.vue"
      via: "data-lnc-skin attribute scoping"
      pattern: "data-lnc-skin"
---

<objective>
Paper & Circuits Narrative-Skin fuer den Abenteuer-Modus implementieren — sichtbar durch veraenderte Hintergruende, glasartige Panels, Typografie-Wechsel und narrative Borders.

Purpose: Visuell deutlicher Unterschied zwischen Abenteuer (immersiv, RPG-Atmosphaere) und Standard-Training (clean, funktional). Der Skin nutzt die --lnc-* Tokens aus Plan 01.
Output: CSS-Skin-Klassen in style.css, AbenteuerMode.vue mit aktiviertem Skin.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-design-token-system-narrative-skin/44-01-SUMMARY.md
@.planning/parallel-agencies/gemini-studio/04_UI_SKIN_SYSTEM.md

<interfaces>
<!-- From Plan 01: Token-Layer in style.css -->
<!-- Available tokens:
  --lnc-surface-dark: #0D1117 (rpg-canvas)
  --lnc-surface: #161B22 (rpg-surface)
  --lnc-cyan: #58A6FF (network energy)
  --lnc-amber: #D29922 (rewards, NOVA)
  --lnc-danger: #F85149 (alarm)
  --lnc-text-light: #C9D1D9 (body text)
  --lnc-border-soft: rgba(255,255,255,0.12)
  --lnc-shadow-glow: cyan glow shadow
  --lnc-radius-md: 14px
  --lnc-radius-lg: 20px
-->
<!-- Scoping mechanism: data-lnc-theme="dark" already on AbenteuerMode root -->
<!-- AbenteuerMode key CSS classes: .abenteuer-mode, .ab-scene-*, .ab-dialogue-*, .ab-choice-*, .ab-skill-* -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paper & Circuits Skin CSS + AbenteuerMode Integration</name>
  <files>app/css/style.css, app/src/components/AbenteuerMode.vue</files>
  <action>
**In style.css** — append a new "Narrative Skins" section AFTER the motion utilities, BEFORE the existing layout rules:

1. **Skin scope** using `[data-lnc-skin="paper-circuits"]`:
   - Override `--lnc-panel` to `rgba(22, 27, 34, 0.75)` (glass effect base)
   - Override `--lnc-border` to `rgba(88, 166, 255, 0.15)` (subtle cyan border)
   - Set `--lnc-font-narrative: 'Crimson Pro', Georgia, 'Times New Roman', serif`
   - Set `--lnc-font-system: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif`

2. **Glass panels** — `[data-lnc-skin="paper-circuits"] .ab-scene-card, [data-lnc-skin="paper-circuits"] .ab-dialogue-box`:
   - `background: var(--lnc-panel)`
   - `backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px)`
   - `border: 1px solid var(--lnc-border)`
   - `border-radius: var(--lnc-radius-md)`
   - `box-shadow: var(--lnc-shadow-card)`

3. **Narrative typography** — `[data-lnc-skin="paper-circuits"] .ab-narrative-text, [data-lnc-skin="paper-circuits"] .ab-dialogue-text`:
   - `font-family: var(--lnc-font-narrative)`
   - `font-size: 1.05rem`
   - `line-height: 1.7`
   - `letter-spacing: 0.01em`

4. **System text** — `[data-lnc-skin="paper-circuits"] .ab-system-msg, [data-lnc-skin="paper-circuits"] .ab-skill-label`:
   - `font-family: var(--lnc-font-system)`
   - `font-size: 0.85rem`
   - `text-transform: uppercase`
   - `letter-spacing: 0.08em`
   - `color: var(--lnc-cyan)`

5. **Choice cards glow** — `[data-lnc-skin="paper-circuits"] .ab-choice-btn:hover`:
   - `box-shadow: var(--lnc-shadow-glow)`
   - `border-color: var(--lnc-cyan)`

6. **Circuit line decorators** — `[data-lnc-skin="paper-circuits"] .ab-scene-card::before`:
   - `content: ""`
   - `position: absolute; top: 0; left: 0; right: 0; height: 2px`
   - `background: linear-gradient(90deg, transparent 0%, var(--lnc-cyan) 20%, var(--lnc-amber) 50%, var(--lnc-cyan) 80%, transparent 100%)`
   - `opacity: 0.4`
   - Ensure `.ab-scene-card` has `position: relative; overflow: hidden`

7. **Pulsing continue indicator** — `[data-lnc-skin="paper-circuits"] .ab-continue-indicator`:
   - `color: var(--lnc-amber)`
   - Add class `lnc-pulse` (from motion utilities)

**In AbenteuerMode.vue** — template change only:

1. Add `data-lnc-skin="paper-circuits"` to the root element (same div that already has `data-lnc-theme="dark"` from Plan 01). The root div should now have both attributes.

2. If the AbenteuerMode doesn't have CSS classes matching the selectors above (e.g., `.ab-narrative-text`, `.ab-dialogue-text`, `.ab-dialogue-box`), check the actual class names used in the template and adjust the CSS selectors in style.css accordingly. The key classes to look for:
   - Scene container → use actual class for glass panel
   - Dialogue/narrative text → use actual class for serif font
   - Choice buttons → use actual class for hover glow
   - Scene wrapper → use actual class for circuit line decorator

Do NOT add Google Fonts imports — use font-stack fallback (Georgia/Times for serif). Crimson Pro is aspirational; if a user has it installed it will be used, otherwise Georgia kicks in.
  </action>
  <verify>
    <automated>grep -c "paper-circuits" /home/andre/Workspace/Code/learning-nc/app/css/style.css && grep -c "paper-circuits" /home/andre/Workspace/Code/learning-nc/app/src/components/AbenteuerMode.vue && grep -c "backdrop-filter" /home/andre/Workspace/Code/learning-nc/app/css/style.css</automated>
  </verify>
  <done>style.css has Paper & Circuits skin rules scoped to [data-lnc-skin="paper-circuits"]. AbenteuerMode.vue root element has data-lnc-skin="paper-circuits" attribute. Glass panels use backdrop-filter, narrative text uses serif font-stack, circuit line decorator visible on scene cards.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual Verification — Paper & Circuits Skin</name>
  <files>app/css/style.css, app/src/components/AbenteuerMode.vue</files>
  <action>
Deploy to learning-dev and verify visually:
1. Run `./scripts/deploy-dev.sh` to deploy CSS + JS changes
2. Open http://192.168.178.65:8080, log in, navigate to a pool, start Abenteuer-Modus
3. Verify dark background, glass panels, serif narrative text, circuit line decorators, cyan glow on choice hover
4. Switch to Training-Modus and verify standard light look is unchanged
  </action>
  <verify>
    <automated>ssh learning-dev 'docker exec learning-app grep -c "paper-circuits" /var/www/html/custom_apps/learning/css/style.css'</automated>
  </verify>
  <done>Visual confirmation that Abenteuer-Modus shows Paper & Circuits skin and Training-Modus is unaffected.</done>
</task>

</tasks>

<verification>
1. `grep -c "paper-circuits" app/css/style.css` returns >= 5 (multiple skin rules)
2. `grep "data-lnc-skin" app/src/components/AbenteuerMode.vue` shows "paper-circuits"
3. `grep -c "backdrop-filter" app/css/style.css` returns >= 1
4. `grep -c "font-narrative\|Crimson" app/css/style.css` returns >= 1
5. Visual verification: Abenteuer-Modus looks distinctly different from Training-Modus
</verification>

<success_criteria>
- Paper & Circuits skin is visually active in Abenteuer-Modus only
- Glass panels with backdrop-filter blur visible on scene cards and dialogue boxes
- Narrative text uses serif font-stack, system text uses sans-serif with cyan color
- Circuit line decorator (gradient line) visible on scene cards
- Training/Leitner/Exam modes are completely unaffected
- All skin rules scoped under [data-lnc-skin="paper-circuits"] — zero leakage
</success_criteria>

<output>
After completion, create `.planning/phases/44-design-token-system-narrative-skin/44-02-SUMMARY.md`
</output>
