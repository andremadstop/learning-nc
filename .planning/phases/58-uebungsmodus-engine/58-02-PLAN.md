---
phase: 58-uebungsmodus-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 58-01
files_modified:
  - app/src/components/SubnetCalculator.vue
autonomous: false
requirements:
  - UEB-01
  - UEB-02
  - UEB-03
  - UEB-04

must_haves:
  truths:
    - "User sees a 5th tab 'Uebung' in the SubnetCalculator"
    - "User can start practice mode and sees a random scenario with question text"
    - "User types answers into labeled input fields for each expected answer field"
    - "After submit, each field shows green checkmark or red X with correct answer"
    - "Progress tracker shows X/Y correct and current streak at all times"
    - "User can click 'Naechste Aufgabe' to get next scenario"
  artifacts:
    - path: "app/src/components/SubnetCalculator.vue"
      provides: "Practice mode tab with scenario display, answer inputs, feedback, progress tracker"
      contains: "isPracticeTab"
  key_links:
    - from: "app/src/components/SubnetCalculator.vue"
      to: "app/src/utils/practiceEngine.js"
      via: "import and use createPracticeSession, nextScenario, checkAnswers, submitAnswer, getProgress"
      pattern: "import.*practiceEngine"
---

<objective>
Integrate the practiceEngine into SubnetCalculator.vue as a 5th "Uebung" tab. The user sees a scenario, types answers, submits, gets per-field feedback, and tracks progress.

Purpose: Connects the tested engine logic to the existing calculator UI, completing the interactive practice mode.
Output: Working practice tab in SubnetCalculator with all 4 requirement behaviors (UEB-01 through UEB-04).
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-uebungsmodus-engine/58-01-SUMMARY.md
@app/src/components/SubnetCalculator.vue
@app/src/utils/practiceEngine.js

<interfaces>
<!-- From practiceEngine.js (created in Plan 01) -->
```javascript
export function createPracticeSession(scenarios?: Scenario[]): Session
export function nextScenario(session: Session): Scenario | null
export function checkAnswers(expected, userAnswers): { field, correct, expected, given }[]
export function submitAnswer(session, fieldResults): void
export function getProgress(session): { correct, total, streak, maxStreak, remaining }
export function normalizeAnswer(raw: string): string
export const DEMO_SCENARIOS: Scenario[]
```

<!-- From SubnetCalculator.vue (existing tabs pattern) -->
```javascript
// tabs computed returns array of {id, label}
// activeTab data property controls which tab is shown
// switchTab(id) method changes tab
// Computed: isCalculatorTab, isBinaryTab, isVlsmTab, isIpv6Tab
// Each tab section: <section v-if="isXxxTab" class="subnet-panel" role="tabpanel">
```

Field label mapping (German):
- networkAddress: "Netzadresse"
- broadcast: "Broadcast"
- cidr: "CIDR-Prefix"
- hostCount: "Anzahl Hosts"
- subnetMask: "Subnetzmaske"
- firstHost: "Erster Host"
- lastHost: "Letzter Host"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add practice tab and wire engine into SubnetCalculator.vue</name>
  <files>app/src/components/SubnetCalculator.vue</files>
  <action>
    Add the practice mode integration to SubnetCalculator.vue:

    1. **Import** practiceEngine functions at top of script:
       `import { createPracticeSession, nextScenario, checkAnswers, submitAnswer, getProgress, DEMO_SCENARIOS } from '../utils/practiceEngine.js'`

    2. **Add tab** to the `tabs()` computed — append: `{ id: 'practice', label: t('learning', 'Uebung') }`

    3. **Add computed** `isPracticeTab() { return this.activeTab === 'practice' }`

    4. **Add data properties**:
       - `practiceSession: null` — the session object
       - `practiceUserAnswers: {}` — current user input keyed by field name
       - `practiceResults: null` — array from checkAnswers after submit (null = not yet submitted)
       - `practiceStarted: false` — whether practice mode is active

    5. **Add computed** `practiceProgress()`:
       - If no session, return null
       - Return `getProgress(this.practiceSession)`

    6. **Add computed** `practiceFieldLabels()`:
       Return map: `{ networkAddress: t('learning', 'Netzadresse'), broadcast: t('learning', 'Broadcast'), cidr: t('learning', 'CIDR-Prefix'), hostCount: t('learning', 'Anzahl Hosts'), subnetMask: t('learning', 'Subnetzmaske'), firstHost: t('learning', 'Erster Host'), lastHost: t('learning', 'Letzter Host') }`

    7. **Add methods**:
       - `startPractice()`: Create session via `createPracticeSession()`, set practiceStarted=true, call `loadNextScenario()`
       - `loadNextScenario()`: Call `nextScenario(this.practiceSession)`, reset practiceUserAnswers to empty object with keys from current scenario's expectedAnswers, set practiceResults to null
       - `submitPracticeAnswer()`: Call `checkAnswers(session.current.expectedAnswers, practiceUserAnswers)`, store in practiceResults, call `submitAnswer(session, practiceResults)`
       - `resetPractice()`: Set practiceSession=null, practiceStarted=false, practiceResults=null, practiceUserAnswers={}

    8. **Add template section** after the IPv6 tab section (`v-if="isPracticeTab"`):

       ```html
       <section v-if="isPracticeTab" class="subnet-panel" role="tabpanel">
         <!-- Not started state -->
         <div v-if="!practiceStarted" class="practice-start">
           <p class="subnet-help">{{ t('learning', 'Teste dein Wissen mit zufaelligen Subnetting-Aufgaben. Du bekommst sofort Feedback zu jeder Antwort.') }}</p>
           <button class="subnet-button subnet-button--primary" @click="startPractice">
             {{ t('learning', 'Uebung starten') }}
           </button>
         </div>

         <!-- Active practice -->
         <div v-else-if="practiceSession && practiceSession.current">
           <!-- Progress bar -->
           <div class="practice-progress">
             <span class="practice-progress__score">
               {{ t('learning', '{correct} von {total} richtig', { correct: practiceProgress.correct, total: practiceProgress.total }) }}
             </span>
             <span v-if="practiceProgress.streak > 0" class="practice-progress__streak">
               {{ t('learning', 'Serie: {streak}', { streak: practiceProgress.streak }) }}
             </span>
             <span class="practice-progress__remaining">
               {{ t('learning', 'Noch {n} Aufgaben', { n: practiceProgress.remaining }) }}
             </span>
           </div>

           <!-- Difficulty badge -->
           <span class="practice-difficulty" :class="'practice-difficulty--' + practiceSession.current.difficulty">
             {{ practiceSession.current.difficulty === 'easy' ? t('learning', 'Leicht') : practiceSession.current.difficulty === 'medium' ? t('learning', 'Mittel') : t('learning', 'Schwer') }}
           </span>

           <!-- Question -->
           <div class="practice-question">
             <p class="practice-question__text">{{ practiceSession.current.question }}</p>
             <p v-if="practiceSession.current.context" class="practice-question__context">{{ practiceSession.current.context }}</p>
           </div>

           <!-- Answer fields -->
           <div class="practice-answers">
             <div v-for="(expected, field) in practiceSession.current.expectedAnswers" :key="field" class="practice-field">
               <label class="subnet-label" :for="'practice-' + field">{{ practiceFieldLabels[field] || field }}</label>
               <div class="practice-field__input-row">
                 <input
                   :id="'practice-' + field"
                   v-model="practiceUserAnswers[field]"
                   class="subnet-input"
                   :class="{
                     'practice-field--correct': practiceResults && practiceResults.find(r => r.field === field && r.correct),
                     'practice-field--wrong': practiceResults && practiceResults.find(r => r.field === field && !r.correct),
                   }"
                   type="text"
                   :disabled="practiceResults !== null"
                   @keyup.enter="practiceResults === null && submitPracticeAnswer()">
                 <!-- Feedback icons -->
                 <span v-if="practiceResults && practiceResults.find(r => r.field === field && r.correct)" class="practice-field__icon practice-field__icon--correct" aria-label="Richtig">&#10003;</span>
                 <span v-if="practiceResults && practiceResults.find(r => r.field === field && !r.correct)" class="practice-field__icon practice-field__icon--wrong" aria-label="Falsch">&#10007;</span>
               </div>
               <!-- Show correct answer on wrong -->
               <p v-if="practiceResults && practiceResults.find(r => r.field === field && !r.correct)" class="practice-field__correction">
                 {{ t('learning', 'Richtige Antwort: {answer}', { answer: practiceResults.find(r => r.field === field).expected }) }}
               </p>
             </div>
           </div>

           <!-- Action buttons -->
           <div class="practice-actions">
             <button
               v-if="practiceResults === null"
               class="subnet-button subnet-button--primary"
               @click="submitPracticeAnswer">
               {{ t('learning', 'Pruefen') }}
             </button>
             <button
               v-else-if="practiceProgress.remaining > 0"
               class="subnet-button subnet-button--primary"
               @click="loadNextScenario">
               {{ t('learning', 'Naechste Aufgabe') }}
             </button>
             <button
               v-else
               class="subnet-button subnet-button--secondary"
               @click="resetPractice">
               {{ t('learning', 'Nochmal starten') }}
             </button>
           </div>
         </div>

         <!-- All done -->
         <div v-else-if="practiceStarted" class="practice-done">
           <p class="practice-done__text">
             {{ t('learning', 'Alle Aufgaben bearbeitet! {correct} von {total} richtig.', { correct: practiceProgress.correct, total: practiceProgress.total }) }}
           </p>
           <p v-if="practiceProgress.maxStreak > 1" class="practice-done__streak">
             {{ t('learning', 'Beste Serie: {streak} in Folge', { streak: practiceProgress.maxStreak }) }}
           </p>
           <button class="subnet-button subnet-button--primary" @click="resetPractice">
             {{ t('learning', 'Nochmal starten') }}
           </button>
         </div>
       </section>
       ```

    9. **Add CSS** at end of `<style scoped>` section. Use existing design tokens (--lnc-* variables). Key classes:
       - `.practice-progress` — flex row, gap 1rem, font-size 0.9rem, padding 0.75rem, background var(--lnc-surface-raised, #f5f5f5), border-radius 8px, margin-bottom 1rem
       - `.practice-progress__streak` — color var(--lnc-accent, #f59e0b), font-weight 600
       - `.practice-difficulty` — inline badge, padding 2px 8px, border-radius 4px, font-size 0.75rem, font-weight 600, text-transform uppercase
       - `.practice-difficulty--easy` — background #dcfce7, color #166534
       - `.practice-difficulty--medium` — background #fef3c7, color #92400e
       - `.practice-difficulty--hard` — background #fecaca, color #991b1b
       - `.practice-question__text` — font-size 1.1rem, margin 1rem 0 0.5rem
       - `.practice-question__context` — font-size 0.9rem, color var(--lnc-text-secondary), font-style italic, margin-bottom 1rem
       - `.practice-field` — margin-bottom 0.75rem
       - `.practice-field__input-row` — display flex, align-items center, gap 0.5rem
       - `.practice-field--correct` — border-color #16a34a, background #f0fdf4
       - `.practice-field--wrong` — border-color #dc2626, background #fef2f2
       - `.practice-field__icon` — font-size 1.2rem, flex-shrink 0
       - `.practice-field__icon--correct` — color #16a34a
       - `.practice-field__icon--wrong` — color #dc2626
       - `.practice-field__correction` — color #dc2626, font-size 0.85rem, margin-top 0.25rem
       - `.practice-actions` — margin-top 1.5rem, display flex, gap 0.75rem
       - `.practice-start`, `.practice-done` — text-align center, padding 2rem

    Important: Use `this.$set` for reactive property updates on practiceUserAnswers (Vue 2.7 reactivity caveat — plain object key assignment is not reactive). In `loadNextScenario`, build the answers object with all keys initialized to '' BEFORE assigning to `this.practiceUserAnswers`.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npx vitest run tests/unit/practiceEngine.test.js && npm run build</automated>
  </verify>
  <done>SubnetCalculator.vue has a 5th "Uebung" tab, imports practiceEngine, renders scenario/inputs/feedback/progress. Build succeeds (npm run build).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify practice mode in browser</name>
  <what-built>
    Practice mode tab in SubnetCalculator with:
    - "Uebung starten" button that loads a random scenario
    - Input fields for each expected answer
    - "Pruefen" button that shows green/red per field + correct answer on wrong
    - Progress tracker (X/Y correct, streak)
    - "Naechste Aufgabe" to continue
    - "Nochmal starten" when all scenarios done
  </what-built>
  <how-to-verify>
    1. Deploy to learning-dev: `cd /home/andre/Workspace/Code/learning-nc && ./scripts/deploy-dev.sh --js-only`
    2. Open http://192.168.178.65:8080 in browser, navigate to Subnetzrechner
    3. Verify 5th tab "Uebung" is visible
    4. Click "Uebung" tab, then "Uebung starten"
    5. Verify: scenario question text is shown with difficulty badge
    6. Type correct answers in the fields, click "Pruefen"
    7. Verify: green checkmarks appear on correct fields
    8. Click "Naechste Aufgabe", type a WRONG answer, click "Pruefen"
    9. Verify: red X appears with correct answer shown below
    10. Verify: progress tracker updates (e.g., "1 von 2 richtig", streak resets on wrong)
    11. Complete all scenarios, verify "Nochmal starten" appears with final score
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# Engine unit tests
cd /home/andre/Workspace/Code/learning-nc/app && npx vitest run tests/unit/practiceEngine.test.js

# ESLint (Gate 1)
cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .js,.vue src/utils/practiceEngine.js src/components/SubnetCalculator.vue

# Build succeeds
cd /home/andre/Workspace/Code/learning-nc/app && npm run build
```
</verification>

<success_criteria>
- 5th tab "Uebung" visible in SubnetCalculator
- Clicking "Uebung starten" shows a random scenario with question text and difficulty badge
- Input fields appear for each expected answer field (labeled in German)
- "Pruefen" checks all fields: green checkmark on correct, red X + correct answer on wrong
- Progress tracker shows "X von Y richtig" and "Serie: N" (streak)
- "Naechste Aufgabe" loads next scenario, "Nochmal starten" resets session
- ESLint 0 errors, build succeeds, engine tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/58-uebungsmodus-engine/58-02-SUMMARY.md`
</output>
