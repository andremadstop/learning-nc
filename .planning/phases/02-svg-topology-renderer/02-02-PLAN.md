---
phase: 02-svg-topology-renderer
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/src/components/PbqPlacement.vue
  - app/src/components/PbqRenderer.vue
autonomous: false
requirements:
  - SVG-03
  - SVG-04

must_haves:
  truths:
    - "PbqPlacement renders NetworkTopologySvg when topologyConfig prop is provided"
    - "PbqPlacement renders <img> fallback when only scenarioImage is provided (unchanged behavior)"
    - "PbqPlacement renders fallback grid when neither topologyConfig nor scenarioImage is set (unchanged behavior)"
    - "Hotspot overlay <div> elements are NOT rendered in topology mode (only in image mode)"
    - "Clicking a topology node opens the device picker (openPicker called with node id)"
    - "PbqRenderer passes config.topology as :topology-config to PbqPlacement"
    - "getNodeScreenPosition can be called via $refs.topologySvg from PbqPlacement"
    - "No v-html in any modified file"
  artifacts:
    - path: "app/src/components/PbqPlacement.vue"
      provides: "Placement component with topologyConfig prop, NetworkTopologySvg integration"
      contains: "topologyConfig"
    - path: "app/src/components/PbqRenderer.vue"
      provides: "Passes config.topology to PbqPlacement as topology-config prop"
      contains: "topology-config"
  key_links:
    - from: "app/src/components/PbqRenderer.vue"
      to: "app/src/components/PbqPlacement.vue"
      via: ":topology-config='config.topology || null'"
      pattern: "topology-config"
    - from: "app/src/components/PbqPlacement.vue"
      to: "app/src/components/NetworkTopologySvg.vue"
      via: "v-if='topologyConfig' ref='topologySvg' @node-click='openPicker'"
      pattern: "NetworkTopologySvg"
    - from: "PbqPlacement openPicker"
      to: "NetworkTopologySvg node-click event"
      via: "@node-click handler receives node.id, calls openPicker(nodeId)"
      pattern: "openPicker"
---

<objective>
Integrate NetworkTopologySvg.vue into PbqPlacement.vue and wire PbqRenderer.vue to pass topology config through.

Purpose: Makes the SVG renderer usable in the actual PBQ question flow — closes SVG-03 (coordinate mapping exposed) and SVG-04 (PbqPlacement uses SVG topology as background).
Output: Modified PbqPlacement.vue (new prop + conditional rendering), modified PbqRenderer.vue (passes topology-config), manual verification on learning-dev.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-svg-topology-renderer/02-RESEARCH.md
@.planning/phases/02-svg-topology-renderer/02-VALIDATION.md
@.planning/phases/02-svg-topology-renderer/02-01-SUMMARY.md

<interfaces>
<!-- Current PbqPlacement.vue props (from codebase) -->
props: {
  config:        { type: Object, required: true },    // pbq_config with positions[], device_options[]
  value:         { type: Object, default: () => ({}) },
  disabled:      { type: Boolean, default: false },
  scenarioImage: { type: String, default: null },
}

<!-- Current PbqPlacement.vue template structure -->
<div class="pbq-diagram-wrapper">
  <img v-if="scenarioImage" :src="scenarioImage" class="pbq-diagram-img" alt="Network diagram" />
  <div v-else class="pbq-topology-grid">  <!-- fallback grid -->
    <div v-for="pos in config.positions" ...>
  </div>
  <!-- Hotspots: only shown when scenarioImage exists -->
  <div v-if="scenarioImage" v-for="pos in config.positions" :key="'hs-' + pos.id" ...>
</div>

<!-- Current PbqRenderer.vue — how PbqPlacement is mounted -->
<PbqPlacement
  v-else-if="subtype === 'placement'"
  :config="config"
  :value="localAnswer"
  :disabled="disabled"
  :scenario-image="configImage"
  @update="onUpdate"
/>
computed: {
  configImage() { return this.config.scenario_image || null },
  scenarioImageUrl() { ... }
}

<!-- Node-link topology schema (contract for topologyConfig prop) -->
{
  "nodes": [{ "id": "r1", "type": "router", "label": "Core Router", "x": 200, "y": 100 }],
  "links": [{ "from": "r1", "to": "sw1" }]
}
<!-- Note: config.positions[].id must match topology nodes[].id for picker mapping -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add topologyConfig prop and NetworkTopologySvg to PbqPlacement.vue</name>
  <files>app/src/components/PbqPlacement.vue</files>
  <action>
    Modify app/src/components/PbqPlacement.vue:

    1. Add import at top of <script>:
       ```javascript
       import NetworkTopologySvg from './NetworkTopologySvg.vue'
       ```

    2. Add to components option:
       ```javascript
       components: { NetworkTopologySvg },
       ```

    3. Add topologyConfig prop (after scenarioImage):
       ```javascript
       topologyConfig: { type: Object, default: null },
       ```

    4. Replace the diagram section in <template> — the current structure is:
       ```html
       <img v-if="scenarioImage" ... />
       <div v-else class="pbq-topology-grid">...</div>
       <div v-if="scenarioImage" v-for="pos in config.positions" ...hotspots...>
       ```

       Replace with:
       ```html
       <!-- SVG topology mode: takes priority over image -->
       <NetworkTopologySvg
         v-if="topologyConfig"
         ref="topologySvg"
         :topology="topologyConfig"
         :disabled="disabled"
         @node-click="openPicker"
       />
       <!-- Image mode: existing behavior unchanged -->
       <img v-else-if="scenarioImage" :src="scenarioImage" class="pbq-diagram-img" alt="Network diagram" />
       <!-- Fallback grid: existing behavior unchanged -->
       <div v-else class="pbq-topology-grid">
         <div
           v-for="pos in config.positions"
           :key="pos.id"
           class="pbq-topology-node"
           :class="{ 'pbq-topology-node--assigned': !!value[pos.id] }"
           @click="!disabled && openPicker(pos.id)"
         >
           <div class="pbq-topology-icon">{{ value[pos.id] ? '✓' : '?' }}</div>
           <div class="pbq-topology-label">{{ pos.label }}</div>
           <div v-if="value[pos.id]" class="pbq-topology-assigned">{{ value[pos.id] }}</div>
         </div>
       </div>
       <!-- Hotspot overlays: only in image mode, not SVG topology mode -->
       <div
         v-if="scenarioImage && !topologyConfig"
         v-for="pos in config.positions"
         :key="'hs-' + pos.id"
         class="pbq-hotspot"
         :style="{ left: pos.x_pct + '%', top: pos.y_pct + '%' }"
         :class="{ 'pbq-hotspot--assigned': !!value[pos.id] }"
         :title="pos.label"
         @click="!disabled && openPicker(pos.id)"
       >
         <span v-if="!value[pos.id]">?</span>
         <span v-else class="pbq-hotspot-label">{{ value[pos.id].substring(0,4) }}</span>
       </div>
       ```

    CRITICAL CONSTRAINT: openPicker(posId) already exists and works — DO NOT change its signature. When called from @node-click, the node id from the SVG event is the posId. The schema contract is: topology.nodes[].id == config.positions[].id.

    ANTI-PATTERN GUARD: No v-html anywhere. The SVG is rendered by NetworkTopologySvg.vue via native Vue template elements.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run test 2>&1 | tail -10 && grep -n 'v-html' app/src/components/PbqPlacement.vue && echo "v-html check done (empty=good)"</automated>
  </verify>
  <done>PbqPlacement.vue has topologyConfig prop. Template has v-if="topologyConfig" NetworkTopologySvg block. Hotspot divs have v-if="scenarioImage && !topologyConfig". No v-html in file. npm run test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PbqRenderer.vue to pass topology config + deploy to learning-dev</name>
  <files>app/src/components/PbqRenderer.vue</files>
  <action>
    Modify app/src/components/PbqRenderer.vue:

    1. Add computed property (after existing configImage):
       ```javascript
       topologyConfig() { return this.config.topology || null },
       ```

    2. Add :topology-config prop to <PbqPlacement> element:
       ```html
       <PbqPlacement
         v-else-if="subtype === 'placement'"
         :config="config"
         :value="localAnswer"
         :disabled="disabled"
         :scenario-image="configImage"
         :topology-config="topologyConfig"
         @update="onUpdate"
       />
       ```

    Then deploy to learning-dev:
    ```bash
    rsync -avz --exclude node_modules --exclude .git /home/andre/Workspace/Code/learning-nc/app/ learning-dev:~/learning-nc/app/
    ssh learning-dev 'cd ~/learning-nc/app && npm run build'
    ssh learning-dev 'cd ~/learning-nc/app && tar cf /tmp/js-bundle.tar js/'
    ssh learning-dev 'docker cp /tmp/js-bundle.tar learning-app:/tmp/'
    ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && tar xf /tmp/js-bundle.tar"'
    ```

    After deploy: verify build succeeded (no webpack errors in build output).
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run test 2>&1 | tail -5 && grep -n 'v-html' app/src/components/PbqRenderer.vue && echo "v-html check done (empty=good)"</automated>
  </verify>
  <done>PbqRenderer.vue has topologyConfig computed property and :topology-config="topologyConfig" on PbqPlacement. Build succeeds on learning-dev (no webpack errors). npm run test green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify SVG topology renders and interacts correctly on learning-dev</name>
  <action>
    Human verification on learning-dev (http://learning-dev:8080). Use the test topology config below in a PBQ placement question's pbq_config field to trigger SVG mode. Verify all 7 acceptance points.

    Test pbq_config:
    ```json
    {
      "subtype": "placement",
      "topology": {
        "nodes": [
          { "id": "pos1", "type": "router",      "label": "Core Router", "x": 200, "y": 100 },
          { "id": "pos2", "type": "switch",      "label": "SW-1",        "x": 100, "y": 250 },
          { "id": "pos3", "type": "firewall",    "label": "Firewall",    "x": 300, "y": 250 },
          { "id": "pos4", "type": "workstation", "label": "PC-01",       "x": 100, "y": 400 },
          { "id": "pos5", "type": "server",      "label": "Server",      "x": 300, "y": 400 }
        ],
        "links": [
          { "from": "pos1", "to": "pos2" },
          { "from": "pos1", "to": "pos3" },
          { "from": "pos2", "to": "pos4" },
          { "from": "pos3", "to": "pos5" }
        ]
      },
      "positions": [
        { "id": "pos1", "label": "Core Router" },
        { "id": "pos2", "label": "SW-1" },
        { "id": "pos3", "label": "Firewall" },
        { "id": "pos4", "label": "PC-01" },
        { "id": "pos5", "label": "Server" }
      ],
      "device_options": ["Router", "Switch", "Firewall", "Server", "Workstation"]
    }
    ```
  </action>
  <how-to-verify>
    1. Open http://learning-dev:8080 — navigate to a PBQ placement question with the above topology config
    2. SVG-04 check: Topology renders as SVG diagram (not fallback grid, not image) — nodes connected by lines
    3. SVG-02 check: All 5 node types show recognizable icons (router=circle+arrows, switch=rectangle, firewall=shield, workstation=monitor, server=rack)
    4. Click a node — device picker opens (SVG-04 interaction)
    5. SVG-03 check: Picker title shows the correct node label
    6. Backward compat: Open a placement question with scenarioImage (no topology) — image + hotspots still work (no regression)
    7. No console errors about v-html, CSP violations, or Vue warnings
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
  <done>All 7 acceptance points verified by human on learning-dev.</done>
</task>

</tasks>

<verification>
After checkpoint approval:
1. `grep -rn 'v-html' app/src/components/PbqPlacement.vue app/src/components/PbqRenderer.vue` — must return empty
2. `grep -n 'topologyConfig\|topology-config' app/src/components/PbqPlacement.vue` — shows prop + v-if block
3. `grep -n 'topologyConfig\|topology-config' app/src/components/PbqRenderer.vue` — shows computed + binding
4. `cd app && npm run test` — green
</verification>

<success_criteria>
- PbqPlacement.vue has topologyConfig prop; renders NetworkTopologySvg when set; image/grid fallbacks unchanged
- PbqRenderer.vue passes config.topology as topology-config to PbqPlacement
- All 5 topology node types display recognizable SVG icons in the browser
- Node click triggers device picker (openPicker receives correct node id)
- No regression in image-mode placement questions
- No v-html in any modified file
- Human verification approved on learning-dev
</success_criteria>

<output>
After completion, create `.planning/phases/02-svg-topology-renderer/02-02-SUMMARY.md`
</output>
