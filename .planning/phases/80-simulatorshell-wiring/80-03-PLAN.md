---
phase: 80-simulatorshell-wiring
plan: 03
type: execute
wave: 2
depends_on:
  - 80-01
  - 80-02
files_modified:
  - app/src/components/AbenteuerMode.vue
  - app/tests/unit/abenteuerModeGraph.test.js
autonomous: false
requirements:
  - SIM-01
  - SIM-02

must_haves:
  truths:
    - "Eine Kampagnen-Szene mit simulator.type='firewall' zeigt den Firewall-Simulator eingebettet an"
    - "Nach Loesen einer Simulator-Challenge wird graph-traverse aufgerufen mit simulator_passed/score/result"
    - "pass_flag wird nach Bestehen durch den Backend-Response im stateBag gesetzt"
    - "stateBag-Update erzeugt neue Vue 2-reaktive Referenz (kein Object.assign)"
    - "Graph-Mode startet ueber /graph-start statt /start fuer is_graph-Kampagnen"
    - "User kann Simulator-Challenge erneut versuchen wenn nicht bestanden"
  artifacts:
    - path: "app/src/components/AbenteuerMode.vue"
      provides: "Graph-Mode-Routing, SimulatorShell-Integration, graph-traverse Handler"
      contains: "isGraphMode"
    - path: "app/tests/unit/abenteuerModeGraph.test.js"
      provides: "Unit-Tests fuer onSimulatorComplete stateBag-Reaktivitaet"
  key_links:
    - from: "AbenteuerMode (startCampaign)"
      to: "/api/story/campaigns/{id}/graph-start"
      via: "axios.post wenn campaign.is_graph === true"
      pattern: "graph-start"
    - from: "AbenteuerMode (onSimulatorComplete)"
      to: "/api/story/campaigns/{id}/graph-traverse"
      via: "axios.post mit simulator_passed, simulator_score, simulator_result"
      pattern: "graph-traverse"
    - from: "AbenteuerMode (simulation phase)"
      to: "SimulatorShell.vue"
      via: "<SimulatorShell :type='...' :scenarioId='...' @complete='onSimulatorComplete'>"
      pattern: "SimulatorShell"
---

<objective>
Verdrahtet den Graph-Mode in AbenteuerMode.vue: die Kampagnen-Engine kann jetzt ueber
`/graph-start`, `/graph-scene` und `/graph-traverse` mit dem Backend kommunizieren,
und der bestehende `simulation`-Phase-Stub wird durch echtes SimulatorShell-Embedding ersetzt.

Das ist der groesste Scope-Anteil von Phase 80. AbenteuerMode ist 1303 Zeilen gross —
alle Aenderungen sind additive (neue Felder in data(), neue Methods, erweiterter simulation-
Phase-Template-Block). Kein vorhandener linearer Pfad wird entfernt.

Purpose: Spielbare Graph-Kampagnen mit echten Simulator-Challenges.
Output: AbenteuerMode.vue mit isGraphMode, stateBag, SimulatorShell-Integration.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-simulatorshell-wiring/80-RESEARCH.md
@.planning/phases/80-simulatorshell-wiring/80-01-SUMMARY.md
@.planning/phases/80-simulatorshell-wiring/80-02-SUMMARY.md

<interfaces>
<!-- SimulatorShell Props (aus Plan 80-02): -->
```javascript
// app/src/components/SimulatorShell.vue
props: {
  type:             String,   // required — 'firewall'|'dns'|'routing'|'nat'|'portscan'|'wireshark'|'authflow'
  scenarioId:       String,   // default: ''
  scenarioOverride: Object,   // default: null — inline-Szenario aus Campaign JSON
  nodeId:           String,   // default: '' — :key fuer frischen Mount
}
// emits: @complete(passed: Boolean, score: Number, result: Object)
```

<!-- Backend-Endpunkte (aus StoryController.php, verifiziert): -->
<!-- POST /apps/learning/api/story/campaigns/{id}/graph-start -->
<!--   Response: { state: { graphPosition, stateBag, characterClass, score }, node, available_edges, timer, simulator } -->
<!-- GET  /apps/learning/api/story/campaigns/{id}/graph-scene -->
<!--   Response: wie graph-start -->
<!-- POST /apps/learning/api/story/campaigns/{id}/graph-traverse -->
<!--   Body: { edgeId, simulator_passed?, simulator_score?, simulator_result? } -->
<!--   Response: wie graph-start -->
<!-- GET  /apps/learning/api/story/campaigns/{id}/graph-state -->
<!--   Response: { state: { stateBag, ... } } -->

<!-- stateBag-Struktur (aus CampaignState.php): -->
```javascript
stateBag: {
  flags: {},          // { flag_name: true/false }
  items: [],          // gesammelte Items
  reputation: {},     // { domain: number }
  timers: {},         // { nodeId: { started_at, deadline_at } }
  simulators: {},     // { nodeId: { completed, passed, type, scenario, pass_flag } }
  bot_corrections: {} // { nodeId: { completed, passed } }
}
```

<!-- STATIC_CAMPAIGNS in AbenteuerMode.vue: -->
<!-- Ist als data()-Property oder const in der Datei — muss um is_graph: true erweitert werden -->
<!-- fuer 'grosser_ausfall' Kampagne (sobald sie existiert). -->
<!-- Fuer den Test-Graph (test_graph_campaign.json): is_graph: true setzen. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AbenteuerMode — data(), imports und Graph-Mode-Start verdrahten</name>
  <files>app/src/components/AbenteuerMode.vue</files>
  <action>
    Lese AbenteuerMode.vue vollstaendig. Es ist 1303 Zeilen — lies es komplett bevor du aenderst.

    **Schritt 1 — SimulatorShell importieren:**
    Am Anfang des `<script>`-Blocks, bei den anderen Component-Imports:
    ```javascript
    import SimulatorShell from './SimulatorShell.vue'
    ```
    Im `components`-Objekt registrieren: `SimulatorShell,`

    **Schritt 2 — data() erweitern:**
    Folgende Felder in `data()` hinzufuegen (nach den bestehenden Feldern, NICHT vorhandene entfernen):
    ```javascript
    // Graph-Mode State
    isGraphMode: false,
    stateBag: {},               // WICHTIG: immer als neue Referenz ersetzen, nie mutieren
    currentGraphNode: null,
    graphAvailableEdges: [],
    currentGraphSimulator: null,
    ```

    **Schritt 3 — is_graph Flag in STATIC_CAMPAIGNS:**
    Finde die STATIC_CAMPAIGNS-Definition (als const oder in data()). Suche den Eintrag
    mit id 'test_graph_campaign' oder 'grosser_ausfall' und fuege `is_graph: true` hinzu.
    Falls keine Graph-Kampagne in der Liste ist, fuege einen Platzhalter-Eintrag hinzu:
    ```javascript
    { id: 'test_graph_campaign', title: 'Test Graph Kampagne', is_graph: true, ... }
    ```
    (Wird in Phase 83 mit echtem Content befuellt.)

    **Schritt 4 — startCampaign() / loadCampaign() erweitern:**
    Finde die Methode die beim Kampagnen-Start aufgerufen wird (wahrscheinlich `startCampaign()`
    oder `selectCampaign()`). Erweitere sie:
    ```javascript
    async startCampaign(campaign) {
      this.selectedCampaign = campaign

      if (campaign.is_graph) {
        this.isGraphMode = true
        try {
          const resp = await axios.post(
            generateUrl(`/apps/learning/api/story/campaigns/${campaign.id}/graph-start`)
          )
          this.stateBag = { ...resp.data.state?.stateBag }   // neue Referenz!
          this.currentGraphNode = resp.data.node
          this.graphAvailableEdges = resp.data.available_edges || []
          if (resp.data.simulator) {
            this.currentGraphSimulator = resp.data.simulator
            this.phase = 'simulation'
          } else {
            this.phase = 'scene'
          }
        } catch (e) {
          // Fallback auf linearen Modus falls graph-start nicht verfuegbar
          this.isGraphMode = false
          // ... linearer Start bleibt unveraendert
        }
        return
      }

      // Linearer Pfad UNVERAENDERT lassen
      // ...bestehender Code...
    }
    ```

    **KRITISCH (Pitfall H1):** Nach JEDEM graph-traverse/graph-start MUSS stateBag als neue
    Referenz gesetzt werden: `this.stateBag = { ...response.state.stateBag }` — NIEMALS
    `Object.assign(this.stateBag, ...)`.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .vue src/components/AbenteuerMode.vue 2>&1 | tail -10</automated>
  </verify>
  <done>
    AbenteuerMode.vue hat isGraphMode, stateBag, currentGraphNode, graphAvailableEdges in data().
    SimulatorShell importiert und registriert.
    startCampaign() verzweigt auf graph-start fuer is_graph-Kampagnen.
    ESLint 0 Errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simulation-Phase + onSimulatorComplete + graph-traverse Handler</name>
  <files>app/src/components/AbenteuerMode.vue, app/tests/unit/abenteuerModeGraph.test.js</files>
  <behavior>
    - Test: onSimulatorComplete mit passed=true setzt this.stateBag als neue Objekt-Referenz
      (d.h. stateBag nach dem Aufruf !== stateBag davor — kein Object.assign)
    - Test: onSimulatorComplete mit passed=true emittiert graph-traverse POST mit simulator_passed=true
    - Test: onSimulatorComplete mit passed=false + leere graphAvailableEdges setzt phase='epilog'
  </behavior>
  <action>
    **Schritt 0 — Test-Datei erstellen (RED):**
    Erstelle `app/tests/unit/abenteuerModeGraph.test.js`.

    Teste `onSimulatorComplete` direkt auf der component-Definition:
    ```javascript
    import AbenteuerMode from '../../src/components/AbenteuerMode.vue'
    import { vi } from 'vitest'

    // Mock axios.post fuer graph-traverse
    vi.mock('@nextcloud/axios', () => ({
      default: {
        post: vi.fn().mockResolvedValue({
          data: {
            state: { stateBag: { flags: { host_isolated: true }, items: [] } },
            node: { id: 'n2', type: 'scene' },
            available_edges: [],
            simulator: null,
          },
        }),
      },
    }))

    describe('onSimulatorComplete — stateBag Reaktivitaet', () => {
      it('setzt stateBag als neue Referenz nach graph-traverse', async () => {
        const oldBag = { flags: {} }
        const vm = {
          isGraphMode: true,
          selectedCampaign: { id: 'test_graph_campaign' },
          stateBag: oldBag,
          graphAvailableEdges: [{ id: 'e1' }],
          currentGraphNode: { id: 'n1' },
          currentGraphSimulator: null,
          phase: 'simulation',
          $emit: vi.fn(),
        }
        await AbenteuerMode.methods.onSimulatorComplete.call(vm, true, 1.0, { correct: true })
        expect(vm.stateBag).not.toBe(oldBag)  // neue Referenz
        expect(vm.stateBag.flags.host_isolated).toBe(true)
      })
    })
    ```

    Tests initial FAIL (RED) weil onSimulatorComplete noch nicht implementiert.

    **Schritt 1 — simulation-Phase Template-Block ersetzen:**
    Finde im Template den `v-if="phase === 'simulation'"` Block. Aktuell rendert er PbqRenderer.
    Ersetze ihn (fuer Graph-Mode) oder erweitere ihn mit einem `v-if/v-else`:

    ```html
    <!-- Graph-Mode Simulation: echte Netzwerk-Simulatoren -->
    <div v-if="phase === 'simulation' && isGraphMode && currentGraphSimulator" class="campaign-simulator">
      <SimulatorShell
        :type="currentGraphSimulator.type"
        :scenario-id="currentGraphSimulator.scenario || ''"
        :scenario-override="currentGraphSimulator.scenario_override || null"
        :node-id="currentGraphNode && currentGraphNode.id || ''"
        @complete="onSimulatorComplete"
      />
      <div class="campaign-simulator__actions">
        <!-- Retry-Button: User kann es erneut versuchen -->
        <NcButton @click="retrySimulator">Erneut versuchen</NcButton>
        <!-- Wenn available_edges vorhanden: User kann Edge manuell waehlen (Skip) -->
        <NcButton v-if="graphAvailableEdges.length" type="tertiary" @click="showEdgeChoice = true">
          Ueberspringen
        </NcButton>
      </div>
    </div>

    <!-- Linearer Modus: bestehender PbqRenderer bleibt unveraendert -->
    <div v-else-if="phase === 'simulation' && !isGraphMode">
      <!-- bestehender PbqRenderer-Block hier (unveraendert) -->
    </div>
    ```

    **Schritt 2 — onSimulatorComplete() hinzufuegen:**
    Als neue Methode in `methods`:
    ```javascript
    async onSimulatorComplete(passed, score, result) {
      if (!this.isGraphMode) return

      // Edge-Auswahl: nehme erste passende Edge basierend auf passed-Status
      // Konvention: passed=true -> Edge die kein requires_flag hat oder dessen Flag erfuellt ist
      //             passed=false -> erste verfuegbare Edge (Retry oder alternative Route)
      const targetEdge = this.graphAvailableEdges.find(e =>
        passed
          ? (!e.requires_flag || this.stateBag.flags?.[e.requires_flag])
          : true
      ) || this.graphAvailableEdges[0]

      if (!targetEdge) {
        // Keine Edge verfuegbar: Epilog anzeigen
        this.phase = 'epilog'
        return
      }

      try {
        const resp = await axios.post(
          generateUrl(`/apps/learning/api/story/campaigns/${this.selectedCampaign.id}/graph-traverse`),
          {
            edgeId: targetEdge.id,
            simulator_passed: passed,
            simulator_score: Math.round(score * 100),
            simulator_result: JSON.stringify(result),
          }
        )
        // MUSS neue Referenz sein (Vue 2 Reaktivitaet — Pitfall H1):
        this.stateBag = { ...resp.data.state?.stateBag }
        this.currentGraphNode = resp.data.node
        this.graphAvailableEdges = resp.data.available_edges || []

        if (resp.data.simulator) {
          this.currentGraphSimulator = resp.data.simulator
          this.phase = 'simulation'
        } else if (resp.data.node?.type === 'ending') {
          this.phase = 'epilog'
        } else {
          this.currentGraphSimulator = null
          this.phase = 'scene'
        }
      } catch (e) {
        console.error('graph-traverse failed', e)
        // Fehlerzustand: User bleibt auf Simulator-Phase, kann erneut versuchen
      }
    },

    retrySimulator() {
      // nodeId-Prop wechseln erzwingt frischen Mount via :key
      // Reuse currentGraphNode.id + timestamp als neuen nodeId
      if (this.currentGraphNode) {
        this.currentGraphNode = { ...this.currentGraphNode, _retryKey: Date.now() }
      }
    },
    ```

    **Schritt 3 — graphChoiceHandler fuer graph-Mode (scene-Phase Edges):**
    Finde die bestehende Methode die eine Choice/Edge ausfuehrt (z.B. `makeChoice()` oder `selectEdge()`).
    Erweitere sie fuer graph-mode: wenn `isGraphMode === true`, rufe `graph-traverse` mit `edgeId`
    statt des linearen `/choice`-Endpunkts auf.

    ```javascript
    async makeGraphChoice(edgeId) {
      try {
        const resp = await axios.post(
          generateUrl(`/apps/learning/api/story/campaigns/${this.selectedCampaign.id}/graph-traverse`),
          { edgeId }
        )
        this.stateBag = { ...resp.data.state?.stateBag }   // neue Referenz!
        this.currentGraphNode = resp.data.node
        this.graphAvailableEdges = resp.data.available_edges || []

        if (resp.data.simulator) {
          this.currentGraphSimulator = resp.data.simulator
          this.phase = 'simulation'
        } else if (resp.data.node?.type === 'ending') {
          this.phase = 'epilog'
        } else {
          this.currentGraphSimulator = null
          this.phase = 'scene'
        }
      } catch (e) {
        console.error('graph-traverse (choice) failed', e)
      }
    },
    ```

    Im Template fuer graph-mode scene-Phase: die vorhandenen Choice-Buttons auf
    `@click="makeGraphChoice(edge.id)"` umstellen wenn `isGraphMode === true`.

    **ESLint danach pruefen.** Keine vorhandenen linearen Methoden entfernen.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run test -- --run abenteuerModeGraph 2>&1 | tail -15 && npx eslint --ext .vue src/components/AbenteuerMode.vue 2>&1 | tail -15 && npm run test -- --run 2>&1 | tail -10</automated>
  </verify>
  <done>
    AbenteuerMode.vue hat onSimulatorComplete(), makeGraphChoice(), retrySimulator().
    simulation-Phase rendert SimulatorShell fuer Graph-Mode.
    abenteuerModeGraph.test.js: stateBag neue Referenz verifiziert (gruen).
    ESLint 0 Errors.
    Gesamte Test-Suite (alle 162+ Tests) weiterhin gruen.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase-80 Gesamt-Verifikation auf learning-dev</name>
  <action>Deploy auf learning-dev (`./scripts/deploy-dev.sh --js-only`) und Gesamte Test-Suite ausfuehren (`npm run test -- --run`). Alle Gate-1-Checks muessen gruen sein bevor Browser-Verifikation.</action>
  <what-built>
    Vollstaendiges Phase-80-Wiring:
    - 5 Simulatoren mit beforeDestroy-Hooks (Plan 80-01)
    - SimulatorShell.vue mit SIMULATOR_MAP + @result-Normierung (Plan 80-02)
    - AbenteuerMode.vue mit Graph-Mode, graph-traverse, SimulatorShell-Integration (diese Tasks)
  </what-built>
  <how-to-verify>
    1. Deploy auf learning-dev:
       `./scripts/deploy-dev.sh --js-only`

    2. Im Browser: https://192.168.178.65:8080 als andre anmelden

    3. Lernmodus → Abenteuer → Kampagne "Test Graph Kampagne" auswaehlen
       (falls noch nicht vorhanden: test_graph_campaign.json muss in STATIC_CAMPAIGNS eingetragen sein)

    4. Erwartetes Verhalten bei einer simulator-Node:
       - Simulator-Komponente erscheint eingebettet in der Szene (nicht als separater Tab)
       - Nach Loesen: naechste Szene laedt automatisch
       - Nach falschem Loesen: "Erneut versuchen"-Button sichtbar

    5. PHPStan Gate-Check (optional, da kein PHP geaendert):
       `ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && php vendor/bin/phpstan analyse --no-progress"'`

    6. Gesamte Test-Suite:
       `cd /home/andre/Workspace/Code/learning-nc/app && npm run test -- --run`
       Erwartung: alle Tests gruen (simulatorLifecycle: 7, SimulatorShell: 9+, abenteuerModeGraph: 3, Rest: 162+)
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn Simulator in Kampagnen-Szene korrekt funktioniert, oder beschreibe gefundene Probleme.</resume-signal>
</task>

</tasks>

<verification>
```bash
# Test-Suite komplett
cd /home/andre/Workspace/Code/learning-nc/app && npm run test -- --run

# ESLint
cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .vue src/components/AbenteuerMode.vue

# Gate 1 komplett
cd /home/andre/Workspace/Code/learning-nc/app && npm run lint && npm run test -- --run
```
Alle Tests gruen, ESLint 0 Errors.
</verification>

<success_criteria>
- AbenteuerMode.vue rendert SimulatorShell bei graph-mode simulation-Phase
- graph-start wird fuer is_graph-Kampagnen aufgerufen (nicht /start)
- graph-traverse wird nach Simulator-Completion mit simulator_passed/score/result aufgerufen
- stateBag wird nach jedem traverse als neue Referenz gesetzt (Vue 2 reaktiv)
- abenteuerModeGraph.test.js bestaetigt stateBag !== alter stateBag nach onSimulatorComplete
- Linearer Pfad (/start, /scene, /choice) unveraendert und funktional
- Alle 162+ bestehenden Tests weiterhin gruen
- ESLint 0 Errors auf AbenteuerMode.vue
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/80-simulatorshell-wiring/80-03-SUMMARY.md` erstellen.
</output>
