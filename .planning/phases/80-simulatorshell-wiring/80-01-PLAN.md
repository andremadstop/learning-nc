---
phase: 80-simulatorshell-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/FirewallBuilder.vue
  - app/src/components/RoutingTable.vue
  - app/src/components/NatTable.vue
  - app/src/components/WiresharkLite.vue
  - app/src/components/AuthFlowSimulator.vue
  - app/tests/unit/simulatorLifecycle.test.js
autonomous: true
requirements:
  - SIM-03

must_haves:
  truths:
    - "Alle 7 Simulator-Komponenten haben einen beforeDestroy-Hook"
    - "Kein Timer oder globaler Event-Listener bleibt nach Komponenten-Destroy aktiv"
    - "Simulatoren koennen problemlos ge-mount und ge-unmount werden ohne Memory-Leaks"
  artifacts:
    - path: "app/src/components/FirewallBuilder.vue"
      provides: "beforeDestroy hook (leer, explizit)"
      contains: "beforeDestroy"
    - path: "app/src/components/RoutingTable.vue"
      provides: "beforeDestroy hook (leer, explizit)"
      contains: "beforeDestroy"
    - path: "app/src/components/NatTable.vue"
      provides: "beforeDestroy hook (leer, explizit)"
      contains: "beforeDestroy"
    - path: "app/src/components/WiresharkLite.vue"
      provides: "beforeDestroy hook (leer, explizit)"
      contains: "beforeDestroy"
    - path: "app/src/components/AuthFlowSimulator.vue"
      provides: "beforeDestroy hook (leer, explizit)"
      contains: "beforeDestroy"
    - path: "app/tests/unit/simulatorLifecycle.test.js"
      provides: "Lifecycle-Tests fuer alle 7 Simulatoren"
      exports: []
  key_links:
    - from: "SimulatorShell.vue (Phase 80-02)"
      to: "alle 7 Simulator-Komponenten"
      via: "dynamic import + :key=nodeId mount/unmount"
      pattern: "beforeDestroy"
---

<objective>
Fuenf der sieben Netzwerk-Simulator-Komponenten haben keinen `beforeDestroy`-Hook.
Wenn SimulatorShell diese Komponenten einbettet und der Spieler eine Szene verlasst,
koennen laufende Timers oder globale Event-Listener haengenbleiben.

Diese Plan ruestet die fehlenden Lifecycle-Hooks nach und erstellt den Test-Scaffold
fuer SIM-03. DnsResolver und PortScanner sind bereits korrekt (haben beforeDestroy)
und werden nur im Test verifiziert, nicht angefasst.

Purpose: Saubere Lifecycle-Grundlage fuer das SimulatorShell-Embedding (Plan 80-02).
Output: 5 modifizierte Komponenten + Test-Datei simulatorLifecycle.test.js
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-simulatorshell-wiring/80-RESEARCH.md

<interfaces>
<!-- Audit-Ergebnis aus 80-RESEARCH.md: -->
<!-- FirewallBuilder: kein beforeDestroy, kein Timer, kein globaler Listener -->
<!-- RoutingTable: kein beforeDestroy, kein Timer -->
<!-- NatTable: kein beforeDestroy, kein Timer -->
<!-- WiresharkLite: kein beforeDestroy, kein Timer -->
<!-- AuthFlowSimulator: kein beforeDestroy, kein Timer -->
<!-- DnsResolver: JA (this.clearTimer()) — NICHT anfassen -->
<!-- PortScanner: JA (window.clearInterval(this.intervalId)) — NICHT anfassen -->

<!-- Alle 7 Simulatoren teilen dieselbe Props-Schnittstelle: -->
```javascript
props: {
  mode: { type: String, default: 'standalone' },  // 'standalone' | 'embedded'
  scenario: { type: Object, default: null },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-Scaffold simulatorLifecycle.test.js erstellen</name>
  <files>app/tests/unit/simulatorLifecycle.test.js</files>
  <behavior>
    - Test: FirewallBuilder hat beforeDestroy-Option in seinem component options-Objekt
    - Test: RoutingTable hat beforeDestroy-Option
    - Test: NatTable hat beforeDestroy-Option
    - Test: WiresharkLite hat beforeDestroy-Option
    - Test: AuthFlowSimulator hat beforeDestroy-Option
    - Test: DnsResolver hat beforeDestroy-Option (war bereits vorhanden — Regression Guard)
    - Test: PortScanner hat beforeDestroy-Option (war bereits vorhanden — Regression Guard)
    - Alle Tests pruefen: `typeof ComponentOptions.beforeDestroy === 'function'`
  </behavior>
  <action>
    Erstelle `app/tests/unit/simulatorLifecycle.test.js` mit Vitest.

    Importiere alle 7 Simulator-Komponenten direkt (kein Vue-Mount noetig — nur die component-Options pruefen):

    ```javascript
    import { describe, it, expect } from 'vitest'
    import FirewallBuilder from '../../src/components/FirewallBuilder.vue'
    import DnsResolver from '../../src/components/DnsResolver.vue'
    import RoutingTable from '../../src/components/RoutingTable.vue'
    import NatTable from '../../src/components/NatTable.vue'
    import PortScanner from '../../src/components/PortScanner.vue'
    import WiresharkLite from '../../src/components/WiresharkLite.vue'
    import AuthFlowSimulator from '../../src/components/AuthFlowSimulator.vue'
    ```

    Fuer jede Komponente: `expect(typeof Component.beforeDestroy).toBe('function')`.

    Der Test MUSS initial fuer 5 Komponenten FAIL (RED-Phase), da diese beforeDestroy noch nicht haben.
    Erst nach Task 2 sind alle 7 gruen.

    Befehl: `cd /home/andre/Workspace/Code/learning-nc/app && npm run test -- --run simulatorLifecycle`
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run test -- --run simulatorLifecycle 2>&1 | tail -20</automated>
  </verify>
  <done>Test-Datei existiert. Initial schlagen 5 Tests fehl (RED). DnsResolver + PortScanner Tests sind gruen.</done>
</task>

<task type="auto">
  <name>Task 2: beforeDestroy-Hooks auf 5 Simulatoren nachrueesten</name>
  <files>
    app/src/components/FirewallBuilder.vue,
    app/src/components/RoutingTable.vue,
    app/src/components/NatTable.vue,
    app/src/components/WiresharkLite.vue,
    app/src/components/AuthFlowSimulator.vue
  </files>
  <action>
    Fuer jede der 5 Komponenten: Lese die Datei, finde den `export default {` Block und fuege
    `beforeDestroy()` als leere Methode in den Lifecycle-Hooks-Bereich ein (nach `mounted` oder
    direkt nach `created`, vor `methods`).

    Schema fuer alle 5 identisch:

    ```javascript
    beforeDestroy() {
      // Keine aktiven Timer oder globalen Event-Listener registriert.
      // Hook vorhanden fuer sauberes Embedded-Rendering in SimulatorShell.
    },
    ```

    **FirewallBuilder.vue**: Hat dragIndex state (Vue-intern, wird automatisch bereinigt). Kein globaler Listener. Leerer Hook mit Kommentar.

    **RoutingTable.vue**: Hat keine Timer, keine globalen Listener. Leerer Hook mit Kommentar.

    **NatTable.vue**: Hat keine Timer, keine globalen Listener. Leerer Hook mit Kommentar.

    **WiresharkLite.vue**: Hat keine Timer (laut Audit). Leerer Hook mit Kommentar. Pruefen ob es doch ein setInterval gibt — wenn ja, clearInterval hinzufuegen.

    **AuthFlowSimulator.vue**: Hat `passed` und `errors` als result-Format. Kein Timer. Leerer Hook mit Kommentar.

    NICHT anfassen: DnsResolver.vue (hat `this.clearTimer()`), PortScanner.vue (hat `window.clearInterval(this.intervalId)`).

    Nach den Aenderungen: ESLint pruefen.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run test -- --run simulatorLifecycle 2>&1 | tail -10 && npx eslint --ext .vue src/components/FirewallBuilder.vue src/components/RoutingTable.vue src/components/NatTable.vue src/components/WiresharkLite.vue src/components/AuthFlowSimulator.vue 2>&1 | tail -10</automated>
  </verify>
  <done>
    Alle 7 simulatorLifecycle Tests sind gruen (GREEN-Phase).
    ESLint 0 Errors auf allen 5 modifizierten Dateien.
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/andre/Workspace/Code/learning-nc/app && npm run test -- --run simulatorLifecycle
cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .vue src/components/FirewallBuilder.vue src/components/RoutingTable.vue src/components/NatTable.vue src/components/WiresharkLite.vue src/components/AuthFlowSimulator.vue
```
Alle 7 Tests gruen, ESLint 0 Errors.
</verification>

<success_criteria>
- Alle 7 Simulator-Komponenten haben `beforeDestroy` als Funktion im component options-Objekt
- simulatorLifecycle.test.js: 7/7 Tests gruen
- ESLint 0 Errors auf den 5 modifizierten Dateien
- DnsResolver.vue und PortScanner.vue unveraendert (git diff zeigt keine Aenderungen)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/80-simulatorshell-wiring/80-01-SUMMARY.md` erstellen.
</output>
