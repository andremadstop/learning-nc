---
phase: 63-exam-sperre-fehler-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ExamMode.vue
  - app/src/components/VirtuProf.vue
  - app/src/components/VirtuProfBubble.vue
autonomous: true
requirements: [EXAM-01, EXAM-02, REP-01, REP-02]

must_haves:
  truths:
    - "VirtuProf chat input is disabled and shows a blocked message when ExamMode is active"
    - "VirtuProf re-enables automatically when exam ends (result screen or back)"
    - "User can click 'Fehler melden' button and the report includes the current questionId and mode automatically"
    - "Reported errors create SupportTicket entries visible to instructors/admins"
  artifacts:
    - path: "app/src/components/ExamMode.vue"
      provides: "virtuprof:exam-mode event emission"
      contains: "virtuprof:exam-mode"
    - path: "app/src/components/VirtuProf.vue"
      provides: "isExamMode state tracking + report-error handler"
      contains: "isExamMode"
    - path: "app/src/components/VirtuProfBubble.vue"
      provides: "Exam blocked message + Fehler melden button"
      contains: "Fehler melden"
  key_links:
    - from: "app/src/components/ExamMode.vue"
      to: "app/src/components/VirtuProf.vue"
      via: "$root.$emit('virtuprof:exam-mode')"
      pattern: "virtuprof:exam-mode"
    - from: "app/src/components/VirtuProfBubble.vue"
      to: "app/src/components/VirtuProf.vue"
      via: "$emit('report-error')"
      pattern: "report-error"
    - from: "app/src/components/VirtuProf.vue"
      to: "/api/virtu-prof/chat (ticket-intent)"
      via: "handleChatSend with 'Fehler melden' message"
      pattern: "handleReportError"
---

<objective>
Exam-Sperre + Fehler-Report: VirtuProf im Exam-Mode deaktivieren und "Fehler melden" Button einfuegen.

Purpose: Faire Pruefungsbedingungen (kein KI-Zugriff im Exam) und direktes Fehler-Reporting mit automatischer Fragen-ID.
Output: ExamMode emittiert exam-mode Events, VirtuProf sperrt Chat, VirtuProfBubble zeigt "Fehler melden" Button.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/components/ExamMode.vue
@app/src/components/VirtuProf.vue
@app/src/components/VirtuProfBubble.vue
@app/lib/Controller/VirtuProfController.php
@app/lib/Service/SupportTicketService.php
@app/appinfo/routes.php

<interfaces>
<!-- ExamMode already emits virtuprof:context with correctAnswerIndex=null (line ~407-417) -->
<!-- ExamMode beforeDestroy emits virtuprof:context with questionContext: null (line ~1028) -->

From app/src/components/VirtuProf.vue:
- data().currentContext tracks area, courseTitle, poolName, questionContext
- updateContext(context) merges into currentContext (line ~264)
- $root.$on('virtuprof:context', this.updateContext) in mounted (line ~181)
- handleChatSend(message) sends to /api/virtu-prof/chat with currentContext payload (line ~977)
- isExamMode does NOT exist yet — needs to be added

From app/lib/Controller/VirtuProfController.php:
- isTicketIntent() detects "fehler melden", "bug melden" etc. (line ~378-396)
- handleTicketIntent() creates ticket via SupportTicketService with poolId, courseId context (line ~399-436)
- SupportTicketService::create() already accepts questionId in context array (line ~55)

From app/src/components/VirtuProfBubble.vue:
- aiEnabled prop controls chat vs FAQ layout
- chat-input-row at line ~71-92 has input + send button
- "Mehr Optionen" section at line ~104+ has expandable options
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exam-Sperre — ExamMode emittiert exam-mode Event, VirtuProf sperrt Chat</name>
  <files>app/src/components/ExamMode.vue, app/src/components/VirtuProf.vue, app/src/components/VirtuProfBubble.vue</files>
  <action>
EXAM-01 + EXAM-02: Automatische Sperre des VirtuProf-Chats im Exam-Mode.

1. **ExamMode.vue** — Emit exam-mode events:
   - In `startExam()` method (or where screen transitions to 'exam'): add `this.$root.$emit('virtuprof:exam-mode', true)`
   - In `beforeDestroy()`: add `this.$root.$emit('virtuprof:exam-mode', false)` BEFORE the existing `virtuprof:context` reset
   - When exam ends (screen transitions to 'result'): add `this.$root.$emit('virtuprof:exam-mode', false)`
   - Also emit false if user clicks "Back" from setup screen (so no stale lock)

2. **VirtuProf.vue** — Listen and track exam mode:
   - Add `isExamMode: false` to data()
   - In mounted(): add `this.$root.$on('virtuprof:exam-mode', this.setExamMode)`
   - In beforeDestroy(): add `this.$root.$off('virtuprof:exam-mode', this.setExamMode)`
   - Add method `setExamMode(active) { this.isExamMode = !!active }`
   - Pass `:exam-blocked="isExamMode"` prop to VirtuProfBubble

3. **VirtuProfBubble.vue** — Show blocked state:
   - Add prop `examBlocked: { type: Boolean, default: false }`
   - When `examBlocked` is true AND `aiEnabled` is true: replace the chat-input-row with a blocked message div:
     ```html
     <div v-if="examBlocked" class="exam-blocked-notice" role="status">
       <span class="exam-blocked-icon">&#128274;</span>
       {{ vt('Not available during exam') }}
     </div>
     ```
   - Also disable the quick suggestion chips when examBlocked
   - Style `.exam-blocked-notice`: centered text, muted color (#888), padding 12px, italic, border-top 1px solid var(--color-border)
   - Do NOT hide the entire bubble — chat history remains visible (user might have pre-exam messages)
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .js,.vue src/components/ExamMode.vue src/components/VirtuProf.vue src/components/VirtuProfBubble.vue</automated>
  </verify>
  <done>
    - ExamMode emits 'virtuprof:exam-mode' true on exam start and false on exam end/destroy
    - VirtuProf tracks isExamMode and passes examBlocked prop to VirtuProfBubble
    - VirtuProfBubble shows lock icon + "Not available during exam" message instead of chat input when examBlocked=true
    - Chat suggestions are disabled during exam mode
    - Sperre lifts automatically when exam ends
  </done>
</task>

<task type="auto">
  <name>Task 2: Fehler-Report — "Fehler melden" Button mit automatischer Fragen-ID</name>
  <files>app/src/components/VirtuProfBubble.vue, app/src/components/VirtuProf.vue</files>
  <action>
REP-01 + REP-02: "Fehler melden" Button der automatisch Fragen-ID und Modus mitschickt.

1. **VirtuProfBubble.vue** — Add report button:
   - Add prop `hasQuestionContext: { type: Boolean, default: false }` (so button only shows when a question is active)
   - Below the chat-input-row (and below the exam-blocked-notice if present), add:
     ```html
     <button
       v-if="hasQuestionContext && !examBlocked"
       type="button"
       class="report-error-btn"
       :disabled="chatLoading"
       @click="$emit('report-error')">
       &#9888; {{ vt('Report error in question') }}
     </button>
     ```
   - Style `.report-error-btn`: text-align left, color: var(--color-warning-text, #c45911), background: transparent, border: none, padding: 6px 12px, cursor: pointer, font-size: 13px, opacity: 0.8, hover: opacity 1, underline

2. **VirtuProf.vue** — Handle report-error event:
   - Add `@report-error="handleReportError"` to VirtuProfBubble in template
   - Pass `:has-question-context="hasActiveQuestionContext"` prop to VirtuProfBubble
   - Add computed `hasActiveQuestionContext()`: returns `!!(this.currentContext?.questionContext?.questionId)`
   - Add method `handleReportError()`:
     - Build a structured report message: `'Fehler melden: Frage #' + this.currentContext.questionContext.questionId`
     - This message will match the existing `isTicketIntent()` in VirtuProfController (pattern "fehler melden")
     - Before sending, temporarily enrich `this.currentContext` with `questionId` from questionContext so handleChatSend includes it in the payload
     - The existing handleTicketIntent in VirtuProfController receives poolId, courseId from the payload; for questionId, add it to the context object in handleTicketIntent:
       No backend change needed — the message itself contains the question ID, and poolId/courseId are already sent. The existing `SupportTicketService::create()` stores context_json which includes poolId and courseId. The questionId from the message text is sufficient for the instructor to identify the question.
     - HOWEVER, to make it cleaner: in VirtuProf.vue's handleChatSend, the payload already sends questionContext which includes questionId. In VirtuProfController::handleTicketIntent, add questionId extraction from the chat() parameters:

3. **VirtuProfController.php** — Pass questionId to ticket context:
   - In `chat()` method, the `handleTicketIntent` call at line ~205 only passes `$message, $poolId, $courseId`
   - Update `handleTicketIntent` signature to accept `?int $questionId = null`
   - In `chat()`, extract questionId: `$questionId = $questionContext['questionId'] ?? null;`
   - Pass it: `return $this->handleTicketIntent($message, $poolId, $courseId, $questionId);`
   - In `handleTicketIntent`, add to context array: `if ($questionId !== null) { $context['questionId'] = $questionId; }`
   - Also add mode/area info: `$context['mode'] = 'question_error_report';`
   - The existing `SupportTicketService::create()` already handles `$context['questionId']` at line 55: `$ticket->setQuestionId($this->normalizeInt($context['questionId'] ?? null));`
   - This means the ticket will have the questionId column populated — queryable by instructors.

4. **Category routing**: Use category `'course_content'` (not `'technical'`) when questionId is present, so the ticket routes to the course instructor instead of admin. Pass `$courseId` context and the routing logic in `resolveRouting()` will handle it.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .js,.vue src/components/VirtuProf.vue src/components/VirtuProfBubble.vue && ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && php vendor/bin/phpstan analyse lib/Controller/VirtuProfController.php --no-progress"' 2>/dev/null || echo "PHPStan check (may need deploy first)"</automated>
  </verify>
  <done>
    - "Fehler melden" button visible in VirtuProfBubble when a question is active (hasQuestionContext)
    - Button hidden during exam mode (examBlocked)
    - Clicking the button sends a structured report via the existing ticket-intent pipeline
    - Report automatically includes questionId from the current question context
    - Ticket is created via SupportTicketService with questionId populated in the DB
    - Tickets with questionId use 'course_content' category for instructor routing
    - Instructor can see the report in their ticket list with question reference
  </done>
</task>

</tasks>

<verification>
1. ESLint passes for all 3 modified Vue files
2. PHPStan passes for VirtuProfController.php
3. Manual verification: Open ExamMode, confirm VirtuProf chat shows lock message. End exam, confirm chat re-enables.
4. Manual verification: Open a training/leitner question, click "Fehler melden", confirm ticket is created with questionId.
</verification>

<success_criteria>
- VirtuProf chat is fully disabled during Exam Mode with visible "Not available during exam" notice
- Exam-Sperre activates/deactivates automatically without user action
- "Fehler melden" button appears only when a question is active
- Error reports include automatic questionId and route to instructor via SupportTicketService
- No regressions in existing VirtuProf chat, hint system, or ticket functionality
</success_criteria>

<output>
After completion, create `.planning/phases/63-exam-sperre-fehler-report/63-01-SUMMARY.md`
</output>
