---
phase: 06-instructor-notes
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/src/components/QuestionForm.vue
  - app/src/components/TrainingMode.vue
  - app/src/components/LeitnerMode.vue
  - app/src/components/SmartQueue.vue
  - app/src/components/ExamMode.vue
autonomous: false
requirements:
  - NOTE-02
  - NOTE-03
  - NOTE-04

must_haves:
  truths:
    - "QuestionForm shows instructor_note textarea always (edit mode)"
    - "QuestionForm shows note_visible toggle always (edit mode)"
    - "Saving a question with note text + toggle ON persists correctly"
    - "TrainingMode shows note in feedback when note_visible=true and instructor_note is set"
    - "LeitnerMode shows note in feedback when note_visible=true and instructor_note is set"
    - "SmartQueue shows note in feedback when note_visible=true and instructor_note is set"
    - "ExamMode post-exam review shows note per question when note_visible=true"
    - "Note is NOT shown in any mode when note_visible=false"
    - "Note uses plain text rendering, no v-html"
  artifacts:
    - path: "app/src/components/QuestionForm.vue"
      provides: "instructor_note textarea + NcCheckboxRadioSwitch visibility toggle"
    - path: "app/src/components/TrainingMode.vue"
      provides: "NcNoteCard for instructor note in answer feedback"
    - path: "app/src/components/LeitnerMode.vue"
      provides: "NcNoteCard for instructor note in answer feedback"
    - path: "app/src/components/SmartQueue.vue"
      provides: "NcNoteCard for instructor note in answer feedback"
    - path: "app/src/components/ExamMode.vue"
      provides: "NcNoteCard in post-exam review list per question"
  key_links:
    - from: "app/src/components/QuestionForm.vue"
      to: "app/src/components/QuestionList.vue"
      via: "save() emit — spread operator auto-forwards instructorNote+noteVisible"
      pattern: "instructorNote.*noteVisible"
    - from: "app/src/components/TrainingMode.vue"
      to: "currentQuestion.note_visible"
      via: "v-if condition on NcNoteCard"
      pattern: "note_visible.*instructor_note"
    - from: "app/src/components/ExamMode.vue"
      to: "this.questions array"
      via: "detailedResults enriched with instructorNote+noteVisible from q object"
      pattern: "instructorNote.*noteVisible"
---

<objective>
Add the instructor note UI to QuestionForm (editor) and all four learning mode components (TrainingMode, LeitnerMode, SmartQueue, ExamMode).

Purpose: Makes the instructor_note field created in Plan 01 visible and editable. Instructors write notes per question; students see them in learning mode feedback when note_visible=true.
Output: QuestionForm with textarea + toggle, four learning modes with conditional NcNoteCard display.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-instructor-notes/06-RESEARCH.md
@.planning/phases/06-instructor-notes/06-01-SUMMARY.md

@app/src/components/QuestionForm.vue
@app/src/components/TrainingMode.vue
@app/src/components/LeitnerMode.vue
@app/src/components/SmartQueue.vue
@app/src/components/ExamMode.vue
</context>

<interfaces>
<!-- Contracts established in Plan 01. Executor uses these directly. -->

API response shape (from Question.php jsonSerialize after Plan 01):
```json
{
  "id": 42,
  "text": "...",
  "explanation": "...",
  "instructor_note": "This question tests VLAN fundamentals.",
  "note_visible": true,
  ...
}
```

QuestionList.vue spread (line 148) — auto-forwards new fields, NO change needed:
```js
async saveQuestion(questionData) {
  const { imageFile, removeImage, questionType, ...data } = questionData;
  // instructorNote and noteVisible in ...data — forwarded automatically
  await axios.put(url, data);
}
```

Existing explanation NcNoteCard pattern (model for note display):
```html
<!-- TrainingMode.vue line 69 / 95 -->
<NcNoteCard v-if="currentQuestion.explanation" type="warning">
  <strong>{{ t('learning', 'Explanation:') }}</strong> {{ currentQuestion.explanation }}
</NcNoteCard>

<!-- LeitnerMode.vue line 106 / 135 -->
<NcNoteCard v-if="currentItem.explanation" type="warning">{{ currentItem.explanation }}</NcNoteCard>

<!-- SmartQueue.vue line 66 / 88 -->
<NcNoteCard v-if="currentItem.explanation" type="warning">{{ currentItem.explanation }}</NcNoteCard>
```

ExamMode detailedResults build loop (lines 796-833) — each result item is built here:
```js
for (const q of this.questions) {
  // q has: q.instructor_note, q.note_visible (from jsonSerialize)
  results.push({
    questionText: ...,
    userAnswerText: ...,
    // ADD: instructorNote: q.instructor_note || null,
    // ADD: noteVisible: q.note_visible || false,
  });
}
```

NcCheckboxRadioSwitch type="checkbox" usage (existing in QuestionForm.vue):
```html
<NcCheckboxRadioSwitch
  v-if="form.questionType === 'single'"
  :checked="correctAnswerIndex === index"
  @update:checked="correctAnswerIndex = index"
  type="radio"
  name="correct-answer"
/>
<!-- For noteVisible: type="checkbox" (no name needed, no v-if guard) -->
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: QuestionForm — instructor_note textarea + note_visible toggle</name>
  <files>app/src/components/QuestionForm.vue</files>
  <action>
    **Step A — Add form fields to `data()`:**

    In the `form` object inside `data()`, add after `pbqConfig: ''`:
    ```js
    instructorNote: '',
    noteVisible: false,
    ```

    **Step B — Initialize from existing question in `mounted()`:**

    After the existing `this.form.pbqSubtype = ...` and `this.form.pbqConfig = ...` init lines (inside the `if (this.question)` block), add:
    ```js
    this.form.instructorNote = this.question.instructor_note || ''
    this.form.noteVisible = this.question.note_visible || false
    ```

    **Step C — Include in `save()` emit:**

    Find where `save()` emits the question data (the object passed to `$emit('save', {...})` or the axios call). Add to the emitted object:
    ```js
    instructorNote: this.form.instructorNote || null,
    noteVisible: this.form.noteVisible,
    ```

    **Step D — Add UI elements to the template:**

    Insert the following block AFTER the closing `</div>` of the existing PBQ Config section (after the `showAuthorTool` NcDialog), and BEFORE the `<div class="dialog-actions">` block:

    ```html
    <!-- Instructor Note -->
    <div class="form-group">
      <label for="instructor-note">{{ t('learning', 'Instructor Note (optional)') }}</label>
      <textarea
        id="instructor-note"
        v-model="form.instructorNote"
        rows="3"
        :placeholder="t('learning', 'Private note for instructors...')"
        class="nc-input"
      ></textarea>
      <NcCheckboxRadioSwitch
        :checked="form.noteVisible"
        @update:checked="form.noteVisible = $event"
        type="checkbox"
      >
        {{ t('learning', 'Visible to students') }}
      </NcCheckboxRadioSwitch>
    </div>
    ```

    DO NOT use v-html anywhere in this template block. The note text is rendered via v-model (edit) and will be rendered via `{{ }}` in learning modes (display). No sanitization concerns.

    NOTE-04 is handled structurally: the instructor_note textarea is always visible in QuestionForm (no v-if gate). The note_visible toggle controls student visibility in learning modes. The instructor editing the form always sees their own note text and toggle state regardless of what note_visible is set to.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>QuestionForm.vue has instructorNote+noteVisible in form data, mounted() initializes from question prop, save() includes both fields, template has instructor-note textarea and NcCheckboxRadioSwitch. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Learning modes — note display in TrainingMode, LeitnerMode, SmartQueue, ExamMode</name>
  <files>app/src/components/TrainingMode.vue, app/src/components/LeitnerMode.vue, app/src/components/SmartQueue.vue, app/src/components/ExamMode.vue</files>
  <action>
    **TrainingMode.vue — two feedback blocks:**

    There are two answered feedback blocks in TrainingMode (one for open questions line ~63, one for MC questions line ~78). In each block, find the existing explanation NcNoteCard:
    ```html
    <NcNoteCard v-if="currentQuestion.explanation" type="warning">...</NcNoteCard>
    ```
    Add immediately AFTER each explanation NcNoteCard:
    ```html
    <NcNoteCard v-if="currentQuestion.note_visible && currentQuestion.instructor_note" type="info">
      <strong>{{ t('learning', 'Note:') }}</strong> {{ currentQuestion.instructor_note }}
    </NcNoteCard>
    ```

    **LeitnerMode.vue — two feedback blocks:**

    LeitnerMode uses `currentItem` (not `currentQuestion`). There are two explanation NcNoteCards (lines 106 and 135):
    ```html
    <NcNoteCard v-if="currentItem.explanation" type="warning">{{ currentItem.explanation }}</NcNoteCard>
    ```
    Add immediately AFTER each:
    ```html
    <NcNoteCard v-if="currentItem.note_visible && currentItem.instructor_note" type="info">
      <strong>{{ t('learning', 'Note:') }}</strong> {{ currentItem.instructor_note }}
    </NcNoteCard>
    ```

    **SmartQueue.vue — two feedback blocks:**

    SmartQueue uses `currentItem`. There are two explanation NcNoteCards (lines 66 and 88):
    ```html
    <NcNoteCard v-if="currentItem.explanation" type="warning">{{ currentItem.explanation }}</NcNoteCard>
    ```
    Add immediately AFTER each:
    ```html
    <NcNoteCard v-if="currentItem.note_visible && currentItem.instructor_note" type="info">
      <strong>{{ t('learning', 'Note:') }}</strong> {{ currentItem.instructor_note }}
    </NcNoteCard>
    ```

    **ExamMode.vue — two changes:**

    Change 1 — Enrich detailedResults in the build loop (around line 824 in the `results.push({...})` call):

    Find the `results.push({` block in the `completeExam` method. Add two fields to the pushed object:
    ```js
    results.push({
      questionText: rv ? rv.questionText : q.text,
      userAnswerText: userAnswerText,
      correctAnswerText: correctText,
      isCorrect: rv ? rv.is_correct : false,
      isMulti: rv ? rv.questionType === 'multi' : q.question_type === 'multi',
      isOpen: isOpen,
      answerDetails: answerDetails,
      instructorNote: q.instructor_note || null,   // ADD
      noteVisible: q.note_visible || false,         // ADD
    });
    ```

    `q` is available in the loop (`for (const q of this.questions)`) and contains `instructor_note` and `note_visible` from the questions API (loaded at exam start from `/api/pools/{id}/questions`).

    Change 2 — Add NcNoteCard in the review template (around line 234, inside each review-item div):

    Find the closing `</template>` of the last `<template v-else>` block in the `v-for="(res, index) in sortedDetailedResults"` loop (around line 234). Insert AFTER the closing `</template>` and BEFORE the closing `</div>` of the review-item:
    ```html
    <NcNoteCard v-if="res.noteVisible && res.instructorNote" type="info">
      <strong>{{ t('learning', 'Note:') }}</strong> {{ res.instructorNote }}
    </NcNoteCard>
    ```

    **Critical rules for all components:**
    - NEVER use v-html — notes render as plain text via `{{ }}` only
    - The v-if must check BOTH `note_visible` (truthy) AND `instructor_note` (non-empty) — empty string must not show
    - Do not modify the stripCorrect logic in QuestionService
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 learning mode components have instructor note NcNoteCard blocks. Build passes. No v-html usage introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy frontend + run npm test</name>
  <files></files>
  <action>
    **Step A — Deploy frontend bundle:**

    ```bash
    ssh learning-dev 'cd ~/learning-nc/app && npm run build'
    ssh learning-dev 'cd ~/learning-nc/app && tar cf /tmp/js-bundle.tar js/'
    ssh learning-dev 'docker cp /tmp/js-bundle.tar learning-app:/tmp/'
    ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && tar xf /tmp/js-bundle.tar"'
    ```

    Alternatively, if local build is more current than learning-dev:
    ```bash
    rsync -avz --exclude node_modules --exclude .git app/ learning-dev:~/learning-nc/app/
    ssh learning-dev 'cd ~/learning-nc/app && npm run build'
    ssh learning-dev 'cd ~/learning-nc/app && tar cf /tmp/js-bundle.tar js/'
    ssh learning-dev 'docker cp /tmp/js-bundle.tar learning-app:/tmp/'
    ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && tar xf /tmp/js-bundle.tar"'
    ```

    **Step B — Run full test suite:**

    ```bash
    cd /home/andre/Workspace/Code/learning-nc/app && npm test
    ```

    All tests must pass including the instructorNote.test.js from Plan 01.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>npm test passes all tests. Frontend bundle deployed to learning-dev container.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser verification — instructor note feature end-to-end</name>
  <action>
    Open http://learning-dev:8080 and log in.

    **Test NOTE-02 (QuestionForm editor):**
    1. Open any pool, click Edit on any question
    2. Scroll to bottom — confirm "Instructor Note (optional)" textarea is present
    3. Confirm "Visible to students" checkbox is present
    4. Type a test note: "This question tests VLAN trunking."
    5. Check the "Visible to students" toggle ON
    6. Save the question
    7. Re-open the same question for editing — confirm note text and toggle state persist

    **Test NOTE-04 (instructor always sees note):**
    8. With a note saved, uncheck "Visible to students" and save
    9. Re-open the question for editing — confirm note text still visible in textarea (even with toggle OFF)

    **Test NOTE-03a (TrainingMode):**
    10. Start a Training session on the pool
    11. Answer the question with the note (toggle must be ON from step 5; if not, re-enable it)
    12. After answering, confirm the note appears in the feedback (blue NcNoteCard with "Note:" prefix)
    13. For a question with note_visible=false, confirm no note card appears after answering

    **Test NOTE-03b (LeitnerMode and/or SmartQueue):**
    14. Navigate to Leitner or SmartQueue, answer the same question
    15. Confirm note appears in feedback when note_visible=true

    **Test NOTE-03c (ExamMode):**
    16. Start an Exam session, complete it (or answer a few questions and submit)
    17. In the post-exam Detailed Review, confirm note appears per question when note_visible=true
    18. Confirm note does NOT appear for questions with note_visible=false or no note set

    All tests pass when:
    - Note textarea + toggle visible in QuestionForm regardless of toggle state
    - Note persists across save/reload cycles
    - Note visible in all 4 mode feedback blocks when note_visible=true
    - Note NOT visible when note_visible=false
    - No raw HTML rendered (note text appears as plain text)
  </action>
  <done>All browser checks pass: QuestionForm editor shows note fields, note persists, note visible in all 4 learning modes when note_visible=true, hidden when false, no v-html.</done>
</task>

</tasks>

<verification>
1. `npm test` — all tests pass including instructorNote.test.js
2. `npm run build` — no errors in all 5 modified components
3. Browser: QuestionForm shows instructor_note textarea and note_visible checkbox
4. Browser: Note visible in TrainingMode feedback when note_visible=true
5. Browser: Note hidden in all modes when note_visible=false
6. Browser: ExamMode post-exam review shows note per question when note_visible=true
7. No v-html usage: `grep -r "v-html" app/src/components/` — no new matches introduced
</verification>

<success_criteria>
- NOTE-02: QuestionForm.vue has instructor_note textarea and NcCheckboxRadioSwitch toggle, always rendered (not behind v-if)
- NOTE-03: All 4 learning mode components show NcNoteCard type="info" with note text when `note_visible && instructor_note` evaluates truthy
- NOTE-04 (frontend half): QuestionForm always renders the instructor_note field regardless of noteVisible state — instructors always see their own note during editing
- No v-html in any note display — plain text {{ }} interpolation only
- ExamMode uses `q.instructor_note`/`q.note_visible` from `this.questions` array (already loaded at exam start), not from sortedDetailedResults which lacks these fields
- Browser checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/06-instructor-notes/06-02-SUMMARY.md`
</output>
