---
phase: 04-multi-panel-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tests/unit/pbqMultiPanel.test.js
  - app/src/components/PbqMultiPanel.vue
  - app/src/components/PbqRenderer.vue
autonomous: false
requirements:
  - PANEL-01
  - PANEL-02
must_haves:
  truths:
    - "multi_panel subtype zeigt PbqCli links und PbqPlacement rechts nebeneinander"
    - "Responsive: auf Screens unter 768px werden die Panels untereinander gestapelt"
    - "CLI-Terminal ist vollständig interaktiv (Eingabe, Enter, State-Machine-Output)"
    - "SVG-Topologie ist klickbar — Inline-Picker öffnet sich an der Node-Position"
    - "PbqRenderer-Footer zeigt korrekte Zahl für totalCount (CLI-Terminals + Placement-Positionen)"
    - "answer-Objekt enthält cli- und placement-Namespace unabhängig voneinander"
  artifacts:
    - path: "app/src/components/PbqMultiPanel.vue"
      provides: "Thin composition wrapper für CLI + Topology side-by-side"
      exports: ["PbqMultiPanel"]
    - path: "app/tests/unit/pbqMultiPanel.test.js"
      provides: "Unit tests für PANEL-01 und PANEL-02"
      contains: "pbqMultiPanel"
    - path: "app/src/components/PbqRenderer.vue"
      provides: "v-else-if branch für multi_panel + totalCount/answeredCount Erweiterung"
      contains: "multi_panel"
  key_links:
    - from: "app/src/components/PbqRenderer.vue"
      to: "app/src/components/PbqMultiPanel.vue"
      via: "v-else-if subtype === 'multi_panel'"
      pattern: "multi_panel"
    - from: "app/src/components/PbqMultiPanel.vue"
      to: "app/src/components/PbqCli.vue"
      via: "@update='onCliUpdate' -> $emit('update', 'cli', cliVal)"
      pattern: "onCliUpdate"
    - from: "app/src/components/PbqMultiPanel.vue"
      to: "app/src/components/PbqPlacement.vue"
      via: "@update='onPlacementUpdate' -> $emit('update', 'placement', placementVal)"
      pattern: "onPlacementUpdate"
---

<objective>
Erstellt PbqMultiPanel.vue als thin composition wrapper der PbqCli und PbqPlacement nebeneinander darstellt, verdrahtet ihn in PbqRenderer.vue und ergänzt Tests für PANEL-01/PANEL-02.

Purpose: Ermöglicht Fragen die gleichzeitig ein CLI-Terminal und eine SVG-Netzwerktopologie zeigen — das ist das OnVUE-Niveau Split-View Feature.
Output: PbqMultiPanel.vue, PbqRenderer.vue update, pbqMultiPanel.test.js
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-panel-layout/04-RESEARCH.md
@.planning/phases/04-multi-panel-layout/04-VALIDATION.md

<interfaces>
<!-- Extracted from existing components — executor braucht keine Codebase-Exploration -->

From app/src/components/PbqCli.vue:
- Props: config (Object, required), value (Object, default {}), disabled (Boolean, default false)
- Emits: 'update', termName (String), history (Array) — z.B. this.$emit('update', term.name, [...history])
- config.terminals: Array of { name, initial_prompt? }
- config.domain: String (cisco_ios/linux/windows/sql/generic)
- config.hint: String (optional)

From app/src/components/PbqPlacement.vue:
- Props: config (Object, required), value (Object, default {}), disabled (Boolean, default false),
         scenarioImage (String, default null), topologyConfig (Object, default null)
- Emits: 'update', posId (String), device (String) — z.B. this.$emit('update', posId, device)
- config.positions: Array of { id, label, correct?, x_pct?, y_pct? }
- config.device_options: Array of String
- config.scoring_mode: 'strict' | 'partial'

From app/src/components/PbqRenderer.vue:
- totalCount computed: switch on subtype — case 'cli': (cfg.terminals||[]).length, case 'placement': (cfg.positions||[]).length, case 'cable': (cfg.questions||[]).length * 2
- answeredCount computed: Object.keys(this.localAnswer).length
- onUpdate(key, value): this.$set(this.localAnswer, String(key), value) — PANEL-01 erfordert Namespace-Keys 'cli' und 'placement'

Config-Schema für multi_panel (neu, definiert in Phase 4):
```json
{
  "pbq_subtype": "multi_panel",
  "pbq_config": {
    "cli": { "domain": "cisco_ios", "terminals": [...], "command_outputs": {} },
    "placement": { "positions": [...], "device_options": [...], "scoring_mode": "strict" },
    "topology": { "nodes": [...], "links": [...] }
  }
}
```

Answer-Struktur für multi_panel:
```json
{
  "cli": { "Router": ["Router> show ip route", "..."] },
  "placement": { "node1": "router", "node2": "switch" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-Stub für PbqMultiPanel erstellen (Wave 0)</name>
  <files>app/tests/unit/pbqMultiPanel.test.js</files>
  <behavior>
    - Test 1 (PANEL-01): onCliUpdate emittiert 'update' mit key='cli' und gemergtem cli-Wert
    - Test 2 (PANEL-01): onPlacementUpdate emittiert 'update' mit key='placement' und gemergtem placement-Wert
    - Test 3 (PANEL-01): config.cli und config.placement werden korrekt an die Kind-Komponenten weitergeleitet (props-check)
    - Test 4 (PANEL-02): Klasse 'pbq-multi-panel' ist am Root-Element vorhanden (CSS-Breakpoint wird per CSS-Media gesteuert, Unit-Test prüft nur das Vorhandensein der Klasse)
    - Test 5 (PANEL-01): answeredCount-Hilfsfunktion: summiert cli-Keys + placement-Keys aus verschachteltem localAnswer korrekt
  </behavior>
  <action>
    Erstelle `app/tests/unit/pbqMultiPanel.test.js` als pure-unit Test-File ohne DOM-Mounting (da kein vue-test-utils im Vitest-Setup vorhanden — alle bestehenden Tests in tests/unit/ testen pure JS-Utilities, keine Vue-Komponenten direkt).

    Da die Tests keine Vue-Komponenten mounten (das Projekt testet nur pure-JS utils), teste stattdessen:
    1. Die Hilfslogik aus PbqMultiPanel direkt als isolierte Funktionen — extrahiere die onCliUpdate und onPlacementUpdate Merge-Logik als testbare Pure Functions in die Komponente (via kommentierter Export-Funktion oder als separates utility).
    2. ODER: Teste die Merge-Logik inline als reine JS-Objekt-Operationen die PbqMultiPanel intern nutzt.

    Konkrete Test-Struktur:
    ```javascript
    import { describe, it, expect } from 'vitest'

    // Pure-function helpers mirroring PbqMultiPanel's internal merge logic
    function mergeCliUpdate(currentValue, termName, history) {
      return { ...(currentValue.cli || {}), [termName]: history }
    }

    function mergePlacementUpdate(currentValue, posId, device) {
      return { ...(currentValue.placement || {}), [posId]: device }
    }

    function multiPanelAnsweredCount(localAnswer) {
      return Object.keys(localAnswer.cli || {}).length + Object.keys(localAnswer.placement || {}).length
    }

    function multiPanelTotalCount(config) {
      const cliTerms = (config.cli && config.cli.terminals || []).length
      const placementPos = (config.placement && config.placement.positions || []).length
      return cliTerms + placementPos
    }

    describe('PbqMultiPanel merge logic', () => {
      it('PANEL-01: mergeCliUpdate fügt terminal-history unter cli-namespace ein', ...)
      it('PANEL-01: mergePlacementUpdate fügt device-assignment unter placement-namespace ein', ...)
      it('PANEL-01: mergeCliUpdate überschreibt existierenden cli-Key ohne andere Keys zu verlieren', ...)
      it('PANEL-01: multiPanelAnsweredCount summiert cli + placement sub-keys', ...)
      it('PANEL-01: multiPanelTotalCount summiert terminals + positions', ...)
      it('PANEL-02: CSS-class pbq-multi-panel erwartet flex-direction row per Convention', ...)
    })
    ```

    Die Helper-Funktionen werden im Test definiert — sie spiegeln exakt die Logik wider die PbqMultiPanel.vue implementieren wird (onCliUpdate, onPlacementUpdate, answeredCount/totalCount branches in PbqRenderer). Tests MÜSSEN rot sein wenn die Logik falsch ist.

    KEIN v-html. KEIN Vue-Component-Mounting.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>app/tests/unit/pbqMultiPanel.test.js existiert, alle Tests grün, npm test exit code 0</done>
</task>

<task type="auto">
  <name>Task 2: PbqMultiPanel.vue erstellen + PbqRenderer.vue erweitern</name>
  <files>app/src/components/PbqMultiPanel.vue, app/src/components/PbqRenderer.vue</files>
  <action>
    **PbqMultiPanel.vue** — neue Datei erstellen:

    Template-Struktur:
    ```vue
    <template>
      <div class="pbq-multi-panel">
        <div class="pbq-panel pbq-panel--cli">
          <PbqCli
            :config="config.cli || {}"
            :value="value.cli || {}"
            :disabled="disabled"
            @update="onCliUpdate"
          />
        </div>
        <div class="pbq-panel pbq-panel--topology">
          <PbqPlacement
            :config="config.placement || {}"
            :value="value.placement || {}"
            :disabled="disabled"
            :topology-config="config.topology || null"
            @update="onPlacementUpdate"
          />
        </div>
      </div>
    </template>
    ```

    Script:
    - name: 'PbqMultiPanel'
    - components: { PbqCli, PbqPlacement }
    - Props: config (Object, required), value (Object, default () => ({})), disabled (Boolean, default false)
    - onCliUpdate(termName, history): const cliVal = { ...(this.value.cli || {}), [termName]: history }; this.$emit('update', 'cli', cliVal)
    - onPlacementUpdate(posId, device): const placementVal = { ...(this.value.placement || {}), [posId]: device }; this.$emit('update', 'placement', placementVal)

    Style scoped — CSS Flexbox split mit PANEL-02 responsive breakpoint:
    ```css
    .pbq-multi-panel {
      display: flex;
      flex-direction: row;
      gap: 16px;
      align-items: flex-start;
      /* KEIN overflow: hidden — PbqPlacement's inline-picker nutzt position:absolute */
    }
    .pbq-panel {
      flex: 1;
      min-width: 0; /* KRITISCH: verhindert horizontal overflow in flex-children */
    }
    @media (max-width: 768px) {
      .pbq-multi-panel {
        flex-direction: column;
      }
    }
    ```

    KEIN v-html. KEIN overflow:hidden auf .pbq-multi-panel.

    ---

    **PbqRenderer.vue** — drei gezielte Änderungen:

    1. Import hinzufügen (nach PbqCable-Import-Zeile):
       ```javascript
       import PbqMultiPanel from './PbqMultiPanel.vue'
       ```

    2. components-Objekt erweitern:
       ```javascript
       components: { NcButton, PbqDropdown, PbqPlacement, PbqCli, PbqCable, PbqMultiPanel },
       ```

    3. Template: Nach dem PbqCable v-else-if Block, vor dem schließenden </div> vor .pbq-footer, neuen Branch einfügen:
       ```vue
       <PbqMultiPanel
         v-else-if="subtype === 'multi_panel'"
         :config="config"
         :value="localAnswer"
         :disabled="disabled"
         @update="onUpdate"
       />
       ```
       Hinweis: onUpdate(key, value) ruft this.$set(this.localAnswer, String(key), value) auf — das funktioniert direkt für 'cli' und 'placement' als Keys.

    4. totalCount computed — multi_panel case ergänzen:
       ```javascript
       case 'multi_panel': {
         const cliTerms = (cfg.cli && cfg.cli.terminals || []).length
         const placementPos = (cfg.placement && cfg.placement.positions || []).length
         return cliTerms + placementPos
       }
       ```

    5. answeredCount computed — multi_panel branch ergänzen (ersetze die einzeilige computed durch eine mit multi_panel-Branch):
       ```javascript
       answeredCount() {
         if (this.subtype === 'multi_panel') {
           return Object.keys(this.localAnswer.cli || {}).length
             + Object.keys(this.localAnswer.placement || {}).length
         }
         return Object.keys(this.localAnswer).length
       },
       ```

    Alle anderen PbqRenderer-Bereiche bleiben unverändert.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
    PbqMultiPanel.vue existiert mit Flexbox-Layout und korrekten emit-Methoden.
    PbqRenderer.vue enthält multi_panel branch, totalCount case und answeredCount branch.
    npm test grün (alle Tests pass).
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + Deploy auf learning-dev</name>
  <files>app/js/ (webpack output)</files>
  <action>
    Deploy-Sequenz gemäß CLAUDE.md:

    1. rsync source auf learning-dev:
       ```bash
       rsync -avz --exclude node_modules --exclude .git /home/andre/Workspace/Code/learning-nc/app/ learning-dev:~/learning-nc/app/
       ```

    2. Frontend bauen auf learning-dev:
       ```bash
       ssh learning-dev 'cd ~/learning-nc/app && npm run build'
       ```

    3. JS-Bundle in Container kopieren:
       ```bash
       ssh learning-dev 'cd ~/learning-nc/app && tar cf /tmp/js-bundle.tar js/'
       ssh learning-dev 'docker cp /tmp/js-bundle.tar learning-app:/tmp/'
       ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && tar xf /tmp/js-bundle.tar"'
       ```

    Kein PHP-Deploy nötig (nur Vue-Änderungen).
  </action>
  <verify>
    <automated>ssh learning-dev 'docker exec learning-app ls /var/www/html/custom_apps/learning/js/learning.js' 2>&1</automated>
  </verify>
  <done>Build erfolgreich, learning.js im Container aktualisiert (kein webpack error)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser-Verifikation Multi-Panel Layout</name>
  <action>Browser-seitige Verifikation von PANEL-01 (Side-by-side) und PANEL-02 (Responsive). Alle Schritte in how-to-verify durchführen.</action>
  <what-built>
    PbqMultiPanel.vue: Flexbox-Split mit PbqCli links + PbqPlacement (SVG-Topologie) rechts.
    PbqRenderer.vue: neuer multi_panel branch, totalCount + answeredCount erweitert.
    Build deployed auf learning-dev.
  </what-built>
  <how-to-verify>
    1. Öffne http://learning-dev:8080 im Browser und navigiere zu einer PBQ-Frage mit pbq_subtype='multi_panel'.
       Falls keine existiert, lege eine Test-Frage an mit folgendem pbq_config:
       ```json
       {
         "cli": {
           "domain": "cisco_ios",
           "terminals": [{ "name": "Router" }],
           "command_outputs": {}
         },
         "placement": {
           "positions": [{ "id": "n1", "label": "Core Switch", "correct": "switch" }],
           "device_options": ["router", "switch", "firewall"]
         },
         "topology": {
           "nodes": [{ "id": "n1", "type": "switch", "label": "Core", "x": 200, "y": 150 }],
           "links": []
         }
       }
       ```

    2. Prüfe PANEL-01 — Side-by-side Layout:
       - [ ] CLI-Terminal erscheint links (dunkler Hintergrund, Prompt sichtbar)
       - [ ] SVG-Topologie erscheint rechts (Node-Icon sichtbar)
       - [ ] Beide Panels nebeneinander in einer Zeile (nicht untereinander)

    3. Prüfe PANEL-01 — Interaktivität:
       - [ ] Tippe einen CLI-Befehl (z.B. `show version`) ins Terminal und drücke Enter — Output erscheint
       - [ ] Klicke auf einen SVG-Node rechts — Inline-Picker erscheint direkt am Node
       - [ ] Wähle ein Gerät im Picker — Summary unten rechts zeigt die Zuweisung

    4. Prüfe PANEL-01 — Footer:
       - [ ] "X / Y beantwortet" Footer zeigt sinnvolle Zahl (nicht "0 / 0")

    5. Prüfe PANEL-02 — Responsive:
       - [ ] Verkleinere das Browser-Fenster unter 768px Breite (DevTools: Responsive Mode)
       - [ ] Panels stapeln sich untereinander (CLI oben, Topologie unten)
       - [ ] Auf breitem Screen kehrt der nebeneinander-Layout zurück
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alle Checks bestanden — oder beschreibe gefundene Probleme</resume-signal>
</task>

</tasks>

<verification>
npm test muss vollständig grün sein (alle bestehenden + neue Tests).
Build muss ohne webpack-Fehler durchlaufen.
Browser-Checkpoint muss PANEL-01 und PANEL-02 bestätigen.
</verification>

<success_criteria>
1. npm test grün: pbqMultiPanel.test.js Tests pass, alle anderen Tests unverändert grün
2. PbqMultiPanel.vue existiert: Flexbox-split, keine overflow:hidden, kein v-html, min-width:0 auf .pbq-panel
3. PbqRenderer.vue: multi_panel branch aktiv, totalCount addiert cli-terminals + placement-positions, answeredCount addiert cli-sub-keys + placement-sub-keys
4. Browser-Checkpoint approved: Side-by-side sichtbar, beide Panels interaktiv, responsive stacking unter 768px
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/04-multi-panel-layout/04-01-SUMMARY.md` erstellen.
</output>
