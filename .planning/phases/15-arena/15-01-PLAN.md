---
phase: 15-arena
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ArenaSelector.vue
  - app/src/components/CourseDetail.vue
  - app/src/App.vue
autonomous: true
requirements:
  - ARENA-01
  - ARENA-02
  - ARENA-03
  - ARENA-04

must_haves:
  truths:
    - "CourseDetail zeigt einen einzigen 'Arena'-Tab statt separater 'Duell'- und 'Gameshow'-Tabs"
    - "Innerhalb des Arena-Tabs sieht der User drei Karten: Duell (1v1), Sprint (2-5), Elimination (2-5)"
    - "Jede Karte hat ein Icon, einen Titel und eine Kurzbeschreibung"
    - "Klick auf eine Karte startet direkt den jeweiligen Lobby/Start-Flow ohne zusaetzlichen Navigationsschritt"
    - "Der Back-Button in DuelMode und GameshowMode navigiert zurueck zum Arena-Tab"
  artifacts:
    - path: "app/src/components/ArenaSelector.vue"
      provides: "Drei-Karten-Auswahl fuer Wettkampfmodi"
      exports: ["ArenaSelector"]
    - path: "app/src/components/CourseDetail.vue"
      provides: "Ersetzter Arena-Tab statt duel+gameshow, plus ArenaSelector-Integration"
      contains: "currentTab === 'arena'"
  key_links:
    - from: "app/src/components/ArenaSelector.vue"
      to: "CourseDetail arena-tab div"
      via: "emit 'select-mode' event mit value 'duel'|'sprint'|'elimination'"
      pattern: "select-mode"
    - from: "app/src/components/CourseDetail.vue"
      to: "DuelMode / GameshowMode"
      via: "arenaSubMode data property"
      pattern: "arenaSubMode"
---

<objective>
Duell und Gameshow unter einem gemeinsamen "Arena"-Tab zusammenfuehren. Ein neues ArenaSelector.vue zeigt drei Karten (Duell, Sprint, Elimination) und delegiert direkt in den jeweiligen Lobby-Flow.

Purpose: 10+ Tabs in CourseDetail reduzieren, Modi-Verwirrung minimieren, Wettkampf-Einstiegspunkt vereinheitlichen.
Output: ArenaSelector.vue (neu), CourseDetail.vue (duel+gameshow Tabs ersetzt durch arena), App.vue (standalone gameshow route angepasst).
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/src/components/CourseDetail.vue
@app/src/App.vue
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

CourseDetail.vue — betroffene Stellen (Zeilennummern approximiert):

Tab-Definitionen in visibleTabs() computed:
```javascript
// Instructor-Zweig (Zeile ~1193):
{ id: 'duel', label: t('learning', 'Duell') },
{ id: 'gameshow', label: t('learning', 'Gameshow') },

// Student-Zweig (Zeile ~1222):
if (enabled('duel')) tabs.push({ id: 'duel', label: t('learning', 'Duell') })
tabs.push({ id: 'gameshow', label: t('learning', 'Gameshow') })
```

Tab-Inhalte (Zeile ~588):
```html
<!-- Duel Tab -->
<div v-if="currentTab === 'duel'" class="duel-section">
  <DuelMode
    :courseId="courseId"
    :coursePools="coursePools"
    :presetDuelCode="presetDuelCode"
    :hideJoinScreen="Boolean(presetDuelCode)"
    :contentLanguage="contentLanguage"
    @preset-consumed="$emit('clearPresetDuel')"
    @back="currentTab = isInstructor ? 'pools' : 'training'" />
</div>
<!-- Gameshow Tab -->
<div v-if="currentTab === 'gameshow'" class="gameshow-section">
  <GameshowMode
    :courseId="courseId"
    :coursePools="coursePools"
    :contentLanguage="contentLanguage"
    @back="currentTab = isInstructor ? 'pools' : 'training'" />
</div>
```

Watcher fuer presetDuelCode (Zeile ~1350):
```javascript
presetDuelCode(newCode) {
  if (newCode) {
    this.currentTab = 'duel'  // muss zu 'arena' werden
  }
},
```

VirtuProf-Context-Emitter (Zeile ~1369):
```javascript
} else if (this.currentTab === 'duel') {
  area = 'course-duel'
} else if (this.currentTab === 'gameshow') {
  area = 'course-gameshow'
}
```

isStudentLearningTab computed (Zeile ~1178):
```javascript
isStudentLearningTab() {
  return !this.isInstructor && ['training', 'leitner', 'swipe', 'exam'].includes(this.currentTab)
}
```

App.vue — Standalone-Gameshow (Zeile ~117):
```html
<GameshowMode
  v-else-if="mode === 'gameshow'"
  :initialPoolId="selectedPool.id"
  :contentLanguage="contentLanguage"
  @back="setMode('train')"
/>
```
App.vue modes-Array (Zeile ~295):
```javascript
{ id: 'gameshow', label: t('learning', 'Gameshow') },
```
App.vue modeDescriptions (Zeile ~312):
```javascript
gameshow: t('learning', 'Multiplayer quiz — compete with others in a live gameshow.'),
```

GameshowMode.vue akzeptiert als :gameMode Prop: 'sprint' | 'elimination' (default: 'sprint')
DuelMode.vue hat keinen gameMode-Prop — steuert Duell intern.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ArenaSelector.vue erstellen</name>
  <files>app/src/components/ArenaSelector.vue</files>
  <action>
Neue Vue 2.7 SFC erstellen. Zeigt drei Karten in einem Grid-Layout (3 Spalten auf Desktop, 1 auf Mobile via CSS).

Karten-Daten (hardcoded im template, kein Backend):
1. Duell — Icon: ⚔️ — Titel: "Duell (1v1)" — Beschreibung: t('learning', 'Fordere einen Mitspieler zum direkten Duell heraus.')
2. Sprint — Icon: ⚡ — Titel: "Sprint (2–5)" — Beschreibung: t('learning', 'Schnellster korrekter Antwortgeber gewinnt.')
3. Elimination — Icon: 💀 — Titel: "Elimination (2–5)" — Beschreibung: t('learning', 'Falsche Antwort kostet ein Leben — wer zuletzt steht, gewinnt.')

Jede Karte ist ein `<button>` mit class `arena-card`. Klick emittet `select-mode` mit dem jeweiligen String-Wert: 'duel', 'sprint', 'elimination'.

CSS direkt in `<style scoped>`:
- `.arena-selector`: display grid, grid-template-columns repeat(3, 1fr), gap 16px
- `.arena-card`: border 1px solid var(--color-border), border-radius 12px, padding 24px, cursor pointer, text-align center, background var(--color-background-hover), transition background 0.2s
- `.arena-card:hover`: background var(--color-primary-light, #e8f4ff)
- `.arena-icon`: font-size 2.5rem, display block, margin-bottom 8px
- `.arena-title`: font-weight bold, font-size 1.1rem, margin-bottom 4px
- `.arena-desc`: font-size 0.85rem, color var(--color-text-maxcontrast)
- `@media (max-width: 600px)`: .arena-selector grid-template-columns 1fr

Kein Props. Kein State. Rein deklarativ. `prefers-reduced-motion` muss beachtet werden — keine CSS transitions wenn prefers-reduced-motion: reduce.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && grep -l "arena-selector\|select-mode\|ArenaSelector" src/components/ArenaSelector.vue</automated>
  </verify>
  <done>ArenaSelector.vue existiert, emittet 'select-mode' mit 'duel'/'sprint'/'elimination', zeigt drei Karten mit Icon+Titel+Beschreibung.</done>
</task>

<task type="auto">
  <name>Task 2: CourseDetail.vue — Tab-Merge + ArenaSelector-Integration</name>
  <files>app/src/components/CourseDetail.vue</files>
  <action>
Folgende Aenderungen in CourseDetail.vue:

**1. Import hinzufuegen** (oben im script, bei den anderen Component-Imports):
```javascript
import ArenaSelector from './ArenaSelector.vue'
```

**2. components-Block ergaenzen**:
```javascript
ArenaSelector,
```

**3. data() — neues Property hinzufuegen**:
```javascript
arenaSubMode: null,  // 'duel' | 'sprint' | 'elimination' | null
```

**4. visibleTabs() — Instructor-Zweig**: Die zwei separaten Eintraege `{ id: 'duel' }` und `{ id: 'gameshow' }` durch einen ersetzen:
```javascript
{ id: 'arena', label: t('learning', 'Arena') },
```
Platzierung: an derselben Stelle wie bisher 'duel' stand (nach 'league', vor 'curriculum').

**5. visibleTabs() — Student-Zweig**: Die bedingten `if (enabled('duel'))` und `tabs.push({ id: 'gameshow' })` Zeilen ersetzen durch:
```javascript
tabs.push({ id: 'arena', label: t('learning', 'Arena') })
```
Hinweis: Arena-Tab immer sichtbar (kein mode_config-Gate noetig — die Submode-Gates bleiben intern bei DuelMode/GameshowMode).

**6. Tab-Inhalte**: Den `duel-section` und `gameshow-section` Block ersetzen durch:
```html
<!-- Arena Tab -->
<div v-if="currentTab === 'arena'" class="arena-section">
  <ArenaSelector
    v-if="arenaSubMode === null"
    @select-mode="onArenaSelectMode" />
  <DuelMode
    v-else-if="arenaSubMode === 'duel'"
    :courseId="courseId"
    :coursePools="coursePools"
    :presetDuelCode="presetDuelCode"
    :hideJoinScreen="Boolean(presetDuelCode)"
    :contentLanguage="contentLanguage"
    @preset-consumed="$emit('clearPresetDuel')"
    @back="arenaSubMode = null" />
  <GameshowMode
    v-else-if="arenaSubMode === 'sprint'"
    :courseId="courseId"
    :coursePools="coursePools"
    :contentLanguage="contentLanguage"
    :gameMode="'sprint'"
    @back="arenaSubMode = null" />
  <GameshowMode
    v-else-if="arenaSubMode === 'elimination'"
    :courseId="courseId"
    :coursePools="coursePools"
    :contentLanguage="contentLanguage"
    :gameMode="'elimination'"
    @back="arenaSubMode = null" />
</div>
```

**7. Method hinzufuegen** (in methods-Block):
```javascript
onArenaSelectMode(mode) {
  this.arenaSubMode = mode
},
```

**8. selectTab-Method erweitern**: Wenn Tab wechselt, arenaSubMode zuruecksetzen:
```javascript
selectTab(tabId) {
  this.currentTab = tabId
  if (tabId !== 'arena') {
    this.arenaSubMode = null
  }
  if (tabId === 'league' && !this.isInstructor) {
    this.$root.$emit('virtuprof:trigger', 'liga-first-visit')
  }
},
```

**9. presetDuelCode-Watcher**: `this.currentTab = 'duel'` → `this.currentTab = 'arena'; this.arenaSubMode = 'duel'`

**10. VirtuProf emitVirtuProfContext**: duel/gameshow Branches auf arena umstellen:
```javascript
} else if (this.currentTab === 'arena') {
  area = this.arenaSubMode ? `course-${this.arenaSubMode}` : 'course-arena'
```

**11. modeConfigKeys**: 'duel' und 'gameshow' Eintraege belassen (Backend unveraendert), aber ggf. Labels anpassen zu 'Duell (Arena)' und 'Gameshow (Arena)' — nur Anzeige-Label, kein Daten-Impact.

**Nicht anfassen**: Alle anderen Tabs (training, leitner, swipe, exam, my-progress, leaderboard, league, curriculum, heatmap, etc.) bleiben unveraendert.
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && grep -c "currentTab === 'arena'" src/components/CourseDetail.vue</automated>
  </verify>
  <done>CourseDetail.vue hat keinen 'duel'- und keinen 'gameshow'-Tab mehr in visibleTabs. Einziger Einstiegspunkt fuer Wettkampfmodi ist 'arena'. ArenaSelector wird gerendert wenn arenaSubMode null ist.</done>
</task>

<task type="auto">
  <name>Task 3: App.vue Standalone anpassen + Build verifizieren</name>
  <files>app/src/App.vue</files>
  <action>
In App.vue (Pool-Standalone-View) den Gameshow-Modus-Eintrag aktualisieren:

**1. modes-Array** (Zeile ~295): Den Eintrag `{ id: 'gameshow', label: t('learning', 'Gameshow') }` durch zwei Eintraege ersetzen:
```javascript
{ id: 'gameshow-sprint', label: t('learning', 'Sprint (Gameshow)') },
{ id: 'gameshow-elimination', label: t('learning', 'Elimination (Gameshow)') },
```
Alternativ (einfacher): Den bestehenden Eintrag belassen und nur das Label anpassen zu `t('learning', 'Arena — Sprint')`. Waehlbar ist, was weniger Risiko hat — belasse 'gameshow' als id aber aendere Label zu 'Sprint / Elimination'.

**Empfehlung**: Minimalster Eingriff. Bestehende `mode === 'gameshow'` Logik NICHT aendern. Nur das Label im modes-Array auf `t('learning', 'Arena — Gameshow')` updaten. Die Standalone-View hat keine Karten-Auswahl (ArenaSelector passt dort nicht hin — es fehlt ein courseId-Kontext). Das ist akzeptabel fuer Standalone.

**2. modeDescriptions**: 'gameshow' Label anpassen:
```javascript
gameshow: t('learning', 'Sprint oder Elimination — konkurriere live mit anderen Spielern.'),
```

**3. Sicherheits-Check**: `grep -n "mode === 'duel'" app/src/App.vue` — DuelMode hat in App.vue keinen eigenen standalone mode (Duelle sind kurs-gebunden), also gibt es dort nichts zu aendern.

Nach den Aenderungen Build ausfuehren:
```bash
ssh learning-dev 'cd ~/learning-nc/app && npm run build 2>&1 | tail -20'
```

Build muss ohne Fehler durchlaufen. Warnings sind ok. Fehler sind nicht ok.

Wenn Build erfolgreich: JS-Bundle auf learning-dev deployen:
```bash
ssh learning-dev 'cd ~/learning-nc/app && tar cf /tmp/js-bundle.tar js/'
ssh learning-dev 'docker cp /tmp/js-bundle.tar learning-app:/tmp/'
ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && tar xf /tmp/js-bundle.tar"'
```
  </action>
  <verify>
    <automated>ssh learning-dev 'cd ~/learning-nc/app && npm run build 2>&1 | grep -E "ERROR|error TS|Cannot find" | head -10'</automated>
  </verify>
  <done>Build laeuft ohne Fehler durch. JS-Bundle ist auf learning-dev deployed. App.vue spiegelt den neuen Arena-Namen fuer den Gameshow-Eintrag.</done>
</task>

</tasks>

<verification>
Nach Abschluss aller Tasks:

1. Manuelle Pruefung auf http://192.168.178.65:8080 (learning-dev):
   - Als Student: CourseDetail oeffnen → Tab-Leiste zeigt "Arena" statt "Duell" und "Gameshow"
   - Arena-Tab klicken → drei Karten sichtbar (Duell, Sprint, Elimination)
   - Duell-Karte klicken → DuelMode startet direkt (Lobby/Join-Screen)
   - Zurueck-Button in DuelMode → ArenaSelector erscheint wieder
   - Sprint-Karte klicken → GameshowMode mit Sprint startet
   - Als Instructor: Arena-Tab ebenfalls sichtbar (statt separater Tabs)

2. Automatisiert:
   ```bash
   grep -c "id: 'duel'" /home/andre/Workspace/Code/learning-nc/app/src/components/CourseDetail.vue
   # Ergebnis: 0 (kein 'duel' Tab mehr in visibleTabs)
   grep -c "id: 'arena'" /home/andre/Workspace/Code/learning-nc/app/src/components/CourseDetail.vue
   # Ergebnis: mindestens 2 (instructor + student Zweig)
   ```
</verification>

<success_criteria>
- ARENA-01: Ein einziger 'Arena'-Tab in CourseDetail (instructor + student) statt 'Duell' + 'Gameshow'
- ARENA-02: Tab heisst "Arena" (t('learning', 'Arena'))
- ARENA-03: Drei Wettkampf-Karten sichtbar: Duell (1v1), Sprint (2-5), Elimination (2-5)
- ARENA-04: Karten haben Icon + Titel + Kurzbeschreibung, ansprechendes Card-Layout
- Build ohne Fehler, JS deployed auf learning-dev
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/15-arena/15-01-SUMMARY.md` erstellen mit:
- Welche Dateien geaendert wurden
- Wie ArenaSelector mit CourseDetail verdrahtet ist (emit-Fluss)
- Bekannte Abweichungen vom Plan (falls vorhanden)
</output>
