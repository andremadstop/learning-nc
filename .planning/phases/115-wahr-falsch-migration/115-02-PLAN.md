---
phase: 115-wahr-falsch-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/TrainingMode.vue
  - app/src/components/CourseTabLernraum.vue
  - app/src/components/CourseTabVerwaltung.vue
  - app/src/components/VirtuProf.vue
  - app/tests/unit/CourseDetail.test.js
  - app/tests/unit/CourseTabLernraum.test.js
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "TrainingMode.vue renders all questions as MC cards — no toggle button, no swipe card block"
    - "CourseTabVerwaltung.vue mode_config section has no Wahr/Falsch swipe checkbox"
    - "VirtuProf.vue has no swipe-area context branches (course-swipe-pool-select, course-swipe-active, pool-swipe)"
    - "Vitest suite passes after UI removal (no broken swipe assertions)"
    - "ESLint reports zero errors after removal"
  artifacts:
    - path: "app/src/components/TrainingMode.vue"
      provides: "MC-only training mode with no WF toggle or swipe card block"
      min_lines: 1
    - path: "app/src/components/CourseTabLernraum.vue"
      provides: "TrainingMode binding without :allowWfMode prop"
      min_lines: 1
    - path: "app/src/components/CourseTabVerwaltung.vue"
      provides: "mode_config list without swipe entry"
      min_lines: 1
    - path: "app/src/components/VirtuProf.vue"
      provides: "Context switch without swipe-area branches"
      min_lines: 1
  key_links:
    - from: "CourseTabLernraum.vue"
      to: "TrainingMode.vue"
      via: "<TrainingMode> component binding — :allowWfMode must be absent"
      pattern: "allowWfMode"
    - from: "CourseTabVerwaltung.vue"
      to: "mode_config object"
      via: "mode toggle list — swipe key must be absent"
      pattern: "swipe"
---

<objective>
Remove the Wahr/Falsch swipe-mode UI from TrainingMode.vue (toggle buttons + swipe card block + props/data/methods), clean up cascading bindings in CourseTabLernraum.vue and CourseTabVerwaltung.vue, remove dead VirtuProf context strings for swipe areas, and update two test files that assert the removed feature.

Purpose: UX-02 requires no separate UI path for Wahr/Falsch. The swipe card block in TrainingMode.vue is exactly that separate UI path. After removal, all pools (including those with only Wahr/Falsch answers) render as standard single-choice MC cards.
Output: Cleaned Vue components + passing Vitest suite
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-wahr-falsch-migration/115-CONTEXT.md
@.planning/phases/115-wahr-falsch-migration/115-RESEARCH.md

<interfaces>
<!-- TrainingMode.vue — what to remove vs keep:
REMOVE: <div v-if="allowWfMode" class="mode-toggle"> block (MC/WF toggle buttons, lines ~7-14)
REMOVE: <div v-if="localWfMode && currentQuestion" class="card-stack"> block (swipe card, lines ~37-81)
REMOVE: props wfMode and allowWfMode
REMOVE: data property localWfMode + all localWfMode-dependent watchers
REMOVE: swipe drag state: cardAnimClass, canDrag, isDragging, pointerDown, dragStartX, dragStartY, dragDeltaX, showFeedback
REMOVE: methods selectWfAnswer, advanceWfCard, onPointerDown, onPointerMove, onPointerUp
REMOVE: CSS for .swipe-card, .swipe-hint-area, .swipe-hint, .card-dragging, .wf-btn, .wf-buttons
KEEP:   the MC card block — change <div v-if="!localWfMode && currentQuestion"> to <div v-if="currentQuestion">
KEEP:   all question_type computed properties (isMultiSelect, isOpenQuestion, isPbq)
-->

<!-- CourseTabLernraum.vue — what to remove:
REMOVE: :allowWfMode="selectedLearningPoolAllowsWfMode" prop binding on <TrainingMode>
REMOVE: computed property selectedLearningPoolAllowsWfMode (checks mode_config.swipe)
-->

<!-- CourseTabVerwaltung.vue — what to remove:
REMOVE: { key: 'swipe', label: t('learning', 'Wahr/Falsch im Training') } from mode toggle list (line ~176)
-->

<!-- VirtuProf.vue — what to remove:
REMOVE: context branches for 'course-swipe-pool-select', 'course-swipe-active', 'pool-swipe' (~lines 1957-2044)
-->

<!-- Tests — what to update:
CourseDetail.test.js line ~130: remove swipe: true from mode_config fixture
CourseTabLernraum.test.js line ~52: remove swipe: true from mode_config fixture
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove WF swipe UI from TrainingMode.vue + cascading bindings</name>
  <files>app/src/components/TrainingMode.vue, app/src/components/CourseTabLernraum.vue, app/src/components/CourseTabVerwaltung.vue</files>
  <action>
**TrainingMode.vue** — remove the following in one surgical edit:
1. Template: Delete the entire `<div v-if="allowWfMode" class="mode-toggle">` block (MC/WF toggle buttons)
2. Template: Delete the entire `<div v-if="localWfMode && currentQuestion" class="card-stack">` block (swipe card with Wahr/Falsch buttons)
3. Template: Change the remaining MC block opening tag from `<div v-if="!localWfMode && currentQuestion"` to `<div v-if="currentQuestion"` (remove the `!localWfMode &&` condition)
4. Props section: Remove `wfMode` and `allowWfMode` props
5. Data: Remove `localWfMode` and all swipe drag state properties: `cardAnimClass`, `canDrag`, `isDragging`, `pointerDown`, `dragStartX`, `dragStartY`, `dragDeltaX`, `showFeedback`
6. Watch: Remove any watchers on `localWfMode`, `wfMode`, `allowWfMode`
7. Methods: Remove `selectWfAnswer`, `advanceWfCard`, `onPointerDown`, `onPointerMove`, `onPointerUp`
8. CSS (`<style>`): Remove rules for `.swipe-card`, `.swipe-hint-area`, `.swipe-hint`, `.card-dragging`, `.wf-btn`, `.wf-buttons`

**CourseTabLernraum.vue** — remove:
1. The computed property `selectedLearningPoolAllowsWfMode` (the one that reads `mode_config.swipe`)
2. The `:allowWfMode="selectedLearningPoolAllowsWfMode"` prop binding on the `<TrainingMode>` component usage

**CourseTabVerwaltung.vue** — remove:
1. The `{ key: 'swipe', label: t('learning', 'Wahr/Falsch im Training') }` entry from the mode toggle list (around line 176)

After editing, verify no remaining references to `allowWfMode`, `localWfMode`, or `mode_config.swipe` in these three files:
```bash
grep -n "allowWfMode\|localWfMode\|mode_config\.swipe" app/src/components/TrainingMode.vue app/src/components/CourseTabLernraum.vue app/src/components/CourseTabVerwaltung.vue
```
Expected output: empty (zero matches).
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .js,.vue src/components/TrainingMode.vue src/components/CourseTabLernraum.vue src/components/CourseTabVerwaltung.vue</automated>
  </verify>
  <done>Three Vue files have zero ESLint errors, zero references to allowWfMode/localWfMode/mode_config.swipe remain in those files</done>
</task>

<task type="auto">
  <name>Task 2: Remove VirtuProf swipe context strings + fix test fixtures</name>
  <files>app/src/components/VirtuProf.vue, app/tests/unit/CourseDetail.test.js, app/tests/unit/CourseTabLernraum.test.js</files>
  <action>
**VirtuProf.vue** — in the context switch block (~lines 1957-2044), remove all three swipe-area branches:
- The branch handling `area === 'course-swipe-pool-select'`
- The branch handling `area === 'course-swipe-active'`
- The branch handling `area === 'pool-swipe'`

These areas will never be triggered after TrainingMode swipe removal. Also check `App.vue` line ~754 for any standalone "True/False" guide text — if it is inside a removed swipe context, it is already gone; if standalone, remove or update the string to not reference the swipe mode.

**CourseDetail.test.js** — find the mode_config fixture around line 130 that contains `swipe: true`. Remove the `swipe: true` line from that fixture. Do not change any other assertions.

**CourseTabLernraum.test.js** — find the mode_config fixture around line 52 that contains `swipe: true`. Remove the `swipe: true` line. If any test specifically asserts that `allowWfMode` prop is passed to TrainingMode or that the swipe checkbox is rendered, remove or update those test cases to reflect the new behavior (no swipe mode).

After editing, run the full Vitest suite to confirm green:
```bash
cd /home/andre/Workspace/Code/learning-nc/app && npm run test
```
  </action>
  <verify>
    <automated>cd /home/andre/Workspace/Code/learning-nc/app && npm run test</automated>
  </verify>
  <done>Vitest suite passes (all tests green), VirtuProf.vue has no swipe-area context branches, test fixtures have no swipe: true references</done>
</task>

</tasks>

<verification>
Full Gate 1 after both tasks complete:

```bash
# ESLint — zero errors
cd /home/andre/Workspace/Code/learning-nc/app && npx eslint --ext .js,.vue src/

# Vitest — all green
cd /home/andre/Workspace/Code/learning-nc/app && npm run test

# PHPStan (frontend plan touches no PHP, but run for completeness after deploy)
ssh learning-dev 'docker exec learning-app bash -c "cd /var/www/html/custom_apps/learning && php vendor/bin/phpstan analyse --no-progress"'

# Confirm no dangling references to removed symbols
grep -rn "allowWfMode\|localWfMode\|mode_config\.swipe\|course-swipe-pool-select\|course-swipe-active\|pool-swipe" /home/andre/Workspace/Code/learning-nc/app/src/
# Expected: zero matches
```
</verification>

<success_criteria>
- TrainingMode.vue has no WF toggle block, no swipe card block, no wfMode/allowWfMode props
- CourseTabLernraum.vue has no selectedLearningPoolAllowsWfMode computed or :allowWfMode binding
- CourseTabVerwaltung.vue has no swipe entry in the mode toggle list
- VirtuProf.vue has no course-swipe-pool-select / course-swipe-active / pool-swipe context branches
- Vitest: all tests pass
- ESLint: zero errors
- No references to allowWfMode, localWfMode, or mode_config.swipe remain in app/src/
</success_criteria>

<output>
After completion, create `.planning/phases/115-wahr-falsch-migration/115-02-SUMMARY.md`
</output>
