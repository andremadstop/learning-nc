---
phase: 155-certificate-artifact-issuer
plan: 03
type: tdd
wave: 3
depends_on: ["155-02"]
files_modified:
  - app/lib/Service/SigningService.php
  - app/tests/Unit/Service/SigningServiceTest.php
  - scripts/verify-credential.mjs
autonomous: true
requirements: [CERT-06]
user_setup: []

must_haves:
  truths:
    - "A credential signed by SigningService verifies with sodium_crypto_sign_verify_detached against its public key"
    - "Flipping a single payload byte makes verification FAIL (tamper detection)"
    - "An issued JWT verifies against an INDEPENDENT verifier (Node jose compactVerify), not only the app's own code"
    - "The JWS header is exactly {alg:EdDSA, typ:vc+jwt, cty:vc, kid:<vmId>}; payload is the credential object with NO vc wrapper"
  artifacts:
    - path: "app/lib/Service/SigningService.php"
      provides: "sign(credential, key, secret): compact JWT; verify(jwt, public): bool; base64url helper"
      exports: ["sign", "verify"]
      min_lines: 30
    - path: "scripts/verify-credential.mjs"
      provides: "Dev-only independent EdDSA/JWT verifier (npx jose) — NOT an app dependency"
      contains: "compactVerify"
  key_links:
    - from: "SigningService kid"
      to: "did:web:<host>:apps:learning#<keyId>"
      via: "same string as DidController verificationMethod.id"
      pattern: "apps:learning#"
    - from: "signed bytes"
      to: "emitted bytes"
      via: "JSON_UNESCAPED_SLASHES on both header and payload"
      pattern: "JSON_UNESCAPED_SLASHES"
---

<objective>
Build the ~40-LOC signing core (ext-sodium) as a strict TDD round-trip: a credential object becomes a
compact VC-JWT, verifiable both by the app and by an independent third-party verifier. This is ADR
follow-up #1 (encoding correctness) and #2 (independent verify) made automated. NO new dependencies —
the independent verifier is a dev-only npx script, never added to package.json.

Purpose: CERT-06 signing mechanism + ADR follow-ups #1/#2.
Output: SigningService, SigningServiceTest (round-trip + tamper + header), scripts/verify-credential.mjs.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-certificate-artifact-issuer/155-RESEARCH.md
@.planning/phases/155-certificate-artifact-issuer/155-ADR-ANCHOR.md
@app/lib/Db/CertKey.php
@app/lib/Service/KeyService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Failing SigningServiceTest — round-trip, tamper, header, byte-stability, independent</name>
  <files>app/tests/Unit/Service/SigningServiceTest.php</files>
  <behavior>
    Contract under test — SigningService::sign(array $credential, CertKey $key, string $secretKeyRaw): string
      - header = ['alg'=>'EdDSA','typ'=>'vc+jwt','cty'=>'vc','kid'=>'did:web:'.$host.':apps:learning#'.$key->getKeyId()]
      - signingInput = b64u(json_encode(header, JSON_UNESCAPED_SLASHES)).'.'.b64u(json_encode(credential, JSON_UNESCAPED_SLASHES))
      - return signingInput.'.'.b64u(sodium_crypto_sign_detached(signingInput, secretKeyRaw))
      - MUST NOT add a `vc`/`vp` wrapper or iss/sub/nbf/jti claims (credential IS the payload).
    verify(string $jwt, string $publicKeyRaw): bool; b64u(string): string.
    Five test cases:
    - Test 1 (round-trip): sign() then verify() with the matching public key → true.
    - Test 2 (tamper-payload): flip one byte of the payload segment, re-assemble → verify() → false.
    - Test 3 (header contract): b64url-decode segment[0] → exactly {alg:EdDSA, typ:vc+jwt, cty:vc, kid:...};
      assert NO 'vc' key anywhere in the decoded payload (segment[1]); assert kid contains 'apps:learning#'.
    - Test 4 (byte-stability): the bytes in the signed payload segment equal json_encode(credential, JSON_UNESCAPED_SLASHES).
    - Test 5 (independent verify): write the JWT + JWK x to a tmp file; shell out to `node scripts/verify-credential.mjs`
      (markTestSkipped if node/jose unavailable) → compactVerify succeeds AND a tampered JWT throws.
  </behavior>
  <action>
    Write SigningServiceTest.php with the 5 cases above against the not-yet-complete SigningService. Generate a
    throwaway sodium keypair in the test (sodium_crypto_sign_keypair, no DB) to drive sign/verify. Run it and confirm
    it FAILS (RED). Commit: `test(155-03): failing VC-JWT sign/verify round-trip + tamper + header tests`.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx phpunit --filter SigningServiceTest tests/Unit/Service/SigningServiceTest.php 2>&amp;1 | grep -Eq 'FAILURES|Error|No tests|Fatal'</automated>
  </verify>
  <done>SigningServiceTest exists with 5 cases and fails (no implementation yet) — RED confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement SigningService + dev-only independent verifier</name>
  <files>app/lib/Service/SigningService.php, scripts/verify-credential.mjs</files>
  <action>
    Implement SigningService per the Task 1 contract (~40 LOC): header/payload b64url with JSON_UNESCAPED_SLASHES so
    signed bytes == emitted bytes (zero canonicalization), sodium_crypto_sign_detached, and a verify() using
    sodium_crypto_sign_verify_detached. Host from IURLGenerator (parse_url base URL host). Write scripts/verify-credential.mjs
    using `import { compactVerify, importJWK } from 'jose'` (compactVerify NOT jwtVerify — payload is a VC object, not a
    claims set); read jwt + JWK x from argv/stdin, print OK/FAIL, exit non-zero on bad signature. Mark verify-credential.mjs
    as dev-only: add it to the app-package/tarball excludes so it never ships (flag for the 155-07 leakage audit).
    Run the test → GREEN. Commit: `feat(155-03): implement SigningService VC-JWT EdDSA signer`.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx phpunit --filter SigningServiceTest tests/Unit/Service/SigningServiceTest.php 2>&amp;1 | tee /tmp/sign.txt; grep -Eq 'OK \(|Tests: [1-9]' /tmp/sign.txt &amp;&amp; ! grep -Eq 'FAILURES|Errors:' /tmp/sign.txt &amp;&amp; php -l app/lib/Service/SigningService.php &amp;&amp; grep -q compactVerify scripts/verify-credential.mjs</automated>
  </verify>
  <done>SigningService implemented; all 5 tests GREEN incl. independent compactVerify; verify-credential.mjs is dev-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (REFACTOR): Tidy + PHPStan L5 clean</name>
  <files>app/lib/Service/SigningService.php</files>
  <action>
    Refactor only if needed: extract the b64u helper, add types/docblocks, ensure no dead code — tests stay GREEN.
    Confirm PHPStan L5 clean (deploy-prod.sh --phpstan). Confirm the signed bytes == emitted bytes by construction
    (no canonicalization path). Commit: `refactor(155-03): tidy SigningService` (skip the commit if no change was needed).
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx phpunit --filter SigningServiceTest tests/Unit/Service/SigningServiceTest.php 2>&amp;1 | tee /tmp/sign2.txt; grep -Eq 'OK \(|Tests: [1-9]' /tmp/sign2.txt &amp;&amp; ! grep -Eq 'FAILURES|Errors:' /tmp/sign2.txt</automated>
  </verify>
  <done>SigningService is clean, typed, PHPStan L5 green; SigningServiceTest still GREEN.</done>
</task>

</tasks>

<verification>
- SigningServiceTest green: round-trip, tamper-fails, header-contract, byte-stability, independent-verify.
- scripts/verify-credential.mjs uses compactVerify and proves a third party can verify the signature.
- php -l clean on SigningService; PHPStan L5 clean at wave merge (deploy-prod.sh --phpstan).
- Independent-verify (ADR follow-up #2) green at the phase gate (re-asserted on a REAL issued cert in 155-07).
</verification>

<success_criteria>
The signing core is correct and third-party-verifiable. ADR follow-up #1 (header/payload encoding) and #2
(independent verifier) are automated, not prose. No new package.json/composer dependency was added.
</success_criteria>

<output>
After completion, create `.planning/phases/155-certificate-artifact-issuer/155-03-SUMMARY.md`.
NOTE: edit STATE.md / ROADMAP.md MANUALLY — gsd-tools state/roadmap commands corrupt the v5.0.0 frontmatter.
</output>
