---
phase: 155-certificate-artifact-issuer
plan: 07
type: execute
wave: 7
depends_on: ["155-02", "155-03", "155-04", "155-06"]
files_modified:
  - app/tests/Unit/Service/LeakageAuditTest.php
  - scripts/test-api.sh
  - scripts/cross-db-migration-check.sh
  - .planning/phases/155-certificate-artifact-issuer/155-LEAKAGE-AUDIT.md
  - scripts/verify-issued-cert-gate.sh
autonomous: false
requirements: [CERT-01, CERT-02, CERT-03, CERT-04, CERT-06]
user_setup: []

must_haves:
  truths:
    - "The private key (secret_key_enc) cannot escape via ANY export/snapshot/app-package/jsonSerialize path"
    - "Migration Version009100 applies cleanly on BOTH PostgreSQL 16 AND MariaDB 11.4 (utf8mb4) — go/no-go"
    - "The JWT kid of an issued credential exactly equals a verificationMethod.id in the live did.json (curl)"
    - "After key rotation, a certificate signed by the now-retired key still verifies (did.json still serves that key)"
    - "A REAL DB/API-issued credential verifies against the INDEPENDENT Python verifier (ADR follow-up #2) — the gate FAILS, not skips, if no verifier is available"
  artifacts:
    - path: ".planning/phases/155-certificate-artifact-issuer/155-LEAKAGE-AUDIT.md"
      provides: "Enumerated sign-off of every key-leakage surface (Rule 18 phase-close gate)"
      contains: "secret_key_enc"
    - path: "app/tests/Unit/Service/LeakageAuditTest.php"
      provides: "Automated assertion that secret_key_enc is absent from all serialize/export outputs"
      contains: "secret_key_enc"
    - path: "scripts/cross-db-migration-check.sh"
      provides: "Ephemeral mariadb:11.4 container applies Version009100 schema — cross-DB go/no-go"
      contains: "mariadb:11.4"
    - path: "scripts/verify-issued-cert-gate.sh"
      provides: "Runs verify-credential.py against a REAL issued credential; fail-not-skip if no independent verifier"
      contains: "verify-credential.py"
  key_links:
    - from: "did.json verificationMethod[].id"
      to: "issued JWT kid"
      via: "curl + jq assertion in test-api.sh (ADR follow-up #3)"
      pattern: "did.json"
    - from: "rotated/retired CertKey"
      to: "past certificate verification"
      via: "findAllNonRevoked still serves the retired key"
      pattern: "findAllNonRevoked"
    - from: "real issued credential JWT (GET /api/certificates)"
      to: "scripts/verify-credential.py (Python cryptography Ed25519)"
      via: "independent third-party verification — fail-not-skip phase gate"
      pattern: "verify-credential.py"
---

<objective>
Phase-close security + portability gates. Three go/no-go checks before Phase 155 closes: (1) the private
key cannot leak through any export/snapshot/app-package/serialize surface (Rule 18, enumerated + automated),
(2) Migration Version009100 applies on both PG16 and MariaDB 11.4 utf8mb4, (3) the issued JWT kid aligns with
the live did.json verificationMethod.id (ADR follow-up #3) and a rotated/retired key still verifies past certs, and (4) a
REAL issued credential verifies against the INDEPENDENT Python verifier (ADR follow-up #2, fail-not-skip).

Purpose: CERT-03 (leakage gate), CERT-04 (rotation-preserves + cross-DB schema).
Also marks CERT-01 (occ init-issuer) + CERT-02 (public did.json) complete — both are owned by 155-02
(code-complete + unit/static-proven there) but only become live-verifiable HERE, once the migration is
applied and the live `occ learning:cert:init-issuer` smoke + `curl did.json` assertions pass.
Output: LeakageAuditTest, 155-LEAKAGE-AUDIT.md sign-off, cross-db-migration-check.sh, test-api.sh kid+did assertions.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-certificate-artifact-issuer/155-RESEARCH.md
@app/lib/Service/DataExportService.php
@app/lib/Service/DataMobilityService.php
@app/lib/Service/CourseArchiveService.php
@app/lib/Db/CertKey.php
@app/lib/Migration/Version009100Date20260627000000.php

<interfaces>
<!-- Leakage surfaces to enumerate (RESEARCH Leakage Audit) — assert secret_key_enc / cert_keys absent from each: -->
<!--   DataExportService::exportForUser, DataMobilityService::exportCourse/exportCourseStatsCsv, -->
<!--   CourseArchiveService + learning_course_snapshots, Export/Import command family, NC user-migration, -->
<!--   app-store tarball (no key file under app/ or appdata), CertKey::jsonSerialize, any logging of decrypted bytes. -->
<!-- Trust-model statement (state explicitly, not a gap): DB backups contain the ICrypto ciphertext; security rests -->
<!--   on config.php instance secret — the accepted NC at-rest model (same as telos fields). -->

<!-- Cross-DB: NO MariaDB harness exists in this repo (verified). The check must spin an EPHEMERAL mariadb:11.4 -->
<!--   container (utf8mb4), apply the Version009100 schema (the CREATE TABLE/INDEX DDL — table names get the oc_ -->
<!--   prefix here since we apply raw, mirroring NC's auto-prefix), assert: tables create, indexes create, index -->
<!--   names ≤ MariaDB's 64-char limit, no utf8mb4 key-length error on the VARCHAR indexes. PG16 is the live devcloud default. -->
<!-- 154 gotcha: `migrations:execute` is unavailable on this NC33 instance (only migrations:preview); migration -->
<!--   applied via `occ upgrade`. So the MariaDB check applies DDL directly to the container, not via occ. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Private-key leakage audit (enumerated + automated) — Rule 18 gate</name>
  <files>app/tests/Unit/Service/LeakageAuditTest.php, .planning/phases/155-certificate-artifact-issuer/155-LEAKAGE-AUDIT.md</files>
  <action>
    Write 155-LEAKAGE-AUDIT.md enumerating EVERY surface from the RESEARCH Leakage Audit checklist, each with a
    PASS/explanation: DataExportService::exportForUser (instance-level keys not per-user — confirm no table loop),
    DataMobilityService::exportCourse + exportCourseStatsCsv, CourseArchiveService + learning_course_snapshots,
    Export/Import command family (ExportCourse/ExportPool/ImportVault), NC user-migration, app-store tarball
    (no key file under app/ or appdata — key lives ONLY in the DB ciphertext column; also confirm scripts/verify-credential.py
    is excluded from the tarball), CertKey::jsonSerialize omits secret_key_enc, no logging of decrypted bytes +
    sodium_memzero after sign. Include the explicit trust-model statement.
    Then write LeakageAuditTest.php asserting automatically: (a) CertKey::jsonSerialize() output contains no
    'secret_key_enc' key and no 'secret' substring; (b) a grep-style assertion (or service-call assertion) that the
    enumerated export/snapshot services never reference the cert_keys table / secret column.
  </action>
  <verify>
    <automated>! grep -rn "secret_key_enc" app/lib/Service/DataExportService.php app/lib/Service/DataMobilityService.php app/lib/Service/CourseArchiveService.php &amp;&amp; ./scripts/deploy-prod.sh --php-only &amp;&amp; ssh relais 'docker exec -w /var/www/html/custom_apps/learning devcloud-app php vendor/bin/phpunit --filter LeakageAuditTest'</automated>
  </verify>
  <done>155-LEAKAGE-AUDIT.md signs off every surface; LeakageAuditTest green; no export/snapshot service references secret_key_enc; trust-model documented.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-DB migration go/no-go (PG16 + MariaDB 11.4 utf8mb4)</name>
  <files>scripts/cross-db-migration-check.sh</files>
  <action>
    Write scripts/cross-db-migration-check.sh that proves Version009100 is cross-DB-safe. PG16 is already the live
    devcloud engine (the migration applies there via `occ upgrade` — assert the two tables exist on the live PG16:
    `ssh relais 'docker exec -u www-data devcloud-app php occ db:show-table learning_cert_keys'` or a psql \dt check).
    For MariaDB: spin an EPHEMERAL `mariadb:11.4` container with utf8mb4 (docker run --rm), wait for ready, then apply
    the Version009100 table DDL (oc_learning_cert_keys + oc_learning_certificates with the exact columns/indexes/types
    from the migration, utf8mb4_general_ci). Assert: both CREATE TABLE succeed, both unique/regular indexes create with
    NO "Specified key was too long" error, index names within limits. Tear the container down. Exit non-zero on any
    failure (go/no-go). Document in the script header that this is the phase-close cross-DB gate.
  </action>
  <verify>
    <automated>bash -n scripts/cross-db-migration-check.sh &amp;&amp; grep -q "mariadb:11.4" scripts/cross-db-migration-check.sh &amp;&amp; grep -qi utf8mb4 scripts/cross-db-migration-check.sh</automated>
  </verify>
  <done>cross-db-migration-check.sh applies Version009100 schema on an ephemeral MariaDB 11.4 utf8mb4 container and asserts PG16, with go/no-go exit codes. (Executor RUNS it and records GREEN in the SUMMARY.)</done>
</task>

<task type="auto">
  <name>Task 3: kid↔did.json alignment + rotation-preserves (test-api.sh + curl)</name>
  <files>scripts/test-api.sh</files>
  <action>
    Add to scripts/test-api.sh (ADR follow-up #3): curl the live did.json `curl -s <base>/apps/learning/did.json`,
    jq-assert it is valid JSON with a non-empty verificationMethod array whose ids match did:web:<host>:apps:learning#<frag>.
    Then take an issued credential's JWT (from /api/certificates as an authenticated user, or a fixture), base64url-decode
    its header, extract kid, and assert that kid EXACTLY equals one verificationMethod[].id from did.json. Add a
    rotation-preserves assertion: after a `learning:cert:init-issuer --rotate`, did.json still lists the PREVIOUS key id
    (retired, non-revoked) so a credential signed by it still resolves. Use direct-output assertions (the test summary
    regexes are buggy). Bruteforce-reset before API calls: `ssh relais 'docker exec -u www-data devcloud-app php occ security:bruteforce:reset 172.21.0.1'`.
  </action>
  <verify>
    <automated>bash -n scripts/test-api.sh &amp;&amp; grep -q "did.json" scripts/test-api.sh &amp;&amp; grep -Eq "kid" scripts/test-api.sh</automated>
  </verify>
  <done>test-api.sh asserts did.json resolves, JWT kid == verificationMethod.id, and a rotated/retired key still appears in did.json (rotation does not invalidate past certs). (Executor RUNS Gate 2 and records GREEN.)</done>
</task>

<task type="auto">
  <name>Task 4: Independent-verifier gate on a REAL issued credential (ADR follow-up #2)</name>
  <files>scripts/verify-issued-cert-gate.sh</files>
  <action>
    Write scripts/verify-issued-cert-gate.sh — the phase-close independent-verify gate. It must:
    (a) obtain a REAL issued credential JWT from the live devcloud — authenticate and `curl` GET /api/certificates
        for a student who has a cert (or trigger issuance via a qualifying pass first), and extract one credential_json (compact JWT);
    (b) resolve the signer's public key `x`: read the JWT header `kid` fragment, fetch /apps/learning/did.json, and pull the
        matching verificationMethod.publicKeyJwk.x;
    (c) write the JWT + the base64url `x` to tmp files and run `python3 scripts/verify-credential.py`;
    (d) HARD-FAIL (exit non-zero) if python3 OR the `cryptography` module is absent — NO skip at this gate
        (guard: `python3 -c "import cryptography" || { echo "no independent verifier"; exit 1; }`);
    (e) assert exit 0 on the valid JWT AND non-zero on a tampered copy (flip one payload byte);
    (f) print a clear PASS/FAIL; exit non-zero on any failure. Bruteforce-reset before API calls
        (`ssh relais 'docker exec -u www-data devcloud-app php occ security:bruteforce:reset 172.21.0.1'`).
    This homes the ADR follow-up #2 promise that 155-03 deferred to the phase gate (155-03 proves the mechanism on a
    throwaway key; THIS proves a real DB/API-issued credential verifies third-party).
  </action>
  <verify>
    <automated>bash -n scripts/verify-issued-cert-gate.sh &amp;&amp; grep -q "verify-credential.py" scripts/verify-issued-cert-gate.sh &amp;&amp; grep -q "import cryptography" scripts/verify-issued-cert-gate.sh</automated>
  </verify>
  <done>verify-issued-cert-gate.sh extracts a real issued JWT, verifies it with the independent Python Ed25519 verifier (fail-not-skip), and proves a tampered copy fails. (Executor RUNS it and records PASS in the SUMMARY.)</done>
</task>

</tasks>

<verification>
- LeakageAuditTest green; 155-LEAKAGE-AUDIT.md every surface signed off; trust-model stated.
- cross-db-migration-check.sh executed → GREEN on ephemeral MariaDB 11.4 + PG16 assertion (recorded in SUMMARY).
- test-api.sh did.json + kid-alignment + rotation-preserves assertions GREEN under Gate 2 (after deploy + bruteforce reset).
- verify-issued-cert-gate.sh PASS: a REAL issued credential verifies via the independent Python verifier; tampered copy fails (ADR follow-up #2; fail-not-skip).
- Full Gate 1 (PHPStan L5 + ESLint + Vitest) + Gate 2 green before /gsd:verify-work.
- This plan is autonomous:false — the go/no-go gates require the executor to RUN the checks and confirm results, not just author scripts.
</verification>

<success_criteria>
The three phase-close gates pass: (1) the issuer private key provably cannot leak (CERT-03 Rule 18), (2) the schema
is cross-DB portable (PG16 + MariaDB 11.4), (3) kid↔did.json alignment holds and rotation preserves past certs (CERT-04), and (4) a REAL issued credential
verifies against the independent Python verifier (ADR follow-up #2). Phase 155 is ready for /gsd:verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/155-certificate-artifact-issuer/155-07-SUMMARY.md`.
NOTE: edit STATE.md / ROADMAP.md MANUALLY — gsd-tools state/roadmap commands corrupt the v5.0.0 frontmatter.
</output>
