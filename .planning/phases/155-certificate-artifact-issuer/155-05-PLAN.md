---
phase: 155-certificate-artifact-issuer
plan: 05
type: execute
wave: 5
depends_on: ["155-04"]
files_modified:
  - app/lib/Controller/CertificateController.php
  - app/appinfo/routes.php
  - app/src/services/CertificateService.js
  - app/tests/Unit/Controller/CertificateControllerTest.php
autonomous: true
requirements: [CERT-07, CERT-09]
user_setup: []

must_haves:
  truths:
    - "An authenticated student can list their own issued certificates and fetch one by verification-id"
    - "A student can only read their OWN certificates (no cross-user access)"
    - "The download endpoint returns the compact VC-JWT credential with a verifiable-credential content type"
  artifacts:
    - path: "app/lib/Controller/CertificateController.php"
      provides: "index() own certs, show(vid) own cert, download(vid) JWT file — all auth-required, ownership-checked"
      exports: ["index", "show", "download"]
    - path: "app/src/services/CertificateService.js"
      provides: "axios client: listCertificates(), getCertificate(vid), download URL builder"
      exports: ["listCertificates", "getCertificate"]
  key_links:
    - from: "CertificateController"
      to: "CertificateMapper.findByUserId() / findByVerificationId()"
      via: "ownership check userId === current user before returning"
      pattern: "getUID"
---

<objective>
Expose the issued credentials to their owner over authenticated HTTP: list, view, and download the
compact VC-JWT. This is the backend the student certificate UI (155-06) consumes. Strict ownership
checks — a student reads only their own certificates. (The PUBLIC verify route is Phase 157, NOT here.)

Purpose: CERT-07 (view data source), CERT-09 (download as credential file).
Output: CertificateController, routes, CertificateService.js, CertificateControllerTest.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-certificate-artifact-issuer/155-RESEARCH.md
@app/lib/Db/Certificate.php
@app/lib/Db/CertificateMapper.php
@app/lib/Controller/IcsController.php

<interfaces>
<!-- Routes format (app/appinfo/routes.php): ['name' => 'certificate#index', 'url' => '/api/certificates', 'verb' => 'GET'] -->
<!-- Existing service modules live in app/src/services/ (CourseService.js created in 154-04) — add CertificateService.js there. -->
<!-- Auth: standard authenticated controller (NO @PublicPage). Use IUserSession->getUser()->getUID() for the current user. -->

CertificateController extends Controller (authenticated):
  - index(): JSONResponse — CertificateMapper.findByUserId(currentUid) → jsonSerialize list.
  - show(string $verificationId): JSONResponse — findByVerificationId; 404 if missing; 403 if userId !== currentUid.
  - download(string $verificationId): DataDownloadResponse — same ownership check; body = credential_json (compact JWT);
    filename "certificate-<verificationId>.jwt"; content type 'application/vc+jwt'. (Optionally also a JSON-LD
    EnvelopedVerifiableCredential variant per RESEARCH Open Question 3 — JWT is the primary verifiable artifact.)

Routes:
  ['name' => 'certificate#index',    'url' => '/api/certificates',                 'verb' => 'GET'],
  ['name' => 'certificate#show',     'url' => '/api/certificates/{verificationId}','verb' => 'GET'],
  ['name' => 'certificate#download', 'url' => '/api/certificates/{verificationId}/download', 'verb' => 'GET'],
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CertificateController — list/show/download (ownership-checked)</name>
  <files>app/lib/Controller/CertificateController.php, app/appinfo/routes.php, app/tests/Unit/Controller/CertificateControllerTest.php</files>
  <behavior>
    - Test 1 (list own): index() returns only the current user's certificates.
    - Test 2 (ownership): show()/download() for a verificationId owned by another user → 403 (not the cert body).
    - Test 3 (download body): download() for an owned cert returns the credential_json (3-segment JWT) with content type application/vc+jwt.
    - Test 4 (missing): show() for an unknown verificationId → 404.
  </behavior>
  <action>
    Implement CertificateController per the &lt;interfaces&gt; contract (authenticated controller; mirror an existing
    authenticated controller's constructor + IUserSession usage). Add the three routes to app/appinfo/routes.php.
    Ownership check on show/download: load by verificationId, compare userId to getUID(), return 403 on mismatch,
    404 on not found. download() uses DataDownloadResponse. Write CertificateControllerTest.php (4 behaviors).
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx phpunit --filter CertificateControllerTest tests/Unit/Controller/CertificateControllerTest.php 2>&amp;1 | tee /tmp/certctrl.txt; grep -Eq 'OK \(|Tests: [1-9]' /tmp/certctrl.txt &amp;&amp; ! grep -Eq 'FAILURES|Errors:' /tmp/certctrl.txt &amp;&amp; grep -q "certificate#download" app/appinfo/routes.php</automated>
  </verify>
  <done>Controller lists/shows/downloads own certs with 403/404 ownership semantics; routes registered; all 4 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CertificateService.js — axios client for the cert UI</name>
  <files>app/src/services/CertificateService.js</files>
  <action>
    Create app/src/services/CertificateService.js mirroring app/src/services/CourseService.js (axios + generateUrl).
    Export listCertificates() → GET /api/certificates, getCertificate(verificationId) → GET /api/certificates/{vid},
    and a downloadUrl(verificationId) helper returning the generated /api/certificates/{vid}/download URL (used by
    the 155-06 download button). Keep it a thin client — no business logic. ESLint must be 0 errors.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx eslint --ext .js src/services/CertificateService.js &amp;&amp; grep -q listCertificates src/services/CertificateService.js</automated>
  </verify>
  <done>CertificateService.js exports listCertificates/getCertificate/downloadUrl; ESLint 0 errors.</done>
</task>

</tasks>

<verification>
- CertificateControllerTest green; routes registered (router:list on NC 33 — NOT routes:list).
- ESLint 0 errors on CertificateService.js; deploy-prod.sh --phpstan clean at wave merge.
- Live authenticated GET /api/certificates asserted in Gate 2 (test-api.sh, after deploy + bruteforce reset).
</verification>

<success_criteria>
The student-owned certificate data is reachable over authenticated HTTP (list/view/download), ownership-checked,
ready for the 155-06 Vue UI. No public exposure (that is Phase 157).
</success_criteria>

<output>
After completion, create `.planning/phases/155-certificate-artifact-issuer/155-05-SUMMARY.md`.
NOTE: edit STATE.md / ROADMAP.md MANUALLY — gsd-tools state/roadmap commands corrupt the v5.0.0 frontmatter.
</output>
