---
phase: 155-certificate-artifact-issuer
plan: 06
type: execute
wave: 6
depends_on: ["155-05"]
files_modified:
  - app/src/components/Certificate.vue
  - app/src/utils/qrcode-generator.js
  - app/src/components/CourseSummary.vue
  - app/src/components/__tests__/Certificate.spec.js
  - app/l10n/de.json
  - app/l10n/en.json
  - app/l10n/fr.json
  - app/l10n/ru.json
  - app/l10n/ar.json
autonomous: false
requirements: [CERT-07, CERT-08, CERT-09, CERT-10, CERT-11, CERT-13]
user_setup: []

must_haves:
  truths:
    - "Student opens their certificate and sees course, score, threshold, issuer name+logo (NC theming), issue + expiry dates"
    - "window.print() renders a clean certificate via a print stylesheet"
    - "A QR code encodes the public verification URL for the certificate"
    - "The student can download the credential as a file and copy a prefilled LinkedIn Add-to-Profile URL"
    - "Viewer chrome (labels) renders in DE and EN via existing i18n; signed credential fields stay frozen"
  artifacts:
    - path: "app/src/components/Certificate.vue"
      provides: "Options-API certificate view: render + print + QR + download + LinkedIn"
      contains: "window.print"
    - path: "app/src/utils/qrcode-generator.js"
      provides: "Vendored MIT single-file QR generator (no npm dependency)"
      contains: "MIT"
  key_links:
    - from: "Certificate.vue"
      to: "CertificateService.js (155-05)"
      via: "getCertificate / downloadUrl"
      pattern: "Certificate(Service|service)"
    - from: "Certificate.vue QR"
      to: "public verify URL /apps/learning/verify/<verificationId>"
      via: "qrcode-generator encodes the URL (verify route ships in Phase 157; URL shape is known now)"
      pattern: "verify/"
---

<objective>
Build the student-facing certificate artifact: an Options-API Vue component that renders the issued
credential with NC-theming branding, prints via window.print(), shows a QR to the public verify URL,
downloads the credential file, and offers a prefilled LinkedIn "Add to Profile" link — all multilingual
(DE/EN min, but i18n keys added in all 5 langs for parity). QR is a vendored MIT single file, NOT an npm
dependency. Ends with a human-verify checkpoint on relay.

Purpose: CERT-07 (view/print), CERT-08 (QR), CERT-09 (download), CERT-10 (i18n), CERT-11 (branding render), CERT-13 (LinkedIn).
Output: Certificate.vue, vendored qrcode-generator.js, CourseSummary entry point, Vitest spec, i18n (5 langs), human verify.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-certificate-artifact-issuer/155-RESEARCH.md
@app/src/services/CertificateService.js
@app/src/utils/summary-export.js
@app/src/components/CourseSummary.vue

<interfaces>
<!-- OPTIONS API ONLY — no <script setup>, no ref/setup/onMounted (113-component project convention, enforced in 154-05). -->
<!-- summary-export.js provides the print() PDF strategy + client-side blob download — REUSE, do not reinvent. -->
<!-- Vendored QR: qrcode-generator by Kazuhiko Arase (MIT). Copy the single file into app/src/utils/ with its MIT -->
<!--   license header intact. Do NOT `npm install qrcode` (Pitfall 6 — no new package.json dependency). -->
<!-- Multilingual rule (Pitfall 5): the SIGNED credential fields (course title etc.) are FROZEN at issue and rendered -->
<!--   as-is; only the viewer CHROME (labels "Issued"/"Valid until"/"Verify"/buttons) is translated live via l10n. -->
<!--   Do NOT re-translate signed fields. -->

LinkedIn Add-to-Profile (CERT-13, client-side, no backend):
  https://www.linkedin.com/profile/add? + URLSearchParams({startTask:'CERTIFICATION_NAME', name:courseTitle,
  organizationName:issuerName, issueYear, issueMonth, certUrl:publicVerifyUrl, certId:verificationId})

Public verify URL shape (route lands in Phase 157, but the string is known now):
  <nc_base>/apps/learning/verify/<verificationId>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor qrcode-generator.js (MIT) + Certificate.vue</name>
  <files>app/src/utils/qrcode-generator.js, app/src/components/Certificate.vue, app/l10n/de.json, app/l10n/en.json, app/l10n/fr.json, app/l10n/ru.json, app/l10n/ar.json</files>
  <action>
    Vendor the MIT qrcode-generator single file into app/src/utils/qrcode-generator.js (keep the MIT license header).
    Create Certificate.vue (OPTIONS API): props/route give a verificationId; on load call CertificateService.getCertificate(),
    decode the credential (parse the JWT payload segment for display ONLY — do not re-verify client-side here),
    render course title, score, threshold, issuer name + live themed logo (CERT-11), validFrom/validUntil (formatted).
    Add: a print button (reuse summary-export.js print() strategy + a print stylesheet/@media print so the card prints
    clean), a QR block (qrcode-generator encodes the public verify URL), a download button (CertificateService.downloadUrl),
    and a LinkedIn "Add to Profile" button building the URL per the &lt;interfaces&gt; spec. All labels via t('learning', ...).
    Add ALL new i18n keys to the 5 l10n JSON files in lockstep (parity gate), then regenerate .js bundles with
    `python3 scripts/l10n_js_sync.py`. ESLint 0 errors; Options API only (no ref/setup/onMounted).
  </action>
  <verify>
    <automated>cd app &amp;&amp; npx eslint --ext .js,.vue src/components/Certificate.vue src/utils/qrcode-generator.js &amp;&amp; ! grep -Eq '<script setup>|onMounted\(|[^a-zA-Z]ref\(' src/components/Certificate.vue &amp;&amp; grep -qi MIT src/utils/qrcode-generator.js &amp;&amp; bash scripts/check-i18n-parity.sh</automated>
  </verify>
  <done>Certificate.vue (Options API) renders + prints + QR + download + LinkedIn with themed branding; QR vendored MIT; i18n parity green across 5 langs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Certificate.spec.js + CourseSummary entry point</name>
  <files>app/src/components/__tests__/Certificate.spec.js, app/src/components/CourseSummary.vue</files>
  <behavior>
    - Test 1 (print): a print trigger calls window.print (spy).
    - Test 2 (QR): the qrcode-generator module is invoked with the verify URL containing the verificationId.
    - Test 3 (download): the download action targets CertificateService.downloadUrl(verificationId).
    - Test 4 (LinkedIn): the LinkedIn URL is a well-formed profile/add URL with name + certId params (URLSearchParams assertion).
    - Test 5 (i18n chrome): a label resolves via t() and signed fields render unchanged across a language switch.
  </behavior>
  <action>
    Write Certificate.spec.js (Vitest, mock CertificateService + window.print + qrcode-generator) asserting the 5
    behaviors. Add the entry point into CourseSummary.vue: when the student has a passed/issued certificate (Zeugnisstatus
    "Bestanden", from 154-05), show a "Zertifikat ansehen" action that opens Certificate.vue (route or modal). Keep the
    existing Zeugnisstatus card intact (154-05 widget). Options API only; ESLint 0 errors.
  </action>
  <verify>
    <automated>cd app &amp;&amp; npm run test -- Certificate &amp;&amp; npx eslint --ext .vue src/components/CourseSummary.vue</automated>
  </verify>
  <done>Certificate.spec.js green (print/QR/download/LinkedIn/i18n); CourseSummary opens the certificate view for a passed student; ESLint 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — full certificate experience on relay</name>
  <action>
    Build + deploy to relay first: `./scripts/deploy-prod.sh` (PHP + PHPStan + JS). If the issuer is not yet
    initialized, run `ssh relais 'docker exec -u www-data devcloud-app php occ learning:cert:init-issuer'`.
    Then drive a student account through a passing course and walk the verification steps below.
  </action>
  <what-built>
    Full student certificate experience on relay: automatic issuance on pass (155-04) → NC notification →
    CourseSummary "Zertifikat ansehen" → Certificate.vue rendering with NC-theming branding → print / QR / download / LinkedIn.
    Build + deploy first: `./scripts/deploy-prod.sh` (PHP + PHPStan + JS), then run the occ init if not yet done:
    `ssh relais 'docker exec -u www-data devcloud-app php occ learning:cert:init-issuer'`.
  </what-built>
  <how-to-verify>
    1. Visit https://devcloud.andrestiebitz.de, log in as a student who meets a certifying course's pass criterion.
    2. Confirm an NC notification "certificate issued" appeared (bell icon).
    3. Open the course summary → click "Zertifikat ansehen" → confirm the certificate shows course title, score,
       threshold, issuer name + logo (from NC theming), issue and (if set) expiry date.
    4. Click Print → confirm the print preview renders a clean certificate (print stylesheet, no app chrome).
    5. Scan the QR with a phone → confirm it points to <nc_base>/apps/learning/verify/<verificationId> (page is 404 until Phase 157 — the URL/target is what we verify here).
    6. Click Download → confirm a certificate-<vid>.jwt file downloads with a 3-segment token.
    7. Click LinkedIn "Add to Profile" → confirm it opens linkedin.com/profile/add prefilled with the course name.
    8. Switch NC language EN↔DE → confirm labels translate while the signed course title stays unchanged.
  </how-to-verify>
  <verify>
    <automated>MANUAL — relay human-verify (issuance→notification→view→print→QR→download→LinkedIn→i18n). No automated gate; preceded by Vitest + ESLint + Gate 2.</automated>
  </verify>
  <done>Reviewer types "approved" after all 8 steps render correctly on relay.</done>
  <resume-signal>Type "approved" or describe any issues (rendering, print, QR target, download, LinkedIn, i18n).</resume-signal>
</task>

</tasks>

<verification>
- Certificate.spec.js green; ESLint 0 on all touched .vue/.js; Options API only (grep-verified).
- i18n parity green across de/en/fr/ru/ar; .js bundles regenerated.
- Human-verify checkpoint approved on relay (issuance → notification → view → print → QR → download → LinkedIn → i18n).
</verification>

<success_criteria>
SC4: the student can view, print, QR-link, download, and LinkedIn-share the certificate; branding comes from NC
theming; chrome renders DE/EN while signed fields stay frozen.
</success_criteria>

<output>
After completion, create `.planning/phases/155-certificate-artifact-issuer/155-06-SUMMARY.md`.
NOTE: edit STATE.md / ROADMAP.md MANUALLY — gsd-tools state/roadmap commands corrupt the v5.0.0 frontmatter.
</output>
