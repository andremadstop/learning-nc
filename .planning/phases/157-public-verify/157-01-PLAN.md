---
phase: 157-public-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/Migration/Version009200Date20260627120000.php
  - app/lib/Db/Certificate.php
  - scripts/cross-db-migration-check.sh
autonomous: true
requirements: [VERIFY-05]
must_haves:
  truths:
    - "The Certificate entity exposes getRevokedAt()/setRevokedAt() returning int|null"
    - "A new migration adds a nullable revoked_at column to learning_certificates without touching existing columns"
    - "The revoked_at column DDL passes the ephemeral MariaDB 11.4 utf8mb4 cross-DB check (PG16 mirrored)"
    - "info.xml is NOT bumped in this plan — the live apply (bump + occ upgrade) is deferred so no in-phase --php-only deploy pushes a needsDbUpgrade state to live devcloud"
  artifacts:
    - path: "app/lib/Migration/Version009200Date20260627120000.php"
      provides: "revoked_at nullable BIGINT on learning_certificates"
      contains: "addColumn('revoked_at'"
    - path: "app/lib/Db/Certificate.php"
      provides: "revokedAt entity field + addType integer"
      contains: "addType('revokedAt'"
    - path: "scripts/cross-db-migration-check.sh"
      provides: "revoked_at column mirrored in the cross-DB DDL"
      contains: "revoked_at"
  key_links:
    - from: "app/lib/Db/Certificate.php"
      to: "learning_certificates.revoked_at"
      via: "addType('revokedAt','integer') + @method int|null getRevokedAt"
      pattern: "revokedAt"
---

<objective>
Add the `revoked_at` column that the entire 157 tombstone + revoke capability depends on. The
live `Version009100` schema and the `Certificate` entity have NO `revoked_at` column (RESEARCH
Pitfall 1: SPEC ⊥ CODE) — yet VERIFY-05 / ROADMAP SC-3 require the tombstone to show "the
revocation date." This is the foundation wave: every downstream plan (verify-service tombstone
branch, revoke write) reads or writes this field.

Purpose: Make `revoked_at` a real, queryable, cross-DB-safe column + entity field.
Output: A new migration + the entity field + an extended cross-DB check script. The migration is
written but NOT applied live, and info.xml is NOT bumped here (see the critical note below).
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-public-verify/157-RESEARCH.md
@.planning/phases/157-public-verify/157-VALIDATION.md

<interfaces>
<!-- The live Version009100 migration this one extends (read for the exact NC DDL style). -->
From app/lib/Migration/Version009100Date20260627000000.php — `learning_certificates` already has:
  id, verification_id, user_id, course_id, key_id, credential_json (TEXT),
  revoked (BOOLEAN), issued_at (BIGINT), expires_at (BIGINT nullable),
  active_idem_key (STRING 128 nullable, UNIQUE learn_cert_idem_uq).
  NO revoked_at. Table names WITHOUT oc_ prefix (NC auto-prepends). Types::* everywhere.

From app/lib/Db/Certificate.php — current entity fields + jsonSerialize() (owner-only serialize,
  emits user_id + credential_json — leave jsonSerialize UNCHANGED; do NOT add revoked_at to it,
  the public DTO is built server-side in 157-02, never via this serializer).
  Constructor calls addType() per field; @method block documents getters/setters.

CRITICAL — why no info.xml bump here (advisor finding): info.xml is currently 4.4.8 on live devcloud.
Bumping it would make NC flag `needsDbUpgrade` (code version > DB version), and `deploy-prod.sh
--php-only` (which 157-02/157-04 run) RSYNCS info.xml but does NOT run `occ upgrade` — so a bumped
info.xml would deploy to devcloud and immediately show the maintenance/upgrade page, breaking the
LIVE app for real users AND making 157-05's logged-out reachability e2e hit the upgrade page instead
of the verify route. So: ship the migration file dormant (NC will not run it until a version bump),
keep info.xml at 4.4.8, and DEFER the bump + `occ upgrade` to the same authorized provisioning pass
(157-05 deferred list) — exactly the 155-01 → 155-07 LIVE pattern (155-01 wrote the migration and did
NOT bump info.xml; 155-07 bumped + upgraded together under authorization).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: revoked_at migration + Certificate entity field</name>
  <files>app/lib/Migration/Version009200Date20260627120000.php, app/lib/Db/Certificate.php</files>
  <action>
    1. Create `app/lib/Migration/Version009200Date20260627120000.php` — a `SimpleMigrationStep`
       mirroring the Version009100 style (namespace OCA\Learning\Migration; Closure $schemaClosure).
       In `changeSchema`, guard with `if ($schema->hasTable('learning_certificates'))` and
       `if (!$table->hasColumn('revoked_at'))` then:
         `$table->addColumn('revoked_at', Types::BIGINT, ['notnull' => false]);`
       Return `$changed ? $schema : null`. Touch NO other table or column. Use `OCP\DB\Types`,
       `OCP\DB\ISchemaWrapper`, `OCP\Migration\{IOutput,SimpleMigrationStep}`. Add a docblock noting
       this is the Phase-157 tombstone column (VERIFY-05); nullable so existing rows stay valid and a
       `SELECT *` on a pre-upgrade row simply leaves it null.
    2. Edit `app/lib/Db/Certificate.php`:
       - add `protected $revokedAt;`
       - in the constructor add `$this->addType('revokedAt', 'integer');`
       - add to the @method block: `@method int|null getRevokedAt()` and
         `@method void setRevokedAt(?int $revokedAt)`.
       - Do NOT add revoked_at to jsonSerialize() (that serializer is owner/internal; the public DTO
         is projected separately in 157-02 — keeping it out prevents accidental leakage paths).
    3. Do NOT bump app/appinfo/info.xml and do NOT run `occ upgrade` (see the CRITICAL note above).
       The bump + live apply are deferred to the authorized provisioning pass recorded in 157-05.
  </action>
  <verify>
    <automated>ssh relais 'docker exec -w /var/www/html/custom_apps/learning devcloud-app php -l lib/Migration/Version009200Date20260627120000.php && docker exec -w /var/www/html/custom_apps/learning devcloud-app php -l lib/Db/Certificate.php' && grep -c "4.4.8" app/appinfo/info.xml</automated>
  </verify>
  <done>Migration + entity field exist, both `php -l` clean; info.xml STILL 4.4.8 (grep finds it); jsonSerialize() unchanged; no live occ upgrade run.</done>
</task>

<task type="auto">
  <name>Task 2: extend cross-DB migration check with revoked_at</name>
  <files>scripts/cross-db-migration-check.sh</files>
  <action>
    Extend `scripts/cross-db-migration-check.sh` (the 155-07 precedent that mirrors the cert-table
    DDL verbatim against an ephemeral `mariadb:11.4 --rm` container) to also assert the new
    `revoked_at BIGINT NULL` column on `learning_certificates`. Mirror the exact NC mapping
    (BIGINT → MariaDB `bigint`, notnull=false → nullable). Keep the existing readiness pattern
    (real authenticated `SELECT 1`, NOT `mariadb-admin ping` — see STATE 155-07 note). The PG16 side
    stays a documented no-op (live `occ db:show-table` post-upgrade). Exit 0 = GO, non-zero = NO-GO.
  </action>
  <verify>
    <automated>bash scripts/cross-db-migration-check.sh; echo "exit=$?"</automated>
  </verify>
  <done>Script runs an ephemeral MariaDB 11.4, creates learning_certificates incl. revoked_at, no "key too long" error, tears the container down, exits 0 (GO).</done>
</task>

</tasks>

<verification>
- `php -l` clean on both new/edited PHP files (relay, no local PHP).
- `bash scripts/cross-db-migration-check.sh` exits 0 (MariaDB 11.4 GO; PG16 documented no-op).
- info.xml STILL 4.4.8 (NOT bumped); CHANGELOG untouched, no git tag.
- jsonSerialize() in Certificate.php is byte-identical to before (no revoked_at leak path added).
</verification>

<success_criteria>
- learning_certificates GAINS a nullable revoked_at column via Version009200 (written, dormant, applyable).
- Certificate entity reads/writes revokedAt as int|null.
- Cross-DB DDL parity proven for MariaDB 11.4 utf8mb4 (PG16 mirrored).
- info.xml unchanged → no in-phase deploy can push a needsDbUpgrade state to live devcloud.
- Live apply (info.xml bump + occ upgrade) explicitly deferred to 157-05's provisioning list.
</success_criteria>

<output>
After completion, create `.planning/phases/157-public-verify/157-01-SUMMARY.md`.
Note: migration is dormant; info.xml NOT bumped; live `occ upgrade` + bump deferred to the 157-05
authorized provisioning pass; reconcile the patch number at the v5.0.0 release.
</output>
