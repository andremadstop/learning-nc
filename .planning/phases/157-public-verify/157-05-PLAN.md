---
phase: 157-public-verify
plan: 05
type: execute
wave: 4
depends_on: ["157-04"]
files_modified:
  - app/tests/e2e/public-verify.spec.js
  - app/playwright.config.js
autonomous: true
requirements: [VERIFY-01, VERIFY-03]
must_haves:
  truths:
    - "A logged-out browser context can load /apps/learning/verify/{vid} and get the public page (no 403, no login redirect)"
    - "The rendered HTML body of the live cert's verify page contains the recipient name NOWHERE — not in visible text, scripts, data-* attributes, embedded JSON, or fallback texts"
    - "The no-leak assertion targets the LIVE cert's actual recipient identity (display name + user id), NOT the invented unit-test fixture name"
  artifacts:
    - path: "app/tests/e2e/public-verify.spec.js"
      provides: "logged-out reachability + DOM-level no-recipient-name check"
      contains: "no-auth"
  key_links:
    - from: "app/tests/e2e/public-verify.spec.js"
      to: "the live synthetic cert eb97720c on devcloud"
      via: "logged-out goto /apps/learning/verify/eb97720c-… then assert recipient absent from page.content()"
      pattern: "eb97720c"
---

<objective>
Close the phase verification gate: a Playwright logged-out E2E proving (a) public reachability
(VERIFY-01) and (b) the DOM-level no-recipient-name guarantee (VERIFY-03, Codex #3 — stronger than
156's DTO-array grep), plus the cross-DB GO confirmation and a grep leak-gate sweep across the new
surfaces.

CRITICAL (advisor finding): the no-leak assertion MUST target the LIVE cert's real recipient, NOT
the unit-test fixture "Jürgen Müller" (which was never in any real cert → a vacuous pass). The live
synthetic cert `eb97720c-59d1-4c65-ba49-229c47341047` on devcloud has recipient display name
**"ZZ Testkandidat"**, user id **`zz-test-cert155`** (STATE line 41). Assert THOSE are absent.

Purpose: VERIFY-01 + VERIFY-03 live gate before `/gsd:verify-work`.
Output: a Playwright spec + the phase-gate sweep. Depends on 157-04 (the live verify route).
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-public-verify/157-RESEARCH.md
@.planning/phases/157-public-verify/157-VALIDATION.md

<interfaces>
Existing Playwright suite: app/tests/e2e/*.spec.js (auth.setup.js sets up logged-IN state).
This spec needs a NO-AUTH context (`test.use({ storageState: { cookies: [], origins: [] } })` or a
fresh `browser.newContext()` with no storageState) so it hits the PUBLIC route without any NC session.
Config: app/playwright.config.js. Run: `cd app && npm run test:e2e` (or `npx playwright test public-verify`).

Live target (devcloud, base URL https://devcloud.andrestiebitz.de):
  GET /apps/learning/verify/eb97720c-59d1-4c65-ba49-229c47341047  → the live synthetic VALID cert.
  Recipient identity to assert ABSENT: "ZZ Testkandidat" AND "zz-test-cert155".
  MANDATORY presence-confirmation (advisor finding — guards against a vacuous pass): before asserting
  ABSENCE, CONFIRM the recipient name is actually IN the cert. On devcloud, psql:
    SELECT credential_json FROM oc_learning_certificates WHERE verification_id='eb97720c-59d1-4c65-ba49-229c47341047';
  base64url-decode the payload (middle JWT segment) and confirm credentialSubject.name === "ZZ Testkandidat".
  If it is instead the fallback "Teilnehmer:in" (IssuanceService::FALLBACK_RECIPIENT), the name was never
  in the credential → absence proves NOTHING; the executor MUST then pick/mint a cert whose
  credentialSubject.name is a real display name and assert THAT. Only after presence is confirmed does the
  page.content() absence assertion become a real VERIFY-03 gate.

Reachability vs migration: a VALID cert like eb97720c renders fine even BEFORE the live `occ upgrade`
of the 157-01 migration — `revoked_at` is only read in the WITHDRAWN branch; SELECT * on a pre-migration
row just leaves it null. So reachability + no-leak are NOT blocked on live-applying the migration. Only
the WITHDRAWN visual is (deferred, option A). Do not treat the un-applied migration as a blocker here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright logged-out reachability + DOM no-leak spec</name>
  <files>app/tests/e2e/public-verify.spec.js, app/playwright.config.js</files>
  <action>
    Create `app/tests/e2e/public-verify.spec.js`:
    - Use a NO-AUTH context (no storageState) so there is no NC session.
    - Test "reachable logged-out": `await page.goto(BASE + '/apps/learning/verify/eb97720c-59d1-4c65-ba49-229c47341047')`;
      assert the response is NOT a login redirect / 403 — expect HTTP 200 and a status banner present
      (e.g. the page contains the localized "verifiziert"/status heading, or a known stable selector).
    - Test "no recipient leak (DOM-level)": on the SAME loaded page, grab `const html = await page.content()`
      and assert `expect(html).not.toContain('ZZ Testkandidat')` AND `expect(html).not.toContain('zz-test-cert155')`.
      This covers visible text, scripts, data-* attributes, embedded JSON, and fallback texts — the
      whole rendered body (Codex #3). DO NOT assert "Jürgen Müller" (that is the unit fixture, never in
      a real cert → vacuous). Add a code comment stating exactly this so a future editor doesn't "fix" it.
      PRECONDITION (mandatory, see interfaces): first CONFIRM "ZZ Testkandidat" IS the decoded
      credentialSubject.name of eb97720c on devcloud (psql + base64url-decode). If it is the fallback
      "Teilnehmer:in", switch to a cert with a real display name before this absence assertion means anything.
    - Test "malformed → generic, no oracle" (optional but cheap): goto `/apps/learning/verify/not-a-uuid`,
      assert HTTP 200 + the generic "Verifizierung fehlgeschlagen" banner, and that NO course/issuer block
      is present (nothing leaks).
    If playwright.config.js needs a project/baseURL entry for an unauthenticated run, add it minimally
    (do not disturb the existing logged-in projects). Keep `animations: 'disabled'` consistent with the
    suite.
  </action>
  <verify>
    <automated>cd app && npx playwright test public-verify --reporter=line</automated>
  </verify>
  <done>Logged-out reachability + DOM no-leak (ZZ Testkandidat + zz-test-cert155 absent) GREEN against the live eb97720c cert; the assertion targets the live recipient, not the unit fixture.</done>
</task>

<task type="auto">
  <name>Task 2: phase gate — cross-DB GO + leak grep sweep + verification wrap-up</name>
  <files>app/tests/e2e/public-verify.spec.js</files>
  <action>
    Run the phase-close automated gates (no new code beyond Task 1; this task records the sweep and
    fixes anything red):
    1. Cross-DB GO: `bash scripts/cross-db-migration-check.sh` (exit 0 = MariaDB 11.4 GO; PG16 the
       documented no-op). Confirms the 157-01 revoked_at column ships cross-DB.
    2. Leak grep gate across ALL new 157 surfaces (added `^+` lines only):
       `grep -nE 'user_id|getUserId|credential_json|credentialSubject' app/lib/Service/CertificateVerifyService.php app/lib/Controller/PublicVerifyController.php app/templates/verify.php`
       — only defensive omissions / payload-field reads (achievement.name) allowed; NO recipient
       projection. Fail the gate if a recipient-identity token is projected.
    3. Full cert PHPUnit suite GREEN on relay:
       `--filter "CertificateVerify\|PublicVerify\|Revoke"`.
    4. Record the DEFERRED-to-human items (do NOT block the phase, user option A): the info.xml patch
       bump (4.4.8 → 4.4.9, NOT done in 157-01 to keep live devcloud reachable) PAIRED WITH `occ upgrade`
       to apply Version009200 — these two go TOGETHER under authorization (155-07 pattern); credentialed
       revoke smoke (instructor 200 / non-owner 404);
       live throttle/rate-limit curl-loop (bruteforce-reset 172.21.0.1 first); the WITHDRAWN/expired
       visual banner eyeball — all ride the authorized demo-course provisioning pass, consistent with
       155/156 discipline. Flag each in the SUMMARY; none silently skipped.
  </action>
  <verify>
    <automated>bash scripts/cross-db-migration-check.sh && ssh relais 'docker exec -w /var/www/html/custom_apps/learning devcloud-app php vendor/bin/phpunit --filter "CertificateVerify\|PublicVerify\|Revoke"'</automated>
  </verify>
  <done>Cross-DB GO (exit 0); full 157 PHPUnit suite GREEN; leak grep clean (no recipient projection on any 157 surface); deferred live items recorded, not skipped.</done>
</task>

</tasks>

<verification>
- `npx playwright test public-verify` GREEN: logged-out reachable + DOM no-leak (live recipient strings absent).
- `cross-db-migration-check.sh` exit 0.
- Full cert PHPUnit suite (CertificateVerify + PublicVerify + Revoke) GREEN; PHPStan L5 clean.
- Leak grep sweep clean across the verify service, public controller, and template.
</verification>

<success_criteria>
- VERIFY-01 live-proven (logged-out reachable).
- VERIFY-03 live-proven at DOM level against the REAL cert recipient (not a vacuous fixture).
- Phase deferred-live items flagged for the authorized demo-course pass; phase ready for /gsd:verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/157-public-verify/157-05-SUMMARY.md`. List every deferred
live item (info.xml 4.4.9 bump + occ upgrade apply [together], credentialed revoke smoke, throttle curl-loop, withdrawn/expired visual)
so the milestone-close provisioning pass picks them up.
</output>
