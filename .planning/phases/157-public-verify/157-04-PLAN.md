---
phase: 157-public-verify
plan: 04
type: execute
wave: 3
depends_on: ["157-02", "157-03"]
files_modified:
  - app/lib/Controller/PublicVerifyController.php
  - app/appinfo/routes.php
  - app/templates/verify.php
  - app/tests/Unit/Controller/PublicVerifyControllerTest.php
  - app/l10n/de.json
  - app/l10n/en.json
  - app/l10n/fr.json
  - app/l10n/ru.json
  - app/l10n/ar.json
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-06]
must_haves:
  truths:
    - "An unauthenticated visitor to /apps/learning/verify/{verificationId} gets a human-readable page (no 403, no NC auth shell)"
    - "A malformed verificationId is rejected by the UUID_V4 precheck BEFORE any DB query and renders the SAME generic page as a not-found (no enumeration oracle)"
    - "throttle() is called ONLY on the unknown/malformed branch — never on valid/expired/withdrawn"
    - "The page shows the status banner, course title, issuer name, and issue/expiry dates — and NEVER the recipient name or user_id"
  artifacts:
    - path: "app/lib/Controller/PublicVerifyController.php"
      provides: "#[PublicPage] verify route, UUID precheck, throttle-on-unknown, server-side DTO"
      contains: "PublicPage"
    - path: "app/templates/verify.php"
      provides: "server-rendered 4-status page (no Vue island), $l->t() i18n, RTL-safe"
      contains: "aria-live"
    - path: "app/tests/Unit/Controller/PublicVerifyControllerTest.php"
      provides: "malformed → unknown (mapper ->never()); not-found indistinguishable"
      contains: "testMalformedUuid"
  key_links:
    - from: "app/lib/Controller/PublicVerifyController.php"
      to: "CertificateVerifyService::verifyByVerificationId"
      via: "delegate all crypto + DTO to the 157-02 service"
      pattern: "verifyByVerificationId"
    - from: "app/appinfo/routes.php"
      to: "publicVerify#verify"
      via: "PAGE section route /verify/{verificationId} (like page#privacy), NOT under /api/"
      pattern: "verify/{verificationId}"
---

<objective>
Build the public, unauthenticated verify page. A pure server-rendered `PublicTemplateResponse` +
PHP template (NO Vue island — LOCKED decision, smaller attack surface) that delegates all crypto +
DTO projection to the 157-02 `CertificateVerifyService`, hardened against enumeration/abuse, with
the Gemini-locked 4-status UX and the load-bearing missing-name explainer.

Purpose: VERIFY-01 (public reachability), VERIFY-02 (shows status/issuer/course/dates), VERIFY-06
(UUID precheck + throttle/rate-limit).
Output: `PublicVerifyController` + page route + `verify.php` template + controller unit test + 5-lang
i18n. Depends on 157-02 (the service) and 157-03 (shared routes.php + l10n json).
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-public-verify/157-RESEARCH.md
@.planning/phases/157-public-verify/157-VALIDATION.md
@app/lib/Controller/PageController.php
@app/templates/privacy.php

<interfaces>
PublicTemplateResponse precedent = PageController::privacy() (`@PublicPage @NoCSRFRequired` +
`new PublicTemplateResponse('learning', 'privacy', [...])`), template app/templates/privacy.php.
Route registered in the PAGE section of routes.php (page#privacy, ~line 6) — NOT under /api/.

CertificateVerifyService::verifyByVerificationId(string $vid): array   // 157-02
  returns ['status' => 'unknown'|'invalid'|'withdrawn'|'expired'|'valid', ...public DTO]
  DTO keys: issuer_name, course_title, issued_at, expires_at(nullable), verification_id,
            revoked_at(withdrawn only). NEVER recipient name/user_id/credential_json.

CertificateController::UUID_V4 regex (copy the constant).
OCP\AppFramework\Http\Attributes\{PublicPage,NoCSRFRequired,BruteForceProtection,AnonRateLimit}.
$response->throttle(['action' => 'learningVerify']) increments the brute-force counter
  (PublicTemplateResponse extends Response → throttle() available). Call ONLY on unknown/malformed.
OCP\Defaults: getName():string, getLogo(bool):string — issuer logo+name for the page header (CERT-11
  parity); the issuer_name in the DTO already comes from the signed payload, prefer that for the data.

i18n for THIS page = SERVER-SIDE `$l->t()` in verify.php (IL10N injected into the template via the
PublicTemplateResponse params or `\OC::$server->get(IFactory)` — pass the IL10N from the controller).
IMPORTANT (STATE 154-04/156-02): the strings STILL live in l10n/<lang>.json and STILL need the .js
regenerated via l10n_js_sync.py and STILL must pass `check-i18n-parity.sh` 5-lang parity — the
template just reads them through `$l->t()` instead of the JS `t()`. Add every new key to all 5 langs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PublicVerifyController + UUID/throttle unit test</name>
  <files>app/lib/Controller/PublicVerifyController.php, app/appinfo/routes.php, app/tests/Unit/Controller/PublicVerifyControllerTest.php</files>
  <behavior>
    - testMalformedUuid: non-UUID input → renders the generic "unknown" page; verifyService AND the
      cert mapper are `->never()` called (precheck short-circuits before any DB query); throttle path taken
    - testUnknownGeneric: a well-formed-but-missing vid (service returns ['status'=>'unknown']) → SAME
      generic page as malformed (no 404, no distinguishing copy — no enumeration oracle)
    - testValidRendersStatus: service returns ['status'=>'valid', ...DTO] → template params carry
      status=valid + issuer_name + course_title (assert the controller passes the DTO through)
    (throttle() middleware is bypassed in direct unit instantiation — assert the BRANCH was taken,
     not the framework side effect; live throttle is verified manually per VALIDATION.)
  </behavior>
  <action>
    Create `PublicVerifyController` with:
      `#[PublicPage] #[NoCSRFRequired] #[BruteForceProtection(action: 'learningVerify')]
       #[AnonRateLimit(limit: 30, period: 60)]`
      `public function verify(string $verificationId): PublicTemplateResponse`
      - Build the response: `$tpl = new PublicTemplateResponse('learning', 'verify', $params)`.
      - UUID precheck FIRST (copy UUID_V4): on no-match → `$tpl->throttle(['action'=>'learningVerify'])`
        + render the generic unknown page (status 'unknown'); RETURN before any DB/service call.
      - Else `$result = $this->verifyService->verifyByVerificationId($verificationId);`
        if `$result['status'] === 'unknown'` → `$tpl->throttle(['action'=>'learningVerify'])`.
      - Pass the projected DTO (status + display fields, NEVER recipient identity) into the template
        params, plus the IL10N-derived labels (or pass IL10N for `$l->t()` in the template) and the
        issuer logo/name (OCP\Defaults). Set a page title via `$tpl->setHeaderTitle(...)`.
      - `return $tpl;` (always HTTP 200 — withdrawn/expired/unknown are all 200 renders, tombstone NOT 404).
      Constructor injects CertificateVerifyService, IL10N (IFactory->get('learning')), OCP\Defaults,
      IRequest, appName. Use attributes (NOT PHPDoc) on this method; do not mix the two forms.
    Register in the PAGE section of routes.php (near page#privacy):
      `['name' => 'publicVerify#verify', 'url' => '/verify/{verificationId}', 'verb' => 'GET'],`
    Write `PublicVerifyControllerTest.php` per &lt;behavior&gt;; mock CertificateVerifyService; assert
    `->never()` on the service for the malformed case. Add `class_exists`-guarded PhpUnitStubs entries
    for PublicTemplateResponse / Http\Attributes\* if mock generation fails (155/156 precedent).
  </action>
  <verify>
    <automated>ssh relais 'docker exec -w /var/www/html/custom_apps/learning devcloud-app php vendor/bin/phpunit --filter PublicVerifyControllerTest && docker exec -w /var/www/html/custom_apps/learning devcloud-app php vendor/bin/phpstan analyse --no-progress'</automated>
  </verify>
  <done>PublicVerifyControllerTest GREEN (malformed→unknown with service->never(); not-found==malformed generic; valid passes DTO); PHPStan L5 clean; route live in PAGE section.</done>
</task>

<task type="auto">
  <name>Task 2: verify.php server template (4 status banners + missing-name explainer) + i18n 5 langs</name>
  <files>app/templates/verify.php, app/l10n/de.json, app/l10n/en.json, app/l10n/fr.json, app/l10n/ru.json, app/l10n/ar.json</files>
  <action>
    Create `app/templates/verify.php` — a PURE server-rendered PHP template (NO `script()` Vue mount;
    mirror privacy.php's structure but render content server-side). Use `$l->t()` for ALL copy and
    `p()` (HTML-escape) for every interpolated DTO value. Layout (Gemini-locked, information order):
      1. STATUS BANNER (full-width, big icon + h1, `aria-live="polite"`, icons `aria-hidden="true"`):
         - valid → green #2f9a48, h1 "Zertifikat erfolgreich verifiziert", sub "Dieses Zertifikat ist gültig."
         - withdrawn → ocker #e69900 (NOT red), h1 "Zertifikat zurückgezogen",
           sub "Dieses Zertifikat wurde am [revoked_at] vom Aussteller widerrufen."
         - expired → grey #6c757d, h1 "Zertifikat abgelaufen", sub "Die Gültigkeit endete am [expires_at]."
         - unknown/invalid → red #d92f2f, h1 "Verifizierung fehlgeschlagen",
           sub "Das Zertifikat konnte nicht gefunden werden. Bitte prüfen Sie den Link oder QR-Code."
      2. course_title (h2)  3. issuer (logo via OCP\Defaults + issuer_name)
      4. data block (issued_at; expires_at or "unbegrenzt gültig")
      5. MISSING-NAME INFO-BOX (load-bearing UX): "Aus Datenschutzgründen (DSGVO) wird der Name des
         Inhabers auf dieser öffentlichen Seite nicht angezeigt. So prüfen Sie die Zugehörigkeit:
         Vergleichen Sie die oben angezeigte Verifizierungs-ID mit der ID auf dem Ihnen vorliegenden
         Dokument." — elevates verification_id to the matching bridge.
      6. fine print (verification_id + query timestamp) + a small "Digital verifiziert" badge + a dezent
         footer link "Wie funktioniert diese Verifizierung?" (plain-language; NO raw signatures, NO
         did.json link, NO "Ed25519/did:web" jargon).
    For the unknown/malformed branch render ONLY the red banner (no course/issuer/data — there is no
    cert) so nothing leaks and there is no oracle. NEVER output recipient name/user_id/credential_json
    (the DTO doesn't carry them — assert by inspection). A11y/mobile: single-column, banner above the
    fold, logical CSS props (`margin-inline-start`) + `flex-direction: row-reverse` for RTL Arabic,
    `#1a3a5c` header. OMIT (over-engineering): timeline, "Save as PDF", QR re-show, course link, modals.
    Add every new string to all 5 l10n/*.json (DE source value==key, real EN/FR/RU/AR), regenerate `.js`
    via `python3 scripts/l10n_js_sync.py`, run the parity gate. Deploy: `./scripts/deploy-prod.sh --php-only` (templates + lib + l10n; PHPStan runs there).
    CRITICAL: confirm info.xml is STILL 4.4.8 BEFORE deploying (157-01 deliberately did NOT bump
    it). `--php-only` rsyncs info.xml; a bumped version would flip devcloud to needsDbUpgrade and
    show the maintenance page (breaking the live app + 157-05 reachability). Live `occ upgrade`
    for the 157-01 migration is NOT run here — deferred to the 157-05 authorized provisioning list.
  </action>
  <verify>
    <automated>bash scripts/check-i18n-parity.sh && grep -nE "credentialSubject|user_id|getUserId|credential_json" app/templates/verify.php || echo "NO-LEAK-GREP-CLEAN"</automated>
  </verify>
  <done>verify.php renders all 4 statuses + missing-name info-box via `$l->t()`; no recipient/user_id token anywhere in the template; i18n parity green across 5 langs; deployed via --php-only.</done>
</task>

</tasks>

<verification>
- PublicVerifyControllerTest GREEN + PHPStan L5 clean.
- `check-i18n-parity.sh` green (5-lang); `.js` regenerated from `.json` (not hand-edited).
- Leak grep on verify.php + PublicVerifyController.php shows no recipient-identity tokens on added lines.
- Route `publicVerify#verify` live in the PAGE section (`occ router:list learning | grep verify`).
- Visual eyeball of the 4 banners + RTL deferred to the demo-course pass (option A) — flag, don't skip.
</verification>

<success_criteria>
- Logged-out reachable page (VERIFY-01), shows status/issuer/course/dates (VERIFY-02).
- UUID precheck before DB + throttle-on-unknown + AnonRateLimit + generic page for malformed==not-found (VERIFY-06).
- Pure server-render, no Vue island, no recipient leak.
</success_criteria>

<output>
After completion, create `.planning/phases/157-public-verify/157-04-SUMMARY.md`.
</output>
