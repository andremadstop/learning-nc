---
phase: 157-public-verify
plan: 03
type: execute
wave: 2
depends_on: ["157-01"]
files_modified:
  - app/lib/Controller/CertificateController.php
  - app/appinfo/routes.php
  - app/tests/Unit/Controller/CertificateRevokeTest.php
  - app/src/components/CourseTabTeilnehmer.vue
  - app/l10n/de.json
  - app/l10n/en.json
  - app/l10n/fr.json
  - app/l10n/ru.json
  - app/l10n/ar.json
autonomous: true
requirements: [VERIFY-05]
must_haves:
  truths:
    - "An instructor who owns the course can revoke one of its certificates; the row persists (revoked=true) for the tombstone — it is NOT deleted"
    - "Revoke sets revoked=true + revoked_at + active_idem_key=NULL together; the FIRST revoked_at is kept on a repeat revoke (idempotent)"
    - "The owner gate (assertInstructorOfCourse) runs BEFORE any write; a foreign cert returns a uniform 404 (no 403 existence oracle)"
    - "An instructor sees a Widerrufen button per row in the compliance table"
  artifacts:
    - path: "app/lib/Controller/CertificateController.php"
      provides: "revoke(verificationId) — authenticated, owner-gated, idempotent"
      contains: "setActiveIdemKey(null)"
    - path: "app/tests/Unit/Controller/CertificateRevokeTest.php"
      provides: "owner-gate-before-write + idempotent revoked_at + active_idem_key null"
      contains: "assertInstructorOfCourse"
    - path: "app/src/components/CourseTabTeilnehmer.vue"
      provides: "per-row Widerrufen button wired to the revoke endpoint"
      contains: "revokeCertificate"
  key_links:
    - from: "app/lib/Controller/CertificateController.php"
      to: "CourseService::assertInstructorOfCourse"
      via: "owner gate BEFORE the update write"
      pattern: "assertInstructorOfCourse"
    - from: "app/src/components/CourseTabTeilnehmer.vue"
      to: "/api/certificates/{vid}/revoke"
      via: "axios POST then refetch certRows"
      pattern: "/revoke"
---

<objective>
Add the revoke write-path (the only net-new capability besides the public route) and a thin
instructor button. Revocation is what produces the tombstone the verify service reads in 157-02.
This plan runs PARALLEL with 157-02 (no shared files: 157-02 owns the Service, this owns the
authenticated Controller + routes + Vue + i18n).

Purpose: Backend-complete VERIFY-05 — instructor can revoke; verification then returns "withdrawn".
Output: `CertificateController::revoke()` + route + REAL-gate PHPUnit + a Widerrufen button in the
156 compliance table + 5-lang i18n.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-public-verify/157-RESEARCH.md
@.planning/phases/157-public-verify/157-VALIDATION.md

<interfaces>
CertificateController (app/lib/Controller/CertificateController.php) currently injects
  `?string $userId` + `CertificateMapper`; has `const UUID_V4` (line 26) and the owner-scoped
  404-on-mismatch pattern. It does NOT yet inject CourseService/ITimeFactory — ADD them to the ctor.
CourseService::assertInstructorOfCourse(int $courseId, string $userId): Course   // app/lib/Service/CourseService.php:563 — throws on non-owner
CertificateMapper::findByVerificationId(string $vid): Certificate                // throws DoesNotExistException
Certificate: setRevoked(bool), setRevokedAt(?int) (157-01), setActiveIdemKey(?string), getRevoked(), getRevokedAt()
ITimeFactory::getTime(): int

Auth pattern in THIS codebase = `?string $userId` ctor param + null-guard → 401 (NOT IUserSession;
STATE 155-05 note). `@NoAdminRequired` is load-bearing (a non-admin instructor 403s without it) and
unit-uncatchable (direct instantiation bypasses the middleware) — present it + verify by inspection.

The compliance table lives in app/src/components/CourseTabTeilnehmer.vue:
  - rows: `certRows` (data, line ~553), each row has `verification_id` etc.
  - table body: `<tr v-for="row in certRows" :key="row.verification_id">` (~line 443), last column
    is the verification_id (~line 448).
  - fetch: `fetchCertReport()` (~line 886) sets `this.certRows`.
  - i18n via `t('learning', '…')`. 5-lang parity gate: `bash scripts/check-i18n-parity.sh` (repo root);
    edit all 5 l10n/*.json (DE source value==key; real EN/FR/RU/AR), regenerate .js via
    `python3 scripts/l10n_js_sync.py` (NOT hand-edited).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: revoke() endpoint + route + REAL-gate PHPUnit</name>
  <files>app/lib/Controller/CertificateController.php, app/appinfo/routes.php, app/tests/Unit/Controller/CertificateRevokeTest.php</files>
  <behavior>
    - testRevokeSetsTombstoneFields: owner revokes → cert.revoked=true, revoked_at=now, active_idem_key=NULL; mapper->update called once
    - testRevokeIdempotentKeepsFirstDate: already-revoked cert (revoked_at=T0) revoked again at T1 → revoked_at stays T0 (do NOT overwrite); still active_idem_key=NULL
    - testOwnerGateRunsBeforeWrite: non-owner (assertInstructorOfCourse throws) → mapper->update `->never()` called; returns 404 (uniform, not 403)
    - testMalformedUuid404: bad vid → 404, mapper->findByVerificationId `->never()`
    - testUnauthenticated401: userId null → 401
  </behavior>
  <action>
    1. Add `revoke(string $verificationId): JSONResponse` to CertificateController with `@NoAdminRequired`
       `@NoCSRFRequired` is NOT needed (authenticated POST keeps CSRF). Logic:
         - `if ($this->userId === null) return new JSONResponse([], 401);`
         - `if (preg_match(self::UUID_V4, $verificationId) !== 1) return new JSONResponse([], 404);`
         - `$cert = $this->mapper->findByVerificationId($verificationId);` (DoesNotExistException → 404)
         - `$this->courseService->assertInstructorOfCourse($cert->getCourseId(), $this->userId);`
           (catch its throw → uniform 404, NOT 403 — no existence oracle, consistent with show())
         - `$cert->setRevoked(true);`
         - `if ($cert->getRevokedAt() === null) { $cert->setRevokedAt($this->timeFactory->getTime()); }`  // keep FIRST
         - `$cert->setActiveIdemKey(null);`  // R2-2: free the UNIQUE slot so re-issue stays possible
         - `$this->mapper->update($cert);`
         - `return new JSONResponse(['revoked' => true, 'verification_id' => $verificationId]);`
       Add CourseService + ITimeFactory to the constructor.
    2. Register the route in the AUTHENTICATED api section of routes.php (next to the certificate#*
       routes, ~line 347):
         `['name' => 'certificate#revoke', 'url' => '/api/certificates/{verificationId}/revoke', 'verb' => 'POST'],`
    3. Write `app/tests/Unit/Controller/CertificateRevokeTest.php` per &lt;behavior&gt;. Use the REAL
       `CourseService` with its 12 mocked deps (copy the ctor wiring from CourseServiceTest, 156-01
       pattern) so the gate runs for real; add `$mapper->expects($this->never())->method('update')`
       on the non-owner test to PROVE the gate runs before the write. Mock ITimeFactory for the clock.
  </action>
  <verify>
    <automated>ssh relais 'docker exec -w /var/www/html/custom_apps/learning devcloud-app php vendor/bin/phpunit --filter CertificateRevokeTest && docker exec -w /var/www/html/custom_apps/learning devcloud-app php vendor/bin/phpstan analyse --no-progress'</automated>
  </verify>
  <done>5/5 revoke tests GREEN; PHPStan L5 clean; route live (`occ router:list learning | grep revoke`); owner-gate-before-write proven via update->never().</done>
</task>

<task type="auto">
  <name>Task 2: instructor Widerrufen button in the compliance table + i18n</name>
  <files>app/src/components/CourseTabTeilnehmer.vue, app/l10n/de.json, app/l10n/en.json, app/l10n/fr.json, app/l10n/ru.json, app/l10n/ar.json</files>
  <action>
    1. In the compliance table (`<tr v-for="row in certRows">`), add a trailing "Aktion" column with a
       small `NcButton` (or existing button style) "Widerrufen" per non-revoked row. On click call a new
       method `revokeCertificate(row)` that confirms (native `confirm()` or NcDialog — keep it thin),
       POSTs to `generateUrl('/apps/learning/api/certificates/{verificationId}/revoke', {verificationId: row.verification_id})`
       via axios, then calls `fetchCertReport()` to refresh. On success show a toast (sonner/Nc showSuccess);
       on error showError. Disable/hide the button when the row is already revoked (if the DTO exposes a
       revoked flag; if not, simply refetch — a revoked cert drops out of/marks in the report).
    2. Add the new i18n keys ("Widerrufen", "Zertifikat widerrufen?", "Zertifikat wurde widerrufen",
       "Widerruf fehlgeschlagen", "Aktion") to ALL 5 l10n/*.json — DE source value==key, real
       EN/FR/RU/AR translations. Regenerate the `.js` via `python3 scripts/l10n_js_sync.py` (do NOT
       hand-edit the .js). Run the parity gate.
    3. Build + deploy frontend: `./scripts/deploy-prod.sh --js-only`.
  </action>
  <verify>
    <automated>cd app && npx eslint --ext .js,.vue src/components/CourseTabTeilnehmer.vue && cd .. && bash scripts/check-i18n-parity.sh</automated>
  </verify>
  <done>ESLint 0 errors on the component; i18n parity green across DE/EN/FR/RU/AR (key-set + .js↔.json value-sync); button renders + wired (visual click rides the deferred demo-course pass, option A).</done>
</task>

</tasks>

<verification>
- `--filter CertificateRevokeTest` GREEN (5/5) + PHPStan L5 clean.
- ESLint 0 on CourseTabTeilnehmer.vue; `check-i18n-parity.sh` green.
- Route `certificate#revoke` live via `occ router:list learning`.
- Live credentialed revoke smoke (instructor 200 / non-owner 404) DEFERRED to the demo-course pass
  (user option A, like 156-02 Gate 2) — flag in SUMMARY, do not silently skip.
</verification>

<success_criteria>
- Owner-gated, idempotent, atomic revoke (revoked + revoked_at-first + active_idem_key=NULL) — VERIFY-05 write side.
- Foreign cert → uniform 404 (no oracle).
- Instructor button present in the 156 compliance table, i18n parity held.
</success_criteria>

<output>
After completion, create `.planning/phases/157-public-verify/157-03-SUMMARY.md`. Note the deferred
credentialed/visual revoke smoke (demo-course pass).
</output>
