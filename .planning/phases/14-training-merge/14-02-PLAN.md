---
phase: 14-training-merge
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - app/src/App.vue
  - app/src/components/CourseDetail.vue
  - app/src/components/PoolList.vue
  - app/src/components/SwipeMode.vue
autonomous: true
requirements:
  - TMERGE-02

must_haves:
  truths:
    - "SwipeMode.vue existiert nicht mehr im Repo"
    - "Alle Einstiegspunkte (App.vue Standalone, CourseDetail Tab, PoolList Button) führen zum TrainingMode"
    - "In CourseDetail gibt es keinen separaten 'Wahr/Falsch'-Tab mehr — Training-Tab startet im wfMode"
    - "Der PoolList-Button 'Wahr/Falsch' öffnet TrainingMode mit vorgesetztem wfMode=true"
  artifacts:
    - path: "app/src/App.vue"
      provides: "SwipeMode-Import entfernt, openSwipeMode -> openTrainingMode"
      contains: "openTrainingMode"
    - path: "app/src/components/CourseDetail.vue"
      provides: "SwipeMode-Import entfernt, swipe-Tab auf training umgebogen"
      contains: "wfMode"
    - path: "app/src/components/PoolList.vue"
      provides: "swipe-mode-btn emittiert openTrainingWf statt openSwipeMode"
  key_links:
    - from: "PoolList.vue swipe-mode-btn"
      to: "App.vue openTrainingWf handler"
      via: "$emit('openTrainingWf')"
      pattern: "openTrainingWf"
    - from: "CourseDetail.vue swipe-Tab"
      to: "TrainingMode.vue :wfMode prop"
      via: "activeLearningMode === 'swipe' → :wfMode=true auf <TrainingMode>"
      pattern: "wfMode"
---

<objective>
SwipeMode.vue aus dem Codebase entfernen und alle drei Einstiegspunkte auf TrainingMode umbiegen: App.vue Standalone-Modus, CourseDetail-Tab "Wahr/Falsch" und PoolList-Schnelleinstieg.

Purpose: Technische Schuld eliminieren — ein Modus statt zwei, keine duplizierte API-Logik mehr.
Output: SwipeMode.vue gelöscht, alle Referenzen auf TrainingMode zeigend, wfMode-Prop als Weiche.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-training-merge/14-01-SUMMARY.md

<interfaces>
<!-- Bestehende Referenzen auf SwipeMode die umzubiegen sind -->

App.vue:
- import SwipeMode from './components/SwipeMode.vue'          → ENTFERNEN
- components: { ..., SwipeMode }                              → ENTFERNEN
- <SwipeMode v-else-if="currentView === 'swipeMode'" ...>     → <TrainingMode :wfMode="true" :poolId="0" :totalQuestions="0" ...>
- <SwipeMode v-else-if="mode === 'swipe'" ...>                → <TrainingMode :wfMode="true" ...>
- modes: [{ id: 'swipe', label: 'Wahr/Falsch' }]             → { id: 'swipe', label: 'Wahr/Falsch' } bleibt, aber rendert TrainingMode
- openSwipeMode() { this.currentView = 'swipeMode' }         → openTrainingWf() { this.currentView = 'training'; this.trainingWfMode = true; }
- virtuprof context 'pool-swipe'                              → 'pool-training' (oder belassen)

CourseDetail.vue:
- import SwipeMode from './SwipeMode.vue'                     → ENTFERNEN
- components: { ..., SwipeMode }                              → ENTFERNEN
- <SwipeMode v-else-if="activeLearningMode === 'swipe'" ...> → zu TrainingMode mit :wfMode="true" umbiegen
  ODER: Tab 'swipe' auf 'training' umbenannt + activeLearningMode='training' mit wfMode-Signal
- isStudentLearningTab check: 'swipe' bleibt oder wird zu 'training' (EINFACHER: wfMode als extra data-Feld)
- Tab visibleTabs: { id: 'swipe', label: 'Wahr/Falsch' } → entweder Tab bleibt und TrainingMode bekommt :wfMode="activeLearningMode==='swipe'"

PoolList.vue:
- @click="$emit('openSwipeMode')" → @click="$emit('openTrainingWf')"
- App.vue: @openSwipeMode="openSwipeMode" → @openTrainingWf="openTrainingWf"

TrainingMode.vue Props (nach Plan 01):
- wfMode: { type: Boolean, default: false }   ← NEU in Plan 01 hinzugekommen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrainingMode wfMode-Prop hinzufügen + SwipeMode.vue löschen</name>
  <files>app/src/components/TrainingMode.vue, app/src/components/SwipeMode.vue</files>
  <action>
**A) TrainingMode.vue — wfMode als Prop:**

In `props` einen neuen Eintrag hinzufügen:
```js
wfMode: { type: Boolean, default: false }
```

In `data()` die Initialisierung von `wfMode` aus Plan 01 ENTFERNEN (war als data-Property) und stattdessen als prop-getriebenen Watcher eintragen:

```js
watch: {
  wfMode(newVal) {
    // Prop-Change: Reset wenn Session läuft
    if (this.session) {
      this.restartTraining();
    }
    // Swipe-State zurücksetzen
    this.cardAnimClass = ''; this.canDrag = false;
    this.isDragging = false; this.dragDeltaX = 0;
    this.showFeedback = false;
  }
}
```

HINWEIS: Da wfMode jetzt ein Prop ist, muss die lokale `data.wfMode` Eigenschaft aus Plan 01 entfernt werden. Der Toggle auf der Startseite soll weiterhin funktionieren — aber als lokale computed property oder als `localWfMode` data-Prop die mit dem Prop initialisiert wird:

```js
data() {
  return {
    localWfMode: this.wfMode,  // starts from prop, user can toggle
    // ... rest of existing data
  }
}
```

Alle Template-Referenzen auf `wfMode` → `localWfMode` umbenennen (Toggle-Buttons, v-if Bedingungen, Watcher).

**B) SwipeMode.vue löschen:**
```bash
rm app/src/components/SwipeMode.vue
```
  </action>
  <verify>
    <automated>test ! -f /home/andre/Workspace/Code/learning-nc/app/src/components/SwipeMode.vue && echo "SwipeMode.vue deleted"</automated>
  </verify>
  <done>
    - SwipeMode.vue existiert nicht mehr
    - TrainingMode.vue hat `wfMode` als Prop (default: false)
    - Intern nutzt TrainingMode `localWfMode` als toggle-bare Kopie des Props
  </done>
</task>

<task type="auto">
  <name>Task 2: App.vue und CourseDetail.vue und PoolList.vue umbiegen</name>
  <files>app/src/App.vue, app/src/components/CourseDetail.vue, app/src/components/PoolList.vue</files>
  <action>
**App.vue — 6 Änderungen:**

1. Import-Zeile `import SwipeMode from './components/SwipeMode.vue'` entfernen.

2. In `components: { ... }` den Eintrag `SwipeMode` entfernen.

3. `<SwipeMode v-else-if="currentView === 'swipeMode'" :contentLanguage="contentLanguage" @back="backToPools" />` ersetzen durch:
```html
<TrainingMode
  v-else-if="currentView === 'swipeMode'"
  :poolId="0"
  :totalQuestions="0"
  :wfMode="true"
  :contentLanguage="contentLanguage"
  @back="backToPools"
/>
```
(poolId=0 und totalQuestions=0 ist ok — SwipeMode hatte dasselbe Fallback-Verhalten mit eigenem Pool-Picker. TrainingMode zeigt dann "No questions available" oder der poolId-Prop wird 0-geprüft. BESSER: `currentView === 'swipeMode'` auf `currentView === 'training'` + `trainingWfMode` data-Feld umstellen — siehe unten.)

TATSÄCHLICHE LÖSUNG für App.vue:
- Neues data-Feld `trainingWfMode: false` hinzufügen.
- `openSwipeMode()` Methode umbenennen ODER behalten aber umleiten:
  ```js
  openSwipeMode() {
    this.trainingWfMode = true;
    this.currentView = 'training';
  },
  ```
- Bei `backToPools()` / `restartTraining()` sicherstellen dass `trainingWfMode` zurückgesetzt wird wenn nötig. Einfachste Lösung: `trainingWfMode` bleibt gesetzt bis User explizit zurück geht.
- `<SwipeMode v-else-if="currentView === 'swipeMode'">` → ENTFERNEN (currentView wird nie mehr 'swipeMode').
- Pool-Detail-Ansicht: `<SwipeMode v-else-if="mode === 'swipe'" :poolId="selectedPool.id" ...>` ersetzen durch:
  ```html
  <TrainingMode
    v-else-if="mode === 'swipe'"
    :poolId="selectedPool.id"
    :totalQuestions="questionCount"
    :wfMode="true"
    :contentLanguage="contentLanguage"
    @back="setMode('train')"
  />
  ```
- Virtuprof-Context: `'pool-swipe'` → `'pool-training'` (in dem Bereich wo der Context gesetzt wird).
- `@openTrainingWf` Event-Handler auf `<PoolList>` hinzufügen: `@openTrainingWf="openSwipeMode"` (nutzt die umgebaute openSwipeMode Methode).
- Bisheriges `@openSwipeMode="openSwipeMode"` auf `@openTrainingWf="openSwipeMode"` umbenennen.

**CourseDetail.vue — 4 Änderungen:**

1. Import `import SwipeMode from './SwipeMode.vue'` entfernen.

2. In `components: { ... }` den Eintrag `SwipeMode` entfernen.

3. Den Block:
```html
<SwipeMode
  v-else-if="activeLearningMode === 'swipe'"
  :poolId="selectedLearningPool.pool_id"
  :courseId="courseId"
  :totalQuestions="selectedLearningPoolQuestionCount"
  :contentLanguage="contentLanguage"
  @back="resetLearningPoolSelection" />
```
ersetzen durch:
```html
<TrainingMode
  v-else-if="activeLearningMode === 'swipe'"
  :poolId="selectedLearningPool.pool_id"
  :courseId="courseId"
  :totalQuestions="selectedLearningPoolQuestionCount"
  :wfMode="true"
  :contentLanguage="contentLanguage"
  @back="resetLearningPoolSelection" />
```

4. `activeLearningModeLabel` computed: `swipe` Key hinzufügen falls fehlend:
```js
swipe: t('learning', 'Wahr/Falsch'),
```

Der Tab `{ id: 'swipe', label: t('learning', 'Wahr/Falsch') }` in `visibleTabs` bleibt unverändert. `isStudentLearningTab` computed enthält bereits `'swipe'` — bleibt unverändert.

**PoolList.vue — 1 Änderung:**

Zeile 36: `@click="$emit('openSwipeMode')"` → `@click="$emit('openTrainingWf')"`.
  </action>
  <verify>
    <automated>ssh learning-dev 'cd ~/learning-nc/app && npm run build 2>&1 | tail -10'</automated>
  </verify>
  <done>
    - `npm run build` fehlerfrei (kein Fehler wegen fehlenden SwipeMode-Imports)
    - App.vue: kein SwipeMode-Import, kein SwipeMode-Component
    - CourseDetail.vue: kein SwipeMode-Import, swipe-activeLearningMode rendert TrainingMode mit :wfMode="true"
    - PoolList.vue: emittiert 'openTrainingWf'
    - grep -r "SwipeMode" app/src/ liefert 0 Treffer
  </done>
</task>

</tasks>

<verification>
Nach `npm run build` und Deploy auf learning-dev:

1. Standalone Wahr/Falsch (PoolList-Button): Klick öffnet TrainingMode im WF-Modus (Karten-Layout sichtbar, kein Error)
2. Pool-Detail-Ansicht → Tab "Wahr/Falsch": TrainingMode mit Karten öffnet sich
3. CourseDetail → Tab "Wahr/Falsch": Karten-Layout erscheint nach Pool-Auswahl
4. `grep -r "SwipeMode" app/src/` → keine Treffer
5. `test ! -f app/src/components/SwipeMode.vue` → true
</verification>

<success_criteria>
- SwipeMode.vue nicht mehr im Repo vorhanden
- `grep -r "import SwipeMode" app/src/` → 0 Treffer
- Alle drei Einstiegspunkte (PoolList-Button, App.vue Standalone, CourseDetail-Tab) rendern TrainingMode mit wfMode=true
- `npm run build` ohne Fehler
- Bestehende MC-Nutzung über CourseDetail Training-Tab unveraendert
</success_criteria>

<output>
After completion, create `.planning/phases/14-training-merge/14-02-SUMMARY.md`
</output>
