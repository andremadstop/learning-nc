---
phase: 156-compliance-report
plan: 02
type: execute
wave: 2
depends_on: ["156-01"]
files_modified:
  - app/src/utils/cert-report.js
  - app/tests/unit/CertReport.test.js
  - app/src/components/CourseTabTeilnehmer.vue
  - app/l10n/de.js
  - app/l10n/de.json
  - app/l10n/en.js
  - app/l10n/en.json
  - app/l10n/fr.js
  - app/l10n/fr.json
  - app/l10n/ru.js
  - app/l10n/ru.json
  - app/l10n/ar.js
  - app/l10n/ar.json
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03, REPORT-04]
user_setup: []

must_haves:
  truths:
    - "An instructor viewing a certifying course (cert_enabled) sees a compliance section with a table: display name, passed date, score, expiry, verification UUID — fetched from the NEW DTO endpoint (never user_id)"
    - "The instructor can set from/to passed-date + expiringDays filters; both the table fetch and the CSV download carry the SAME filter query params so they match"
    - "The Export CSV button triggers a browser download of /cert-report/export/csv with the active filter query string appended"
    - "The section is hidden for students and for non-certifying courses (no cert_enabled)"
  artifacts:
    - path: "app/src/utils/cert-report.js"
      provides: "Pure, testable helpers: buildCertReportQuery(filters) → query string; shouldShowCertReport(isInstructor, course) → bool; formatScore/formatDate row helpers"
      exports: ["buildCertReportQuery", "shouldShowCertReport"]
    - path: "app/tests/unit/CertReport.test.js"
      provides: "Vitest: query-string build (empty filters → no stray params), cert_enabled+instructor gating truth table"
      contains: "buildCertReportQuery"
    - path: "app/src/components/CourseTabTeilnehmer.vue"
      provides: "Instructor compliance section: filter inputs, table bound to certRows from the DTO endpoint, Export CSV button"
      contains: "fetchCertReport"
  key_links:
    - from: "CourseTabTeilnehmer.vue fetchCertReport()"
      to: "/apps/learning/api/courses/{courseId}/cert-report"
      via: "axios.get with from/to/expiringDays params"
      pattern: "cert-report"
    - from: "CourseTabTeilnehmer.vue exportCertReportCsv()"
      to: "/apps/learning/api/courses/{courseId}/cert-report/export/csv"
      via: "window.location.href + buildCertReportQuery(filters)"
      pattern: "cert-report/export/csv"
---

<objective>
Surface the Phase-156 backend as an instructor-only compliance section inside the existing course Teilnehmer tab: a filterable table (passed-date range + expiry window) bound to the clean DTO endpoint, plus an Export CSV button that downloads the same filtered set. Following the 155-06 lesson, all branchable logic lives in a pure util tested by Vitest; the Vue section is thin glue. Visual render + actual file download ride on the deferred demo-course check (user option A — non-blocking, same as CERT-07/08/13).

Purpose: REPORT-01 (view), REPORT-02 (filter), REPORT-03 (export) — the instructor-facing half.
Output: A working, i18n'd compliance report in the instructor course view.
</objective>

<execution_context>
@/home/andre/.claude/get-shit-done/workflows/execute-plan.md
@/home/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/156-compliance-report/156-RESEARCH.md
@.planning/phases/156-compliance-report/156-01-PLAN.md

# Vue 3.5 Options API (ALL 113 components Options-API — new code stays Options-API, NO <script setup>).
# i18n DE-source: keys are German; add real translations to ALL 5 langs (de/en/fr/ru/ar) in BOTH .js AND .json;
# regenerate .js via `python3 scripts/l10n_js_sync.py`; the parity script lives at repo-root scripts/ (NOT app/scripts).
# Vitest collects ONLY app/tests/unit/**/*.test.js — component mounting is NOT a pattern here (no @vue/test-utils);
# put branchable logic in a util and test the util (155-06 lesson).

<interfaces>
<!-- The backend contract this plan consumes (from 156-01). Use directly. -->

JSON table endpoint:  GET /apps/learning/api/courses/{courseId}/cert-report?from=&to=&expiringDays=
  → { rows: [ { display_name, passed_at (unix), score (str, '' = none), expires_at (unix|null), verification_id } ] }
  NOTE: rows carry NO user_id — render straight to the table. Do NOT reuse the 155-06 CertificateService
  listCertificates() here: that returns Certificate entities whose jsonSerialize() emits user_id + raw
  credential_json (Certificate.php:68) — using it would re-introduce the DSGVO leak client-side.

CSV download endpoint: GET /apps/learning/api/courses/{courseId}/cert-report/export/csv?from=&to=&expiringDays=
  → file download (window.location.href). Filters MUST travel as query string (GET can't POST a body).

From app/src/components/CourseTabTeilnehmer.vue (existing patterns to mirror):
```js
import { generateUrl } from '@nextcloud/router'         // line 429
import axios from '@nextcloud/axios'
computed isInstructor() { ... }                          // line 520 — already gates instructor sub-views
// at-risk precedent (lines 778-786): exportAtRiskCsv() builds generateUrl(...) → window.location.href;
//   fetchAtRisk() axios.get(...). Mirror these two verbatim for cert-report.
// course prop fields are SNAKE_CASE (jsonSerialize): course.cert_enabled (154-02 decision).
```
</interfaces>

# expiringDays UI label MUST match the chosen semantics: "expires_at <= now + N days, already-expired included,
# never-expiring excluded" → label e.g. "Läuft ab in N Tagen (inkl. bereits abgelaufene)".
# Data boundary to surface in UI copy/empty-state: the report reflects ISSUED certificates, not historical
# passes (pre-issuance passers have no cert; backfill = DIFF-03, deferred). Empty table is a valid state.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure cert-report util + Vitest (filter query + gating)</name>
  <files>app/src/utils/cert-report.js, app/tests/unit/CertReport.test.js</files>
  <behavior>
    Write Vitest first (RED), then implement:
    - buildCertReportQuery({ from, to, expiringDays }): returns a query string with ONLY the set params. Empty/null/undefined → omitted (no `from=&to=` noise). Numeric values pass through. Assert: {} → '' ; { expiringDays: 30 } → 'expiringDays=30' ; { from: 1000, to: 2000 } → 'from=1000&to=2000'.
    - shouldShowCertReport(isInstructor, course): true ONLY when isInstructor===true AND course && course.cert_enabled (snake_case) truthy. Assert the 4-cell truth table: student+cert → false; instructor+non-cert → false; instructor+cert → true; instructor+undefined-course → false.
    - (optional) formatScore(score) → score==='' ? '—' : score ; formatDate(unix) → '' when falsy else a YYYY-MM-DD string. Assert empty/null handling.
  </behavior>
  <action>
    Create app/src/utils/cert-report.js exporting buildCertReportQuery, shouldShowCertReport, and the small format helpers (pure functions, no Vue, no axios). buildCertReportQuery filters out null/undefined/'' before assembling with URLSearchParams or manual join. Keep it dependency-free so Vitest runs it in isolation (no backend needed). Author the test cases from <behavior> in app/tests/unit/CertReport.test.js (matches the `*.test.js` collection glob).
  </action>
  <verify>
    <automated>cd app && npm run test -- CertReport && npx eslint src/utils/cert-report.js tests/unit/CertReport.test.js</automated>
  </verify>
  <done>Vitest CertReport.test.js GREEN (query-string build + gating truth table + format helpers); ESLint 0 errors on both files.</done>
</task>

<task type="auto">
  <name>Task 2: Compliance section in CourseTabTeilnehmer.vue + i18n (5 langs) + deploy</name>
  <files>app/src/components/CourseTabTeilnehmer.vue, app/l10n/de.js, app/l10n/de.json, app/l10n/en.js, app/l10n/en.json, app/l10n/fr.js, app/l10n/fr.json, app/l10n/ru.js, app/l10n/ru.json, app/l10n/ar.js, app/l10n/ar.json</files>
  <action>
    1. In CourseTabTeilnehmer.vue (Options API), add an instructor-only compliance section (mirror the at-risk section visual pattern at lines 91-101 — collapsible card, NcButton export). Gate the section with shouldShowCertReport(this.isInstructor, this.course) from the util (import course prop if not already present; course.cert_enabled is snake_case). 
       - data(): certRows: [], certFrom: null, certTo: null, certExpiringDays: null, certCollapsed: false.
       - methods:
         fetchCertReport(): axios.get(generateUrl('/apps/learning/api/courses/{courseId}/cert-report', { courseId: this.courseId }), { params: { from: this.certFrom, to: this.certTo, expiringDays: this.certExpiringDays } }) → this.certRows = response.data.rows || []. Render rows directly (they carry NO user_id — do NOT route through CertificateService.listCertificates()).
         exportCertReportCsv(): const qs = buildCertReportQuery({ from: this.certFrom, to: this.certTo, expiringDays: this.certExpiringDays }); window.location.href = generateUrl('/apps/learning/api/courses/{courseId}/cert-report/export/csv', { courseId: this.courseId }) + (qs ? '?' + qs : '').
       - Filter inputs: from/to passed-date (NcDateTimePicker or date inputs → convert to unix seconds before storing in certFrom/certTo) and an expiringDays number input/select; a "Filter anwenden" action calling fetchCertReport. Table columns: Name, Bestanden am (format passed_at), Score (%) (formatScore), Gültig bis (expires_at or "—"/"unbegrenzt"), Verifizierungs-ID. Empty state: a neutral "Noch keine Zertifikate ausgestellt" note (data-boundary: issued certs only).
       - Fetch on section reveal (e.g. when the section becomes visible / a watcher or the existing tab-activation pattern lines 588-598), not on every render.
    2. i18n: add the new keys (section title, column headers, filter labels incl. the expiry semantics label, empty state, export button) to ALL 5 langs in BOTH .js and .json. DE source value == key; real EN/FR/RU/AR translations. Regenerate .js with `python3 scripts/l10n_js_sync.py`; verify with the repo-root parity script.
    3. Build + deploy frontend: `./scripts/deploy-prod.sh --js-only`.
  </action>
  <verify>
    <automated>cd app && npx eslint --ext .js,.vue src/components/CourseTabTeilnehmer.vue src/utils/cert-report.js && npm run test -- CertReport && cd /home/andre/Workspace/Code/learning-nc && bash scripts/check-i18n-parity.sh</automated>
  </verify>
  <done>ESLint 0 errors; Vitest green; i18n parity green across all 5 langs (new keys present in .js + .json); frontend builds and deploys via --js-only. Section is instructor+cert_enabled gated and consumes the clean DTO endpoint (no user_id client-side).</done>
</task>

</tasks>

<verification>
- Vitest CertReport.test.js green; ESLint 0 errors on cert-report.js + CourseTabTeilnehmer.vue.
- i18n parity green (5 langs, .js + .json).
- grep gate: `grep -n "user_id\|listCertificates" app/src/components/CourseTabTeilnehmer.vue` shows the cert-report code does NOT pull user_id or reuse the entity-returning listCertificates() for the table.
- MANUAL (folded into the deferred demo-course visual check, user option A — non-blocking, same as CERT-07/08/13): open a certifying course as instructor → compliance table renders → set filters → table updates → click Export CSV → downloaded file shows display name only (no email/user_id). Run after the demo course exists; not a blocking checkpoint.
</verification>

<success_criteria>
- REPORT-01: instructor sees the per-course compliance table (display name, passed date, score, expiry, verification UUID) from the clean DTO endpoint — no email/user_id on screen.
- REPORT-02: from/to + expiringDays filters drive both the table fetch and the CSV download via the same query string.
- REPORT-03: Export CSV downloads the filtered report (display name only).
</success_criteria>

<output>
After completion, create `.planning/phases/156-compliance-report/156-02-SUMMARY.md`.
Do NOT run gsd-tools state/roadmap/requirements commands (they corrupt v5.0.0 frontmatter) — hand-edit if needed.
</output>
