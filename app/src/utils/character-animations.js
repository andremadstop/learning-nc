/**
 * Character animation WAAPI helpers.
 * Populated in Phase 150 Plan 03 (Wave 1).
 * Every exported helper MUST gate on matchMedia('(prefers-reduced-motion: reduce)')
 * AND a11yStore.animationsEnabled manual override.
 */

export {}
