/**
 * Generic character reaction engine (event -> { animation, emotion, sound, duration }).
 * Populated in Phase 150 Plan 04 (Wave 1).
 * Non-breaking for existing nova-reaction-engine.js consumers.
 */

export {}
