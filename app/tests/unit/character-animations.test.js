import { describe, it } from 'vitest'

describe('character-animations', () => {
	it.todo('playWave returns immediately when prefers-reduced-motion: reduce matches')
	it.todo('playCelebrate returns immediately when manual override animationsEnabled=false')
	it.todo('playShrug exports a callable helper')
})
