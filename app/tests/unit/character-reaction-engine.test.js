import { describe, it } from 'vitest'

describe('character-reaction-engine', () => {
	it.todo('resolveReaction returns object for each known event')
	it.todo('resolveReaction falls back to { animation: "idle" } for unsupported state')
	it.todo('resolveNovaReaction wrapper preserves legacy Nova API')
})
